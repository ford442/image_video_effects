# Community Preset-Pack Gallery — UI Wiring Session

## Objective
Wire `CommunityGallery` into the live UI so users can publish their active multi-slot shader chain, browse community packs, and apply them via the existing `onApplySharedChain` path.

## Files touched this session
- `src/hooks/useShareChain.ts` — added `getCurrentChain`
- `src/hooks/useShareChain.test.ts` — new tests for `getCurrentChain`
- `src/components/controls/types.ts` — `getCurrentChain` prop
- `src/components/controls/ControlsContainer.tsx` — forward `getCurrentChain` to `AiVjStudioPanel`
- `src/components/controls/panels/AiVjStudioPanel.tsx` — mount `CommunityGallery` beside `PresetPackGallery`
- `src/components/Controls.tsx` — thin re-export of `ControlsContainer`
- `src/components/CommunityGallery.tsx` — play-count sort, card metadata, optimistic publish insert
- `src/components/CommunityGallery.test.tsx` — updated list URL expectation
- `src/services/communityGallery.ts` — `sortBy` query param
- `src/services/communityGallery.test.ts` — sortBy client test
- `src/components/app/AppShell.tsx` — pass `getCurrentChain` to Controls
- `src/App.tsx` — hook imports, dedupe share-chain path, `AppShell` layout, pass `getCurrentChain`
- `src/components/Controls.test.tsx` — mount wiring test
- `storage_manager/app.py` — `sort_by=date|play_count` on list endpoint
- `storage_manager/tests/test_preset_packs.py` — play-count sort test (+1)

## Verification
- `npx tsc --noEmit` — `src/` clean (only pre-existing `node_modules/@xenova/transformers` noise)
- `CI=true npx react-scripts test --watchAll=false` — **31 suites, 268 tests passed** (baseline ~254; +14)
- `PYTHONPATH=/root/image_video_effects storage_manager/.venv/bin/python -m pytest storage_manager/tests/test_preset_packs.py -q` — **23 passed** (was 22)

## Round-trip proof
- **Backend contract (pytest, mocked GCS):** `test_publish_happy_path`, `test_published_chain_decodes_to_original`, `test_list_sort_by_play_count` — publish → list → fetch → play all green.
- **Frontend client (Jest, mocked fetch):** `communityGallery.test.ts` publish/list/round-trip + `CommunityGallery.test.tsx` publish/apply flows green.
- **Live uvicorn on :8001:** POST/GET failed with `500` — real GCS credentials not available in Cloud VM (`'NoneType' object has no attribute 'blob'`). Expected in this environment; mocked pytest is the authoritative E2E proof here.
- **UI mount:** `Controls.test.tsx` confirms **Community Gallery** and **Preset Packs** render when `onApplySharedChain` + `getCurrentChain` are provided (`inputSource="image"`).

## Open decisions / next iteration
- App.tsx still has some legacy duplicate handlers (`handleLoadImage` vs `useImageLoading`, manual `startWebcam` vs `useWebcam`) that predate this session; tests pass but a future cleanup wave could remove them.
- Live browser proof against production `storage.noahcohn.com` requires network access outside the Cloud VM sandbox.
- Gallery visible only when `inputSource === 'image'` (same gate as `AiVjStudioPanel`).

## Status: COMPLETE
