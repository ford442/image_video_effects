## #1312-A thumbs cliff — iteration 1 — 2026-09-26T15:48Z
- done: validator hardening (a44947cb), writer scripts/defer-thumbnail.js (a7962158), tests (dde4b9c9), 09-29 disposition (dd5b380b), docs incl. writer/disposition/next expiry (4459e347), PNG reconciliation (f8ba344f)
- next: nothing required. Follow-ups for the owner: next expiry 2026-10-26 (warning from 10-19, gate fails 10-27); ratchet 234 vs target 200
- blocked: `npm run verify:toolchain-foundation` stops at verify:catalog-counts (manifest 1370 vs lists 1373; gen-bismuth-hyper-crystals, gen-hyper-geometric-void-glass-seraph, gen-sentient-holographic-neuro-lace-matrix missing from manifest) — out of scope, not touched. Every other stage incl. verify:thumbs-deferrals passes (run individually)
- bucket counts: reclassified 0 / renewed 234 (generative 212 + visual-effects 22; attract 0 — none deferred) / removed 835 / remaining pending 234 / new ratchet 234 (was 1069)
- verify-thumbs-deferrals exit codes: today 0; THUMBS_DEFERRALS_NOW=2026-09-30 0 (mission's Date-mock repro also 0); THUMBS_DEFERRALS_NOW=2026-10-27 **1** (expected per the +30 rule: renewals expire 10-26; the 7-day warning shows 10-19..10-26, exit 0)
- thumbs:test 48/48 pass; check-thumbnail-coverage --base-ref=origin/main passes (283 healthy / 234 deferred / 855 missing); Jest attractShowcasePool 8/8
- files changed: scripts/defer-thumbnail.js(+test), scripts/lib/thumbnailDeferrals.js, scripts/verify-thumbs-deferrals.js, package.json (thumbs:test), reports/thumbnail_deferrals.json, reports/thumbnail_deferral_ratchet.json, reports/thumbnail_coverage.md, docs/THUMBNAIL_PIPELINE.md, .swarm-state.md
- note: verify stages rewrite generated_at in public/shader-id-aliases.json + src/utils/shader-id-aliases.json and audit rewrites reports/thumbnail_integrity_audit.json generated_at; all reverted (out of scope / timestamp only)

---

# Swarm Save-State — RendererManager + StoragePanel strangler (#1079)

Mission: Mirror WebGPU-split philosophy — extract pure seams from RendererManager and StoragePanel with Jest tests; keep facades thin; zero behavior change.

## Iteration 0 — baseline (2026-08-08)

- Branch: `cursor/renderermanager-storage-strangler-f2e5`
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **71 suites, 486 passed, 1 skipped** (487 total)
- `RendererManager.ts`: **987 LOC** (target ≤350)
- `StoragePanel.tsx`: **738 LOC** (target ≤400 or panel-composed)

## Iteration 1 — COMPLETE (2026-08-08)

### RendererManager strangler
- **New seams** (pure, colocated Jest):
  - `src/renderer/backendLifecycle.ts` — create/destroy/switch, `?renderer=` URL, exclusive WebGPU release
  - `src/renderer/slotOrchestration.ts` — duck-typed slot enable/mode/params, loadShader/resync
  - `src/renderer/inputSourceBridge.ts` — image/video/webcam/depth handoff (#887 duck-types)
  - `src/renderer/performanceStatus.ts` — format tier / FP32 pin / pass budget
  - `src/renderer/rendererDiagnostics.ts` + `rendererTypes.ts` — diagnostics/types helpers
- **Facade**: `RendererManager.ts` **344 LOC** (≤350 target)
- Contracts preserved: no WASM auto-fallback; feedback order untouched; all public IRenderer/app API methods retained

### StoragePanel strangler
- **New panels** under `src/components/storage/panels/`:
  - `StorageListBrowser.tsx` — list/browser tabs (shaders/images/videos/audio/library/operations)
  - `StorageDetailPanel.tsx` — preview modal
  - `StorageRatings.tsx` — `useShaderRating` hook
  - `StorageUploadPanel.tsx` — drag-drop upload surface
  - `OperationsPanel.tsx` — operations queue UI
- **Facade**: `StoragePanel.tsx` **200 LOC** (panel-composed)
- Library tab now uses `StorageClient.listLibrary()` (no raw `fetch` to `/api/songs`)

### Verify (iteration 1)
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **76 suites, 506 passed, 1 skipped** (507 total; +5 suites, +20 tests)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully**
- `npm run verify:dependency-boundaries`: **green**

---

# Swarm Save-State — WebGPU hard-fail boot probe (#1107)

Mission: WebGPU required; structured ladder probe → window.webgpuProbe + canvas-slot overlay; no WebGL/auto Canvas2D fallback.

## Iteration 0 — baseline (2026-08-15)

- WIP already in tree: `webgpuBootProbe.ts`, `WebGpuProbeFailureOverlay.tsx`, handoff, no auto JS fallback
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **80 suites, 532 passed, 1 skipped** (533 total)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully** (pre-finish baseline)
- `npm run verify:dependency-boundaries`: **green**
- `RendererManager.ts`: **356 LOC** (over ≤350)

## Iteration 1 — COMPLETE (2026-08-15)

### Changes
- Mounted `WebGpuProbeFailureOverlay` in `WebGPUCanvas` canvas slot only + CSS
- `initializeWebGPUDevice` publishes probe when no handoff (switch/re-init path)
- Exhaustion summary uses last attempt's `failedStage`/`error` when not pure adapter-null
- Docs: `docs/WEBGPU_BOOT_PROBE.md` + BINDING_CONTRACT link
- Tests: overlay mount, publish breadcrumb shape, success-path handoff
- Extracted `RendererInitOptions` / `WebGpuProbeHandoff` re-exports to `backendLifecycle.ts`
- `RendererDiagnostics.webgpuProbe` optional field for diagnostics breadcrumb
- Facade **348 LOC** (≤350)

### Verify (iteration 1)
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **81 suites, 535 passed, 1 skipped** (536 total)
- `SKIP_WASM_BUILD=1 npm run build`: (see iteration 2)
- `npm run verify:dependency-boundaries`: (see iteration 2)

## Iteration 2 — closeout (2026-08-15)

### Verify
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **81 suites, 535 passed, 1 skipped** (536 total)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully**
- `npm run verify:dependency-boundaries`: **green**
- `RendererManager.ts`: **348 LOC**

### Acceptance
- Failed probe → canvas-slot overlay + full attempt log
- No WebGL; no auto Canvas2D fallback (explicit `?renderer=js` only)
- Chrome/Edge delta from `window.webgpuProbe` (`userAgentBrands` + attempts)
- Success path: handoff, single requestDevice
- Real-GPU visual QA of overlay remains workstation-side (Cloud VM has no adapter)

---

# Swarm Save-State — stop second WebGPU devices (#1126)

Mission: Eliminate secondary `requestAdapter`/`requestDevice` in ShaderValidator, ShaderScanner, and depth loader; adopt boot-probe device via registry + `RendererManager.getDevice()`.

## Iteration 0 — baseline (2026-08-23)

- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **81 suites, 545 passed, 1 skipped** (546 total)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully**
- `npm run verify:dependency-boundaries`: **green**
- `RendererManager.ts`: **348 LOC**
- **Next:** device registry + RendererManager.getDevice()

## Iteration 1 — device registry + consumers (2026-08-23)

### Files touched
- `src/utils/adoptedGpuDevice.ts` (NEW; replaced `requestPixelocityDevice.ts`)
- `src/renderer/RendererManager.ts` — `getDevice()`, adopt/release on backend switch
- `src/components/ShaderScanner.tsx` — adopt registry device, `WebGpuProbeFailureOverlay` idle
- `src/components/ShaderValidator.tsx` — boot-probe-once for `?validator`, adopt device, no per-shader destroy
- `src/services/depthEstimation/loader.ts` — probe gate, `rendererDeviceActive`
- `src/hooks/useDepthEstimation.ts` — pass `rendererDeviceActive`
- `docs/GPU_CHORES.md`, `docs/BINDING_CONTRACT.md`
- Tests: `ShaderScanner.test.tsx`, `ShaderValidator.test.tsx`, `depthEstimation/__tests__/loader.test.ts`

### Verify (closeout)
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **84 suites, 550 passed, 1 skipped** (551 total; +3 suites, +5 tests)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully**
- `npm run verify:dependency-boundaries`: **green**
- `RendererManager.ts`: **353 LOC** (≤~350 target; +5 net for `getDevice` + adopt hooks)

### Acceptance (#1126)
- Validator/Scanner adopt boot-probe device — zero `requestAdapter`/`requestDevice` when handoff registered
- Depth availability uses `window.webgpuProbe.ok` — zero `requestAdapter`
- Transformers adopt-or-cpu note in loader + docs
- gpu-chores unchanged (`attach` only)
- Boot probe / overlay / feedback order untouched

# Swarm Save-State — TS/C++ workgroup + packing parity (#1180)

## Iteration 0 — baseline (2026-08-29)

- Jest: **84 suites**, **560 tests** (559 passed, 1 skipped)

## Iteration 1 — COMPLETE (2026-08-29)

### Files touched
- `src/contracts/workgroup_dispatch.json` (new)
- `scripts/verify-device-policy-sync.js` — `verifyWorkgroupDispatch()` (TS import/fallback, C++ ParseWorkgroupSize literals, catalog 8×8 ban)
- `src/renderer/ShaderCompilation.ts` — unparsed fallback from contract (16×16)
- `src/renderer/ShaderCompilation.workgroup.test.ts` — 16×16 assertion + contract regression
- 26 `public/shaders/*.wgsl` — 24 migrated 8×8→16×16; `crystal-freeze`/`neon-pulse` comment-only; `neon-pulse-edge` bounds guard added
- `wasm_renderer/resources.cpp` — emptyTexture_ explicit r32float format/usage, single-float upload (bytesPerRow=4)
- `src/renderer/webgpu/resources.test.ts` — emptyTex r32float packing regression
- `docs/BINDING_CONTRACT.md` — Canonical dispatch subsection

### Verify
- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **84 suites, 561 passed, 1 skipped** (562 total; +2 tests)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully**
- `npm run verify:device-policy`: **PASS** (workgroup_dispatch ↔ TS/C++/shaders)
- `npm run verify:uniforms`: **PASS**
- `npm run verify:dependency-boundaries`: **PASS**
- `npm run audit:extrabuffer`: **0 new, 93 known**
- `npm run audit:dead-sliders`: **0 new, 28 known**
- `python3 scripts/wgsl_precommit_gate.py --files` (26 shaders): **26/26 PASS**
- `npm run wasm:build`: **PASS** (emcc 6.0.6; C++ placeholder fix compiled)

### Decisions
- C++ does not import JSON at runtime; CI regex on `ParseWorkgroupSize` defaults matches contract (same pattern as limits).
- `neon-pulse-edge.wgsl` was the only shader in the set missing a resolution bounds guard.

### Unverified
- WASM emptyTexture_ upload on a real discrete GPU (headless VM has no WebGPU adapter). Code reviewed against TS `resources.ts` parity.

### Definition of done
- All checklist items satisfied.

---

# Swarm Save-State — WASM runtime invariants (un-regressable gates)

Mission: Lock six already-fixed Pascal/Chrome bugs behind contracts + CI. Do not re-fix, do not rebuild wasm, do not close issues, HOLD go.1ink.us.

## Iteration 0 — baseline (2026-09-05) before this change set

- `npm ci --ignore-scripts`: **exit 0**
- `npx tsc --noEmit`: **clean** (exit 0)
- `CI=true npx craco test --watchAll=false`: **Test Suites: 93 passed, 93 total / Tests: 1 skipped, 619 passed, 620 total**

## Iteration 1 — COMPLETE (2026-09-05)

### Files
- NEW `src/contracts/wasm_runtime_invariants.json`
- NEW `scripts/wasm_import_table.js` (wasm section id 2)
- `scripts/verify-device-policy-sync.js` — extended; `--wasm-invariants`
- NEW `src/gpuChores/dispatch.ts`, `dispatchLimits.ts`, `dispatchLimits.test.ts`
- `src/gpuChores/GpuChoresHost.ts` — five `dispatchWorkgroupsSafe` sites
- `src/gpuChores/gpuChores.test.ts`, `src/gpuChores/index.ts`
- `tests/helpers/rendererHarness.ts`, `tests/wasm-renderer.smoke.spec.ts`
- NEW `docs/WASM_RUNTIME_INVARIANTS.md`, `reports/wasm_promotion_evidence.md`
- `docs/GPU_CHORES.md`, `package.json` (`verify:wasm-invariants` appended to `verify:toolchain-foundation`)

### Bite-proofs (reintroduced then reverted; wasm_renderer left at HEAD)

- `wgpuSurfacePresent(` in `device.cpp` → fail: `must not call wgpuSurfacePresent`
- Drop `samplerDesc.maxAnisotropy = 1` → fail: must assign maxAnisotropy
- Crafted wasm import `wgpuSurfacePresent` → reader lists it; committed artifact has 0 present imports
- `HISTORY_FULL_WORKING_SIZE = 4096` → fail: ladder mismatch 4096 vs 2048
- `assertDispatchWithinLimits(65536,1,1)` throws (Jest `dispatchLimits.test.ts`)
- Rename `RewriteWgslStorageFormats(` → fail: must define RewriteWgslStorageFormats
- Remove `rebindMediaAfterBackendSwitch(` from RendererManager → fail: must call after switch
- Reduce pass uses raw `dispatchWorkgroups` → fail: found 4 of 5 `dispatchWorkgroupsSafe`

### Verify (iteration 1)

- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false`: **Test Suites: 94 passed, 94 total / Tests: 1 skipped, 625 passed, 626 total** (+1 suite dispatchLimits, +6 tests vs iteration 0)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully.**
- `npm run verify:device-policy`: **✅ Device policy sync OK (... + wasm_runtime_invariants ↔ TS/C++/shaders/wasm)**
- `npm run verify:wasm-invariants`: **✅ Device policy sync OK (... wasm_runtime_invariants ...)**
- `npm run wasm:validate`: **✅ All WASM artifacts are valid!**
- `npm run verify:uniforms`: **✅ Uniforms layout contract in sync**
- `npm run verify:dependency-boundaries`: **passed**
- `npm run audit:extrabuffer`: **New violations: 0 / Known: 84**
- `npm run audit:dead-sliders`: **New dead sliders: 0 / Known: 26**

### GPU-PENDING (cannot close from here)

All six: chores 65536, present abort, maxAnisotropy, #1204 historyTex, #1205 format rewrite, #1206 media rebind. See `reports/wasm_promotion_evidence.md`.

### If interrupted now

Gates are green. Next for a maintainer: Pascal/Chrome confirm of the six, then decide go.1ink.us. Do not rebuild wasm on this VM (emcc 3.1.56 / no emdawn).

---

# Swarm Save-State — Jest WASM bridge resolution (#1234)

Mission: Make `npx craco test --watchAll=false --ci --testPathPattern='WASM'` exit 0 without changing the emitted browser-ESM `.js` specifier contract. Do not close #1234.

## Iteration 0 — MEASUREMENT ONLY (2026-09-13)

- Branch: `main` (clean at start). Node v24.5.0, Jest 27.5.1 via craco/react-scripts.
- `npm ci --ignore-scripts`: **ok** (1413 packages). Plain `npm ci` still blocked on `sharp` libvips behind the proxy.
- Merged Jest `moduleNameMapper` (craco `createJestConfig`, pre-fix):
  - `"^react-native$": "react-native-web"`
  - `"^.+\\.module\\.(css|sass|scss)$": "identity-obj-proxy"`
  - `"^(\\.{1,2}/.*)\\.js$": "$1"`  ← already present, last key
- `moduleFileExtensions`: `web.js, js, web.ts, ts, web.tsx, tsx, json, web.jsx, jsx, node` (`js` before `ts`)
- `roots`: `["<rootDir>/src"]`
- `CI=true npx craco test --watchAll=false --ci`:
  - **Test Suites: 97 passed, 97 total**
  - **Tests: 1 skipped, 662 passed, 663 total**
  - **Failures: 0**
  - Owner-note “652 pass / 6 fail” is **stale on this machine**. Do not carry it forward.
- `npx craco test --watchAll=false --ci --testPathPattern='WASM'`:
  - **Test Suites: 6 passed, 6 total / Tests: 49 passed, 49 total**
  - Suites: `WASMBridge.uniforms.test.ts` (real barrel + `resetModules` + dynamic import), `WASMBridge.test.ts` (mocked), `WASMRenderer.input.test.ts`, `WASMRenderer.parity.test.ts`, `wasmInitDiagnostics.test.ts`, `src/wasm/bridge/wgslFormat.test.ts`
  - Verbatim resolution error: **none**. Real-bridge uniforms suite logs `[WASM] Loading from: /wasm/pixelocity_wasm.js` and completes init against the mocked `PixelocityWASM` factory.
- No `Haste module naming collision` in the log. No `Configuration error` / `Could not locate module './bridge/api.js'`.
- **Bisect so far (no code change):** candidate 1 (`$1` extensionless + js-before-ts) did **not** fire on Jest 27.5.1 here — `$1` then extension search finds `api.ts`. Candidates 2–5 also did not produce a red path. Historical agent reds look like a cached “6 known fail” story, not current main.

### If interrupted now

Iteration 0 complete. Resume at iteration 1: still apply the issue’s durable Jest-only mapper (`$1.ts` + `<rootDir>`-anchored, prepend), then useWASM types, docs, regression test, CI report. Baseline to not drop below: **97 suites / 662 passed / 1 skipped**.

## Iteration 1 — bisect + mapper (2026-09-13)

### Bisect result / root cause

1. **`$1` extensionless is what currently works** on Jest 27.5.1: map `./bridge/api.js` → `./bridge/api`, then `moduleFileExtensions` finds `api.ts`.
2. **`$1.ts` globally is the trap that actually goes red.** Applied first; WASM pattern died with:
   ```
   Configuration error:
   Could not locate module ./cjs/react-is.development.js mapped as: $1.ts.
   ```
   (`node_modules/react-is/index.js` relative CJS). First-match + `$1.ts` is too broad.
3. `wasm_bridge.d.ts` haste collision: **not observed**.
4. Dynamic `import()` after `resetModules`: **not the failure** (uniforms suite already passed in iteration 0).
5. `transformIgnorePatterns` / emitted copies: **not involved** (`roots` is `<rootDir>/src`).

**Fix applied in `craco.config.js`:** prepend `<rootDir>`-anchored WASM mappers so `./bridge/api.js` and `./capture.js` (etc.) hit `.ts` files explicitly; keep `'^(\\.{1,2}/.+)\\.js$': '$1'` **last** as the extensionless fallback. Do not use `$1.ts` globally.

Final mapper order:
- `^src/wasm/bridge/(.+)\\.js$` → `<rootDir>/src/wasm/bridge/$1.ts`
- `^src/wasm/wasm_bridge\\.js$` → `<rootDir>/src/wasm/wasm_bridge.ts`
- `^\\./bridge/(.+)\\.js$` → `<rootDir>/src/wasm/bridge/$1.ts`
- `^\\./(capture|diagnostics|init|recording|shader|state|uniforms|wgslFormat)\\.js$` → `<rootDir>/src/wasm/bridge/$1.ts`
- CRA `react-native` / CSS modules
- `'^(\\.{1,2}/.+)\\.js$': '$1'` fallback

Source `.js` specifiers **unchanged**. Emit contract intact (`verify:wasm-bridge-sync` green).

## Iteration 2 — types, docs, regression (2026-09-13)

- Deleted local `interface WASMBridge` in `src/hooks/useWASM.ts`. `bridgeRef` / `getBridge` typed as `WasmRenderer` (`export type WasmRenderer = typeof wasmBridge` on the TS barrel). Hook return shape unchanged. Did not add `setMouseDown` to the barrel (would change emit); hook never called it.
- Docs SoT: edit `src/wasm/bridge/*.ts`; never edit `public/wasm/bridge/*.js` / `wasm_renderer/bridge/*.js`. Files: `useWASM.ts`, `wasm_renderer/README.md`, `WASM_BUILD_CI_GUIDE.md`, `wasm_renderer/ARTIFACTS.md`.
- New `src/__tests__/WASMBridge.resolve.test.ts` (static + `resetModules` dynamic import). Name matches `WASM` pattern.

## Iteration 3 — CI wasm job (report only)

- Recent main run `34728608810` (`Implement Sentient Void-Silk Nebula generative shader (#1255)`): **wasm job success**. Steps 6 Build WASM renderer **success**, 9 Run WASM unit smoke tests **success**. The gate **reaches Jest and is currently green-on-test**, not red-on-build.
- `#1237` still applies as **latent** risk: `mymindstorm/setup-emsdk` `version: latest` runs **before** Jest. If latest emcc/emdawn fails, the job would be **red-on-build** and never reach the Jest step.
- CI change: one-line echo before the WASM Jest command so a failure is labeled. **No `|| true`.** Did not pin emcc. Did not close #1234.

## Verify (iteration 2/3 closeout)

- `npx tsc --noEmit`: **clean**
- `CI=true npx craco test --watchAll=false --ci`: **Test Suites: 98 passed, 98 total / Tests: 1 skipped, 664 passed, 665 total** (baseline 97/662; +1 suite +2 tests from resolve regression; 0 failures)
- `npx craco test --watchAll=false --ci --testPathPattern='WASM'`: **7 passed, 7 total / 51 passed, 51 total** (was 6/49)
- `SKIP_WASM_BUILD=1 npm run build`: **Compiled successfully.**
- `npm run verify:wasm-bridge-sync`: **✅ WASM bridge copies match src/wasm TypeScript source of truth**
- `npm run wasm:validate`: **✅ All WASM artifacts are valid!**
- `npm run verify:device-policy`: **✅ Device policy sync OK**
- `npm run verify:wasm-invariants`: **✅ Device policy sync OK (... wasm_runtime_invariants ...)**
- `npm run verify:uniforms`: **✅ Uniforms layout contract in sync**
- `npm run verify:dependency-boundaries`: **passed**
- `npm run audit:extrabuffer`: **AUDIT PASS** (New violations: 0)
- `npm run audit:dead-sliders`: **AUDIT PASS** (New dead sliders: 0)

### Still open

- GitHub issue **#1234 is not closed** (per instructions).
- **#1237** emsdk `latest` still first in the wasm job (latent red-on-build).
- No GPU confirmation (no adapter on this VM).

### If interrupted now

Work for #1234 is done in tree, uncommitted. All listed gates green. Next person: review `craco.config.js` mapper, `useWASM.ts` types, `WASMBridge.resolve.test.ts`, then commit if the owner wants it. Do not rebuild `public/wasm/pixelocity_wasm.{js,wasm}` on this VM.


