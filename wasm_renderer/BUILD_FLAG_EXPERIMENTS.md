# WASM build flag experiments

Auto-generated by `measure_wasm_build_flags.sh` on 2026-07-19 06:35 UTC.

| Variant | .wasm bytes | .js bytes |
|---------|------------:|----------:|
| baseline-default-memory | 129731 | 76696 |
| initial-memory-64mib | 129731 | 76693 |
| no-asyncify-size-only | 89593 | 71168 |

## Analysis (2026-07-19, Emscripten 6.0.3, `-O2`)

### `-sINITIAL_MEMORY=67108864` (64 MiB)

| Metric | Default (16 MiB implicit) | 64 MiB initial |
|--------|---------------------------|----------------|
| `.wasm` size | 129,731 B | 129,731 B (identical) |
| `.js` glue | 76,696 B | 76,693 B (−3 B) |

**Decision: reject as default.** No binary-size win; raises baseline heap reservation 4× for every session. `ALLOW_MEMORY_GROWTH` already covers 2048² rgba32f stacks. Revisit only if discrete-GPU first-frame profiling shows measurable growth thrash.

First-frame latency was not measured in CI (requires browser + GPU); use `performance.memory` / Chrome tracing on a 2048² canvas if revisiting.

### `-sASYNCIFY`

| Metric | With ASYNCIFY | Without (size-only; init broken) |
|--------|---------------|----------------------------------|
| `.wasm` size | 129,731 B | 89,593 B |
| Delta | — | **−40,138 B (−31%)** |

ASYNCIFY is required for `wgpuInstanceWaitAny` + emdawn callback mode (`device.cpp`). **Keep ASYNCIFY** until emdawn ships a non-ASYNCIFY WaitAny path. Long-term watch: emdawn sync adapter/device APIs.

### CMake vs `build.sh`

CI and `npm run wasm:build` use **`build.sh` only**. CMake (`CMakeLists.txt`) is IDE/fallback — two-step emdawn historically broke with `-sUSE_WEBGPU=1` leakage.
