<<<<<<< SEARCH
    it('shows connection status', async () => {
      render(<StorageBrowser />);

      await waitFor(() => {
        expect(screen.getByText(/Connected to VPS/i)).toBeInTheDocument();
      });
    });
=======
    it('shows connection status', async () => {
      // Mock the StorageService to return true to simulate connection
      jest.spyOn(StorageService, 'checkHealth').mockResolvedValue({ status: 'ok', service: 'test' });
      render(<StorageBrowser />);

      await waitFor(() => {
        expect(screen.getByText(/Connected to VPS/i)).toBeInTheDocument();
      });
      jest.restoreAllMocks();
    });
>>>>>>> REPLACE
