# image_video_effects — Weekly Plan

## Today's focus
<!-- Routine writes here each run. You can delete after day ends, or keep as history. -->
**2026-08-29 — Close the TS↔C++ dispatch/upload divergences: flip the TS `parseWorkgroupSize` fallback from 8×8 to the canonical 16×16, migrate the 26 leftover `@workgroup_size(8,8,1)` catalog shaders, and fix the C++ 1×1 `emptyTexture_` r32float/16-byte packing mismatch — then lock all of it behind a shared `src/contracts/` JSON so the two parsers can never drift again (User Idea mode — Noah ran a fresh 2026-08-26 progress audit that CLOSED the entire 08-21 set (#1123–#1129) and opened a new live set #1179–#1185; that set is the newest in-context signal, per the standing precedent of treating Noah's freshest authored issues as the live Ideas source. Picked #1180 — the only labeled `bug` in the set, foundation-tagged, and parts A+B are fully headless-self-verifiable. Foundation healthy → not Fix First). Tracks open issue #1180 under the 2026-08-26 audit.**
Last week's focus (#1126 — stop the second WebGPU devices) **SHIPPED + MERGED + CLOSED** (verified on `main` this run at `a27a627`: `src/utils/adoptedGpuDevice.ts` present and `src/utils/requestPixelocityDevice.ts` **deleted**; a full `grep` of `src/**` for `requestAdapter|requestDevice` outside tests now returns **zero live call sites** beyond `webgpuBootProbe.ts` (the sole owner) and `webgpuDevicePolicy.ts` (the ladder it calls); `RendererManager.getDevice()` at `:305`, facade **353 LOC**; `depthEstimation/loader.ts:28` now carries the explicit "Never call requestAdapter() here — the renderer owns the sole adapter/device" contract comment). The `#1107 → #1126` "one `GPUDevice` from the boot probe" discipline is now enforced across the whole UI. **The 08-24 hygiene pass's CI-RED finding has also cleared:** `python3 scripts/audit_dead_sliders.py` (the exact unfiltered CI invocation) now exits **0** — 1191 definitions scanned, **0 new** dead sliders, 28 known/triaged; `audit_extrabuffer.py` likewise PASS (1380 files, 0 new, 93 known, 32 dynamic-index for review). Nothing from last week is broken → not Fix First.
Today attacks **#1180** — every claim in the issue re-verified against the tree this run: (A) `src/renderer/ShaderCompilation.ts:68-69` warns "defaulting to 8x8" and returns `{ x: 8, y: 8 }`, while C++ `wasm_renderer/wasm_internal.cpp:54-55` `ParseWorkgroupSize` defaults `x = 16; y = 16;` — and `src/renderer/ShaderCompilation.workgroup.test.ts:77-79` **asserts the 8×8 fallback**, so the wrong value is encoded as intent. Canonical 2D dispatch is 16×16×1, so an unparsed shader **under-dispatches on TS — 3/4 of the frame never runs** — while the same shader is correct under WASM. (B) A full histogram of the first `@workgroup_size` across `public/shaders/*.wgsl` confirms the drift exactly: **1359 × 16×16×1, 26 × 8×8×1, 1 × 256×1×1, 1 × 16×16×4**. The 26 are real catalog FX (`vortex`, `liquid-swirl`, `halftone`, `double-exposure`, `pixel-rain`, `split-flap-display`, `voronoi`, `neon-pulse`, several `gen-*` — full list in the swarm prompt). (C) `wasm_renderer/resources.cpp:156` sets `texDesc.format = WGPUTextureFormat_R32Float` for the depth targets, then `:166` creates `emptyTexture_` from that **same, un-reset `texDesc`** — so the 1×1 placeholder is r32float (4 B/px) — while `:179` uploads it with `emptyDataLayout.bytesPerRow = sizeof(float) * 4` (**16 bytes**). TS `src/renderer/webgpu/resources.ts` does the correct r32float pack (`bytesPerRow: 4`, one float). On a real GPU this is a **WebGPU validation error on first WASM init**; headless CI cannot catch it. Note `:185` already carries a `QueueWriteRgba` bytesPerRow comment for the *main* path — the 1×1 path was simply never converted. **Anti-drift is the real deliverable:** `src/contracts/` is already the established SoT pattern (`uniforms_layout.json`, `webgpu_limits.json`, `webgpu_optional_features.json`, `wasm_exports.json`) with `scripts/verify-device-policy-sync.js` already diffing TS ↔ `device.cpp` ↔ JSON — so the fallback constant belongs in a new contract JSON that that same script validates, exactly as #1180 asks ("mirror `webgpu_optional_features.json`"). **A + B are fully self-verifiable headless**; C is a small, well-specified C++ edit whose *acceptance* is GPU-side, so it lands with a TS-side focused resources test + a C++ comment and is flagged for Noah's workstation QA (the Cloud VM has no adapter and the emscripten build is blocked by #848).
- **kimi-cli swarm (section A):** (1) Add `src/contracts/workgroup_dispatch.json` as the canonical SoT (`defaultWorkgroupSize: {x:16, y:16, z:1}`, the documented 1D-helper exceptions, and the gpu-chores carve-out), have `ShaderCompilation.ts` import its fallback from that contract rather than a literal, and extend `scripts/verify-device-policy-sync.js` to assert TS ↔ `wasm_internal.cpp` ↔ JSON agree — mirroring how it already handles `webgpu_limits.json`/`webgpu_optional_features.json`. (2) Flip the TS fallback to 16×16 and **update `ShaderCompilation.workgroup.test.ts:77-79`** to assert 16×16, adding a regression test that fails if TS and the contract diverge. (3) Migrate the 26 leftover 8×8 catalog shaders to `@workgroup_size(16, 16, 1)`, adding bounds guards where a file still assumes an 8-wide tile — **workgroup size and guards only, do not rewrite shader math**; gate every changed file. (4) Fix C++ `emptyTexture_`: set `texDesc.format` **and** `usage` explicitly instead of inheriting the depth descriptor, and make the upload match (one r32 float, `bytesPerRow = 4`) — pick r32float to match TS and say so in a comment. (5) Keep `@workgroup_size(64,1,1)`/`(256,1,1)` 1D helpers and `src/gpuChores/shaders.ts`'s 8×8 downsample kernels **untouched** — they are not catalog FX. Save-state to `.swarm-state.md` each iteration.
- **Allowed to touch:** `src/renderer/ShaderCompilation.ts` + `src/renderer/ShaderCompilation.workgroup.test.ts` (the 8×8 assertion MUST be updated — it is the encoded-wrong-intent), NEW `src/contracts/workgroup_dispatch.json`, `scripts/verify-device-policy-sync.js` (extend only — do NOT weaken the existing limits/features/exports checks), the **26 listed** `public/shaders/*.wgsl` files (workgroup size + bounds guards ONLY), `wasm_renderer/resources.cpp` (the `emptyTexture_` block at `:164-183` ONLY), `wasm_renderer/wasm_internal.cpp` (only if the C++ default must read the contract — otherwise leave it, it is already correct at 16), `src/renderer/webgpu/resources.ts` (+ a focused colocated test for the r32float placeholder pack — the TS side is already correct, so this is test-only), `docs/BINDING_CONTRACT.md` (the canonical-dispatch note). READ-ONLY reference: `src/contracts/webgpu_optional_features.json` + `src/contracts/webgpu_limits.json` (the contract shape to mirror — do NOT edit), `wasm_renderer/device.cpp` (parity reference — do NOT edit).
- **Must NOT touch:** the adapter ladder / `webgpuDevicePolicy.ts` / `webgpuBootProbe.ts` / `WebGpuProbeFailureOverlay` (#1107-frozen), `src/utils/adoptedGpuDevice.ts` + `RendererManager.getDevice()` (#1126 just landed — do NOT re-plumb the device SoT), `src/gpuChores/**` (its 8×8 kernels are correct by design — explicitly carved out by #1180), `src/renderer/UniformBuffer.ts` (immutable), `wasm_renderer/device.cpp` + the `compatibleSurface=nullptr` / TimedWaitAny / canvas `alphaMode:'opaque'` + preferred-format double-configure paths (#1180 says these are healthy — do NOT "simplify"), `storage_manager/**`, `scripts/audit_*.py` + `.github/workflows/**` + `README.md` + the catalog/manifest scripts (**today's decoupled Copilot track, section B — #1184**; Noah explicitly scoped the 8×8 migration OUT of #1184 and INTO #1180, so respect that split), and `shader_plans/**` + any `public/shaders/*.wgsl` **outside the 26** (content stream is actively churning — open draft PRs #1174/#1190/#1191/#1192). Do NOT add a WGSL parser dependency (that is a later naga-WASM ticket, #1183), do NOT mass-rewrite shader math, and do NOT change the WASM default-backend policy or feedback copy order.
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`; `CI=true npx craco test --watchAll=false` green (**re-establish the baseline on iteration 0** — `node_modules` is absent on this VM so the count is carried, not measured: `.swarm-state.md` 2026-08-23 saw 84 suites / 551 total, the 2026-08-24 hygiene pass saw 84 suites / 560 total; use `npm ci --ignore-scripts`, since plain `npm ci` is still blocked by the `sharp` postinstall behind the proxy); `SKIP_WASM_BUILD=1 npm run build` compiles ("Compiled successfully"); `npm run verify:device-policy` green (**this is the gate that proves the anti-drift contract works** — it must fail if you hand-edit the TS fallback away from the JSON); `npm run verify:uniforms` + `npm run verify:dependency-boundaries` green; `python3 scripts/wgsl_precommit_gate.py --files <each changed .wgsl>` green on all 26; `npm run audit:extrabuffer` + `npm run audit:dead-sliders` still exit 0 (baseline today: 0 new / 93 known, and 0 new / 28 known — do NOT let the shader edits regress these). Acceptance targets (from #1180): TS and C++ unparsed-workgroup fallback both 16×16 with tests updated; catalog 8×8 leftovers **0** (or a documented allowlist of true 1D kernels); WASM empty-placeholder `bytesPerRow` matches the allocated format; no change to adapter ladder, limits JSON, feedback copy order, or WASM default. **GPU-gated residual for Noah:** part C's real proof is a WASM init on a discrete-GPU host — flag it in `.swarm-state.md` rather than claiming it verified. eslint noise non-gating.

<details><summary>Archived — 2026-08-22 focus (#1126 stop second WebGPU devices — SHIPPED + MERGED + CLOSED, on main)</summary>

**2026-08-22 — Stop the second WebGPU devices: make `ShaderValidator`, `ShaderScanner`, and the depth-estimation loader adopt the single renderer `GPUDevice` (via `window.webgpuProbe.handoff` / a `RendererManager` device accessor) instead of each calling `requestAdapter()`/`requestDevice()` on their own (User Idea mode — Noah ran a fresh 2026-08-21 progress audit that closed the old epic #1076 children and opened a new live set #1123–#1129; that set is the newest in-context signal, per the standing precedent of treating Noah's freshest authored issues as the live Ideas source. Picked #1126 — the only labeled `bug` in the set, foundation-tagged, fully headless-self-verifiable, and a direct continuation of #1107's "one GPUDevice from the boot probe" discipline. Foundation healthy → not Fix First). Tracks open issue #1126 under the 2026-08-21 audit.**
Last week's focus (#1107 — WebGPU hard-fail boot probe) **SHIPPED + MERGED + CLOSED** (issue closed completed by Noah 2026-08-15; verified on `main` this run: `src/renderer/webgpuBootProbe.ts` (12.3KB) + `webgpuBootProbe.test.ts`, `src/components/WebGpuProbeFailureOverlay.tsx`/`.css`/`.test.tsx`, `docs/WEBGPU_BOOT_PROBE.md` all present on `origin/main` — landed via commit `b03ac5a`; `.swarm-state.md` iteration-2 closeout = 81 suites / 535 passed / 1 skipped, `SKIP_WASM_BUILD=1 npm run build` compiled, `verify:dependency-boundaries` green, `RendererManager.ts` 348 LOC). #1107 established the gpu-chores policy (**one** `GPUDevice` from the boot probe, kill-switch `?no_gpu_compute`) and `src/gpuChores/` (`GpuChoresHost.ts` + `histogram`/`reduce`/`lut`/`downsample` kernels — the old #1106 scope) is on `main` and already `attach(device)`-only (never calls `requestAdapter`). #1126 is the follow-on: the *other* UI paths still cheat.
Today attacks **#1126** — grounded in the tree this run: `src/components/ShaderValidator.tsx:95-98` does `requestAdapter()` + `requestDevice()` per shader; `src/components/ShaderScanner.tsx:100/161/167` pings `requestAdapter()` "to check support" then requests **another** adapter + `requestPixelocityDevice()`; `src/services/depthEstimation/loader.ts:28-29` calls `requestAdapter()` just to see if WebGPU exists before handing Transformers `device:'webgpu'` (which then allocates its *own* device). On a page that already owns the Pixelocity device these second devices (a) break the `?renderer=wasm` switch with the exact Windows Chrome "No available adapters" error `wasm_renderer/device.cpp` documents, (b) fight gpu-chores for the adapter, and (c) compile shaders against a **different** feature/limit set than the live renderer (`requestPixelocityDevice` vs the live `collectOptionalDeviceFeatures`). The fix is **adopt-or-idle**: consumers take an optional `GPUDevice` from the renderer/`window.webgpuProbe.handoff`; if the probe didn't succeed they show the existing failure-overlay reason and do **not** request a second device; the "is WebGPU supported?" `requestAdapter()` pings are deleted (`window.webgpuProbe` already answered). **Fully self-verifiable headless** — the acceptance criteria are literally Jest spies asserting Validator/Scanner/depth never call `requestAdapter` when a handoff exists; no GPU adapter required.
- **kimi-cli swarm (section A):** (1) Expose the live device as a single SoT — add a `getDevice(): GPUDevice | null` accessor on `RendererManager` (thin facade, do NOT re-expand past ~350 LOC; it's at 348) and/or lean on `window.webgpuProbe.handoff` which the boot probe already publishes. (2) `ShaderValidator.tsx` + `ShaderScanner.tsx`: adopt that device; delete the standalone `requestAdapter()` support-pings; if probe `ok !== true`, render the existing `WebGpuProbeFailureOverlay` reason and idle — no second device. (3) `depthEstimation/loader.ts`: replace the `requestAdapter()` availability probe with `window.webgpuProbe.ok` (+ `navigator.gpu` presence); document that `@xenova/transformers` may still allocate its own device, and **prefer wasm/cpu depth while the canvas is live** (the lazy chunk is already excluded from the 320 KiB main budget). (4) Align `requestPixelocityDevice`'s feature list with the live `collectOptionalDeviceFeatures`, **or delete it** once scanners adopt the live device. (5) Add the Jest spy tests. Save-state to `.swarm-state.md` each iteration.
- **Allowed to touch:** `src/components/ShaderValidator.tsx` (+ NEW `*.test.tsx`), `src/components/ShaderScanner.tsx` (+ NEW/existing `*.test.tsx`), `src/services/depthEstimation/loader.ts` (+ colocated test), `src/utils/requestPixelocityDevice.ts` (align-or-delete + its test), `src/renderer/RendererManager.ts` (add the `getDevice()` accessor ONLY — facade stays ≤~350 LOC), `src/hooks/useDepthEstimation.ts` (only if the loader signature changes force it), `docs/GPU_CHORES.md` + `docs/BINDING_CONTRACT.md` (the "one device / Transformers adopt-or-cpu" note). READ-ONLY reference: `src/renderer/webgpuBootProbe.ts` (the handoff/`window.webgpuProbe` shape — mirror it, do NOT edit), `src/gpuChores/GpuChoresHost.ts` (the correct `attach(device)` pattern to imitate — do NOT edit), `src/renderer/webgpuDevicePolicy.ts` (`collectOptionalDeviceFeatures`/`buildRequiredLimits` — read to align features).
- **Must NOT touch:** the boot probe itself (`webgpuBootProbe.ts`), the adapter ladder (`webgpuDevicePolicy.ts` beyond reading the feature helpers), the overlay component, or the feedback copy order — all #1107-frozen; `src/renderer/webgpu/**` engine internals, `src/renderer/UniformBuffer.ts` (immutable); `src/gpuChores/**` (already correct — do NOT re-plumb it); `wasm_renderer/**` + `src/wasm/wasm_bridge.js` (#1123/#1125 own that — keep decoupled; `device.cpp` is read-only parity reference); `public/shaders/**` + `shader_definitions/**` + `shader_plans/**` (content stream churning — see Backlog); `scripts/` + `.github/workflows/**` (today's decoupled Copilot track, section B); `storage_manager/**`. Do NOT add a WebGL2 FBO fallback, a second WebGPU helper library, or make WASM the default. Do NOT rewrite `@xenova/transformers` — only choose its backend.
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`; `CI=true npx craco test --watchAll=false` green (record the baseline on iteration 0 — `.swarm-state.md` last saw 81 suites / 536 total; add the spy tests on top); `SKIP_WASM_BUILD=1 npm run build` compiles ("Compiled successfully"); `npm run verify:dependency-boundaries` green. Acceptance targets (from #1126): with a live renderer, opening Validator/Scanner calls **zero** `requestAdapter`/`requestDevice`; the depth availability check calls **zero** `requestAdapter`; `loader.ts` + `GPU_CHORES.md` carry the Transformers adopt-or-cpu note; gpu-chores still `attach(rendererDevice)` only; boot probe / adapter ladder / feedback copy order unchanged; `RendererManager` facade stays ≤~350 LOC. eslint noise non-gating.

</details>

<details><summary>Archived — 2026-08-15 focus (#1107 WebGPU hard-fail boot probe — SHIPPED + MERGED + CLOSED, on main via b03ac5a)</summary>

**2026-08-15 — Fatalize the WebGPU boot: hard-fail adapter/device probe with `window.webgpuProbe` breadcrumbs + a blocking canvas-slot overlay — no WebGL fallback (User Idea mode — Noah's freshest in-context signal is the two 2026-08-14 foundation issues #1107 + #1106, which supersede the 08-07 Ideas items as the live source; picked #1107, the tighter, fully headless-self-verifiable one that builds directly on last week's newly-modularized `backendLifecycle`. Foundation healthy → not Fix First). Tracks open issue #1107 under epic #1076.**
Last week's focus (#1079 — residual god-module strangler) **SHIPPED + MERGED and is on `main`** (verified in tree: commit `ffb8eef` "refactor: strangler RendererManager + StoragePanel into tested seam modules (#1079)" via PR #1092; `src/renderer/RendererManager.ts` **344 LOC** facade, `src/components/storage/StoragePanel.tsx` **200 LOC** panel-composed; seams `backendLifecycle`/`slotOrchestration`/`inputSourceBridge`/`performanceStatus` present with colocated Jest; `.swarm-state.md` iteration-1 log = 76 suites / 506 passed / 1 skipped, build compiled, `verify:dependency-boundaries` green). Since then Noah opened **two new foundation/architecture issues 2026-08-14** — **#1107** (WebGPU required: hard-fail boot probe, no WebGL fallback) + **#1106** (gpu-chores: shared analysis kernels — histogram/reduce/LUT/downsample). These are the freshest in-context user signal, newer than the 08-07 audit set.
Today attacks **#1107** — the tighter of the pair and a **direct continuation of #1079**: last week carved the backend seam out of RendererManager into `backendLifecycle.ts`; the adapter ladder (`ADAPTER_ATTEMPT_LADDER` / `requestAdapterWithFallback`) already lives cleanly in `src/renderer/webgpuDevicePolicy.ts` and is consumed at `src/renderer/webgpu/device.ts:145`. The task is to make **ladder exhaustion fatal** instead of silently absorbed (no empty canvas, no last-frame freeze, **no WebGL branch**), emit a structured `window.webgpuProbe` breadcrumb (per-attempt: powerPreference, adapter info, device limits, error string, Chrome-vs-Edge brand via `navigator.userAgentData`), and render a **blocking overlay in the canvas slot only** (gallery/storage stay visible). It's **fully self-verifiable headless** — mock `navigator.gpu` to assert fatalization, probe-JSON shape, and overlay mount; unit-test only, no GPU adapter required (unlike the GPU-gated #1078/#1080/#1081). #1106 (gpu-chores) is bigger, cross-app, and its correctness is GPU-gated → left for a future/hands-on run.
- **kimi-cli swarm (section A):** (1) In `src/renderer/webgpuDevicePolicy.ts` extend `requestAdapterWithFallback` (or add a `probeWebGpuBoot()` seam beside it) to record every ladder attempt into a structured result and treat **total exhaustion as a thrown fatal**, not a null-adapter soft path; include the swapchain/first-pipeline create in the probe if that's where Edge dies. (2) Publish the probe to `window.webgpuProbe` (typed, test-visible) with the Chrome/Edge brand delta legible from the JSON alone. (3) Thread the fatal up through `backendLifecycle.ts` create/switch so `RendererManager` surfaces a hard-fail state instead of absorbing it via backend switching. (4) Add a **canvas-slot blocking overlay** component (new `src/components/renderer/WebGpuFailureOverlay.tsx` or similar) that shows the failure + attempt log; gallery/storage unaffected. Save-state to `.swarm-state.md` each iteration.
- **Allowed to touch:** `src/renderer/webgpuDevicePolicy.ts` (+ its `webgpuDevicePolicy.test.ts`), `src/renderer/backendLifecycle.ts` (+ colocated test), `src/renderer/RendererManager.ts` (facade hard-fail surface only — do NOT re-expand it past ~350 LOC; extract if needed), NEW `src/renderer/webgpuProbe.ts` (+ test) if a dedicated probe seam is cleaner, NEW `src/components/renderer/WebGpuFailureOverlay.tsx` (+ `*.test.tsx`), `docs/BINDING_CONTRACT.md`/a new `docs/WEBGPU_BOOT_PROBE.md` for the "WebGPU required this phase" note. READ-ONLY reference: `src/renderer/webgpu/device.ts` (the adapter-request consumer — mirror its call shape, minimal edits only where the fatal must propagate), `wasm_renderer/device.cpp` (the C++ ladder mirror — do NOT edit; keep parity in mind).
- **Must NOT touch:** **no WebGL / WebGL2 renderer branch anywhere** (explicit #1107 non-goal — hard-fail, don't fall back), `src/renderer/webgpu/**` engine internals beyond the one `device.ts` call site (frame/present/pipeline/resources are Wave-2 modular — leave them), `src/renderer/UniformBuffer.ts` (immutable), `wasm_renderer/**` + `src/wasm/wasm_bridge.js` (#1063/#1080 own that — keep decoupled; WASM `emdawnwebgpu` is still WebGPU, so if *its* adapter fails, hard-fail too — but do not edit C++ here), `public/shaders/**` + `shader_definitions/**` (content track — actively churning), `scripts/` + `.github/workflows/**` (today's decoupled Copilot track, section B), `storage_manager/**`, and the whole `src/components/storage/**` split that just landed (#1079). Do NOT add auto-fallback of any kind; do NOT hide failure behind a still image or last frame; do NOT invent a GL backend for GPU-less CI (headless CI must **skip** GPU e2e).
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`; `CI=true npx craco test --watchAll=false` green (record the baseline on iteration 0 — `.swarm-state.md` last saw 76 suites / 507 total; add probe + overlay tests on top); `SKIP_WASM_BUILD=1 npm run build` compiles ("Compiled successfully"); `npm run verify:dependency-boundaries` green (probe/overlay must not import upward into `src/App`). Acceptance targets (from #1107): failed probe → hard-fail overlay + full attempt log; **no WebGL context created for the main view**; Chrome/Edge mismatch legible from `window.webgpuProbe` JSON alone; RendererManager facade stays ≤~350 LOC; zero behavior change on the **success** path (probe passes → renderer boots exactly as today). eslint noise non-gating.
</details>

<details><summary>Archived — 2026-08-08 focus (#1079 residual god-module strangler — SHIPPED + MERGED via #1092, on main)</summary>

**2026-08-08 — Strangler-refactor the two residual god modules that tax every new shader: `RendererManager.ts` (986 LOC) + `StoragePanel.tsx` (737 LOC) (User Idea mode — Noah's fresh 2026-08-07 progress audit is the live Ideas source; picked #1079, the highest-leverage non-GPU-blocked item in epic #1076. Foundation healthy → not Fix First). Tracks open issue #1079 under epic #1076.**
Last week's focus (#1038 — Uniforms Single-Source-of-Truth, kill the `config.y = delta_time` folklore) **SHIPPED + MERGED and is on `main`** (verified in tree: `src/contracts/uniforms_layout.json` present; `docs/BINDING_CONTRACT.md:61/73/91` + `agents/WGSL_BUILTINS_GENERATIVE.md:31` now state `config.y = rippleCount`; commits `639cd31` #1055 + `67a8eb7` #1056; `npm run verify:uniforms` + `audit:config-y` scripts landed). Since then Noah ran a **fresh 2026-08-07 progress audit** creating a new strategic backlog — epic **#1076** + children **#1077–#1084** — which supersedes the #1038–#1044 set. The 08-07 audit's strategic call: *"interleave foundation residual with content; prefer multipass excellence + discoverability over pure volume; stay CRA/CRACO; no GraphRunner C++ port until the WASM decision."*
Today attacks the highest-leverage **non-GPU-blocked** item in that audit: **#1079**. The two residual god modules survived Foundation Wave 2 — `src/renderer/RendererManager.ts` (**986 LOC**: backend switch, slots, quality, input, recording glue) and `src/components/storage/StoragePanel.tsx` (**737 LOC**: UI + API + ratings). Every new shader/backend feature pays interest on these. It's a clean strangler pattern kimi has executed twice before (WebGPU split #967, App/Controls Wave 3 #938) and is **fully self-verifiable headless** (Jest + `npm run build` — no GPU adapter required, unlike thumbnails #1078 / multipass QA #1081 / WASM promotion #1080).
- **kimi-cli swarm (section A):** Mirror the WebGPU-split philosophy. **RendererManager strangler** — extract pure seams with Jest tests: `backendLifecycle.ts` (create/destroy TS vs WASM, `?renderer=` query), `slotOrchestration.ts` (enable/mode/params forwarding), `inputSourceBridge.ts` (image/video/webcam/depth handoff), `performanceStatus.ts` (format tier / FP32 pin / pass budget surface); keep `RendererManager` as a thin facade (≤~350 LOC) implementing `IRenderer` + the app API. **StoragePanel** — split list/browser, detail, ratings, upload into `src/components/storage/panels/`; keep `StorageClient` as the sole network SoT. **Preserve contracts (do not regress):** feedback copy order **dataB→dataC then dataA→dataC**; WASM stays opt-in Tier B with **no auto-fallback**; duck-typed WASM methods must not regress (#887). Save-state to `.swarm-state.md` each iteration.
- **Allowed to touch:** `src/renderer/RendererManager.ts` + NEW `src/renderer/*.ts` seam modules (`backendLifecycle`/`slotOrchestration`/`inputSourceBridge`/`performanceStatus`) + colocated `*.test.ts`; `src/components/storage/StoragePanel.tsx` + NEW `src/components/storage/panels/*` + colocated `*.test.tsx`; `docs/STORAGE_API.md` (only if the `StorageClient` SoT boundary is clarified — no API changes). READ-ONLY reference: `src/renderer/webgpu/*` (the already-modular device/resources/pipeline/frame/present/slotDispatch — mirror its shape, do NOT edit it), `src/renderer/UniformBuffer.ts`.
- **Must NOT touch:** `src/renderer/webgpu/**` engine internals (read-only reference — do NOT re-modularize what Wave 2 already split), `src/renderer/UniformBuffer.ts` (immutable uniform packing), `wasm_renderer/**` + `src/wasm/wasm_bridge.js` (#1063 owns that concat — keep decoupled), `public/shaders/**` + `shader_definitions/**` (content track — Jules/Copilot own generative), `scripts/` + `.github/workflows/**` + prestart/shader-list generation (**today's decoupled Copilot track, section B — #1077 base-url drift**), `storage_manager/**` backend. Do NOT change the feedback copy order, add WASM auto-fallback, or rename any public `IRenderer`/app-API method.
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`; `CI=true npx craco test --watchAll=false` green (record the new suite/test count — establish the baseline on iteration 0 before touching anything); `npm run build` compiles ("Compiled successfully"); `npm run verify:dependency-boundaries` green (the new modules must not import upward into `src/components`/`src/App`). Acceptance targets: `RendererManager.ts` facade ≤~350 LOC, `StoragePanel.tsx` ≤~400 LOC or clearly panel-composed, new seam modules unit-tested for backend-switch + slot-param forwarding, **zero behavior change** in the Controls → renderer-switcher / recording smoke paths. eslint noise is non-gating — focus on tests + build.
</details>

<details><summary>Archived — 2026-08-01 focus (#1038 Uniforms Single-Source-of-Truth — SHIPPED + MERGED, on main via #1055/#1056)</summary>

**2026-08-01 — Kill the `config.y = delta_time` folklore: establish a single source of truth for the Uniforms layout and stop swarms re-learning wrong uniform semantics (User Idea mode — Noah authored #1038 in-context 2026-07-29; foundation healthy → not Fix First). Tracks open issue #1038.** → **DONE 2026-08-08:** `src/contracts/uniforms_layout.json` + `verify:uniforms`/`audit:config-y` scripts landed; `docs/BINDING_CONTRACT.md` + `agents/WGSL_BUILTINS_GENERATIVE.md` corrected to `config.y = rippleCount`; commits `639cd31` (#1055) + `67a8eb7` (#1056) on `main`.
- **kimi-cli swarm (section A):** (1) write an authoritative Uniforms table into `docs/BINDING_CONTRACT.md`; (2) fix `agents/WGSL_BUILTINS_GENERATIVE.md` + brief templates; (3) add machine-checkable SoT — NEW `src/contracts/uniforms_layout.json` + a drift check vs `UNIFORM_OFFSETS`; (4) grep-audit `public/shaders/**` for `config.y`-as-dt bugs (follow-up PRs only). Save-state each iteration.

<details><summary>Archived — 2026-07-25 focus (generative updatedParams campaign #1011 — SHIPPED, MERGED, CLOSED)</summary>

**2026-07-25 — Finish the generative `updatedParams` campaign + land the extraBuffer/dead-slider audit tooling that stops swarms re-learning the FFT footgun (New Idea mode — user `Ideas` list is exhausted; foundation healthy → not Fix First). Tracks open issue #1011.**
Last week's focus (mount `CommunityGallery`) **shipped, merged, and is LIVE** — the component is mounted in `src/components/controls/panels/AiVjStudioPanel.tsx`, fed by `getCurrentChain` from `src/hooks/useShareChain.ts`; the stale "unmounted" backlog note is corrected below (MEMORY.md flagged it 07-22). Since 07-18 the repo also absorbed **Foundation Wave 2** (#965–970: `App.tsx`→562 LOC, WebGPU modularization, binding contract, GraphRunner multipass, thumbnail pipeline 26.6%) and **six generative upgrade swarm batches** (9–14, ~150 shaders). Per Noah's own 2026-07-22 progress audit, the priority *parallel content track* is closing out the generative `updatedParams` pool while giving future swarms a guardrail against the reserved-zone `extraBuffer` bug that bit 9 shaders across Batches 10–12.
- **kimi-cli swarm (section A):** (1) build TWO new standalone audit scripts — `scripts/audit_extrabuffer.py` (flags WGSL writing persistent state into reserved `extraBuffer[0..132]`, the engine/FFT-bin zone; safe zone is `[133..255]`) and `scripts/audit_dead_sliders.py` (JSON param ids with no matching WGSL read) — then (2) run 8-shader generative batches wiring 4 `updatedParams` from **existing** param ids (no renames — preset compatibility) until the empty-`updatedParams` pool is `<10` (issue #1011 estimated ~79 on 07-22 — recount first). Verify every batch; save-state each batch boundary to `.swarm-state.md`.
- **Allowed to touch:** `public/shaders/*.wgsl` (only the batch's generative targets), `shader_definitions/**` + generative JSON defs, NEW `scripts/audit_extrabuffer.py` + `scripts/audit_dead_sliders.py` (+ colocated tests), `agents/WGSL_BUILTINS_GENERATIVE.md` + brief templates (`temp/make_briefs_*.py`), `.swarm-state.md`.
- **Must NOT touch:** `src/**` (immutable engine + the whole TS app — Copilot owns `src/renderer/webgpu/` this week, section B), `storage_manager/**`, `wasm_renderer/**`, existing `scripts/wgsl_precommit_gate.py` + `.github/workflows/**` (that's the #983/#984 gate-promotion track — keep decoupled). Do NOT rename any JSON param id (breaks saved presets). Do NOT write persistent shader state into `extraBuffer[0..132]`.
- **Verify each iteration:** per-file gate `python3 scripts/wgsl_precommit_gate.py --files <changed .wgsl>` green; full naga scan clean **except** the pre-existing unrelated `gen-luminescent-aether-plasma-astro-axolotl.wgsl` "invalid left-hand side of assignment" failure (in Backlog, NOT in scope here); `node scripts/generate_shader_lists.js` + duplicate-id check clean; `CI=true npx craco test --watchAll=false` green (baseline ~339 Jest per MEMORY 07-22 — record new count). New audit scripts must run clean (or emit a triaged report) before batches start.

</details>

<details><summary>Archived — 2026-07-18 focus (community gallery UI wiring — SHIPPED + MERGED, now LIVE on main)</summary>

**2026-07-18 — Land the community gallery for real: wire `CommunityGallery` into the live UI now that #936/#938 have merged (User Idea mode).**
Last week's whole-stack vertical slice **shipped and merged to `main`** (backend `POST/GET /api/preset-packs`, `src/services/communityGallery.ts`, `src/components/CommunityGallery.tsx`, 94 backend + 241 frontend tests green) — but the component is **unmounted**: it's referenced only by its own test, so no user can actually publish or browse. The two PRs that forced the "must NOT touch App.tsx/Controls.tsx" deferral — **#936 (MIDI, merged via PR #957) and #938 (foundation strangler refactor, merged)** — have both landed. App.tsx/Controls.tsx are now carved into `src/hooks/*` + `src/components/controls/panels/*`, so the merge-contention reason to defer is gone. **Today closes the loop: mount the gallery, prove the round trip end-to-end in the running app, then take the deferred stretch goals.**
- **Vertical slice completion (kimi-cli, section A):** mount `CommunityGallery` in `src/components/controls/ControlsContainer.tsx` (mirror how `PresetPackGallery` is passed `onApplySharedChain` at line ~324), thread the current-chain getter + `onApplySharedChain`/`onApplyPack` down from `App.tsx`, add a "Publish current chain → browse community" happy path, then the deferred stretch (play-count sorting on the list, thumbnail/param-summary cards, error/empty states). Half-to-full-day for the wiring; stretch is the long-horizon swarm tail.
- **Allowed to touch:** `src/components/controls/ControlsContainer.tsx`, `src/components/controls/panels/*` (only if a new gallery panel is the clean fit), `src/App.tsx` (now post-refactor and stable — safe to wire props), `src/components/CommunityGallery.tsx`, `src/services/communityGallery.ts`, colocated `*.test.ts(x)`. Backend play-count-sort tweaks in `storage_manager/app.py` + `storage_manager/tests/test_preset_packs.py` are **allowed as stretch only** (kimi-cli owns backend this week so it doesn't collide with the Copilot scripts/ track).
- **Must NOT touch:** `src/renderer/` (immutable engine), `wasm_renderer/` (C++/WASM epic #885–890), `scripts/` + `.github/workflows/` + `reports/` (today's decoupled Copilot WGSL-gate track, section B), `public/shaders/*.wgsl`, `shader_definitions/`. Publish/decode MUST keep routing through `layerChainShare` encode/decode and the existing `STORAGE_API_URL` from `src/config/appConfig` — do not fork the chain format or hardcode a host.
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`, then `CI=true npx react-scripts test --watchAll=false` green (baseline **~254 test calls across 29 files** on current `main`; record the new count and keep it green). If backend touched: `cd storage_manager && python -m pytest tests/ -q` green (baseline 94). **End-to-end proof (the point of "landed"):** run the app, open the gallery panel, publish the active chain, confirm it appears in the live list, apply it back, and confirm the rendered result matches — screenshot or console-log the round trip. Ignore the pre-existing `wasm_renderer/build.sh --use-port=emdawnwebgpu` failure (issue #848).

</details>

---
<details><summary>Archived — 2026-07-11 focus (community gallery vertical slice, now merged)</summary>

**2026-07-11 — Community preset-pack gallery backed by the Storage Manager backend: whole-stack (User Idea mode).**
Last remaining user-written Idea, and the genuine whole-stack unlock. Share links + preset packs + AI-VJ→chain bridge + variation breed/favorites are all shipped; the missing public-audience piece is letting users **publish** a shared chain to the FastAPI backend and **browse a live community gallery** — upgrading today's static `public/preset_packs.json` into a real backend-backed feature. Reuse `encodeChain`/`decodeChain` (`src/services/layerChainShare.ts`) + `mapVJStackToSharedChain` (`vjToSharedChain.ts`); mirror the existing `PresetPackGallery.tsx` component shape and the backend's `/api/shaders/upload` + ratings/play-count patterns in `storage_manager/app.py`. kimi-cli swarm drives the vertical slice; UI wiring into `Controls.tsx` is deferred because that file is under active strangler refactor (open PRs #936, #938).
- **Ship the first vertical slice:** publish → list → fetch → render + apply. Moderation, pagination polish, and play-count sorting are stretch. Multi-day scope — kimi-cli's save-state loop is the right vehicle.
- **Allowed to touch — BACKEND:** `storage_manager/app.py` (NEW endpoints mirroring existing patterns: `POST /api/preset-packs` publish — validate by decoding the chain, reject undecodable; `GET /api/preset-packs` list with limit/offset; `GET /api/preset-packs/{id}` fetch; optional `POST /api/preset-packs/{id}/play`. Reuse the existing GCS/store + Pydantic-model + CORS conventions already in the file — do NOT invent a new persistence layer), NEW `storage_manager/tests/test_preset_packs.py` (pytest, mirror `test_streaming_endpoints.py`/`test_sync_endpoints.py`).
- **Allowed to touch — FRONTEND (NEW files only):** NEW `src/services/communityGallery.ts` (API client: `publishPack`, `listCommunityPacks`, `getCommunityPack`; reuse `encodeChain`/`decodeChain` from `layerChainShare.ts`; **reuse the existing storage-manager fetch base URL** that `ShaderBrowserWithRatings`/the ratings client already use — find and import it, do NOT hardcode a host), NEW `src/components/CommunityGallery.tsx` (mirror `PresetPackGallery.tsx` props `open/onToggle/onApplyPack`/`onApplySharedChain`; live-fetch list; "Publish current chain" action driven by a passed-in current-chain getter), NEW colocated `communityGallery.test.ts` (mock `fetch`) + `CommunityGallery.test.tsx`.
- **Must NOT touch:** `src/App.tsx` and `src/components/Controls.tsx` (**both under active strangler refactor in open PRs #936 MIDI + #938 foundation-epic #912 — editing them here guarantees merge conflict**; leave a `// TODO(wire): mount CommunityGallery once #936/#938 land` marker instead of wiring), `src/renderer/` (immutable engine), `wasm_renderer/` (C++/WASM epic #885–890), `scripts/` + `.github/workflows/` + `reports/` (today's decoupled Copilot track), `public/shaders/*.wgsl`, `shader_definitions/`. Publish/decode MUST route through the existing `layerChainShare` encode/decode — do not fork the chain format.
- **Verify each iteration:** FRONTEND — `npx tsc --noEmit` clean, then `CI=true npx react-scripts test --watchAll=false` green (baseline is now **217 tests** once #936 lands, ~180 on current main; keep whatever you branch from green and record the new count). BACKEND — `cd storage_manager && python -m pytest tests/ -q` green including the new suite. Round-trip contract: a chain published via `POST /api/preset-packs` must come back from `GET /api/preset-packs/{id}` and `decodeChain` to an equal `SharedChain`. Ignore the pre-existing `wasm_renderer/build.sh --use-port=emdawnwebgpu` failure (issue #848).
</details>
</details>

## Ideas
<!--
Write ideas here during the week as they come to you.
Routine prioritizes these over generated ideas.
Format: - [ ] Short description (optional: more context on next line indented)
Routine will mark picked items as "[in progress — YYYY-MM-DD]".
-->
- [x] AI VJ Mode — prompt-to-shader-stack generation via in-browser Gemma-2-2b
  Feed user vibe prompt → LLM picks N shaders + params from the 700+ library → renders a live VJ stack. Half-day prototype, multi-day polish.
  → Completed 2026-04-25 (buildShaderManifest all 15 categories, selectShadersFromLLM with params, vibe-prompt UI wired in Controls.tsx)
- [x] Shader metadata normalization + full-text search over 700+ library
  Reconcile `params_missing.md`, `SHADER_PARAMETER_AUDIT.md`, and `shader_params_extracted.json` into a single canonical metadata schema so the scanner/rating/AI-VJ paths share one source of truth. Full-day.
  → Completed 2026-05-02 (shaderCatalog.ts built, wired into ShaderBrowserWithRatings + AutoDJ.buildShaderManifest)
- [x] AI VJ stack persistence + history panel
  Add localStorage save/load of generated stacks, a "VJ history" panel showing last N stacks with their vibe prompts, and a one-click "regenerate variation" button. Surface live shader IDs in the UI. Half-day.
  → Completed 2026-05-23 (vjHistory.ts service, saveVJStack in AutoDJ.ts, collapsible VJ History panel with Restore + Regen buttons in Controls.tsx)
- [x] Per-shader param presets + AI VJ randomizer
  Build a preset system for param combos (keyed off shaderCatalog), add a "randomize params" operator that samples within min/max/step ranges for each shader in the active AI VJ stack. Enables live-performance use. Full-day.
  → Completed 2026-05-23 (vjPresets.ts: VJPreset + save/load/delete + randomizeParams with step-snap & [min,max] clamp; randomizeActiveParams() on Alucinate in AutoDJ.ts; Randomize Params / Randomize Slot / Randomize All Slots buttons + collapsible Presets panel save/restore/delete in Controls.tsx; tsc PASS). Verified in repo audit 2026-05-30.
- [x] AI VJ "auto-transition" / beat-aware sequencer
  Extend the AI VJ stack to auto-transition between generated stacks on a timer or audio-beat trigger, cross-fading params via the existing randomizeParams/preset infra. Turns one-shot generation into a continuous live set. Full-day. (Generated 2026-05-30 — was the Copilot track.)
  → Completed 2026-05-30 (commits 812b5ab feat + 60853dd fix). Full stack shipped: `beatDetector.ts`, `transitionOrchestrator.ts`, `audioGraph.ts`, `transitionMath.ts` + tests; `AutoTransitionConfig`, `startAutoTransition`/`stopAutoTransition`/`triggerNextTransition`/`buildTransitionTarget` on Alucinate (AutoDJ.ts); Auto-Transition panel (timer/beat source, interval, duration, randomize/cyclePresets mode) in Controls.tsx. Verified in repo audit 2026-06-06.
- [x] Multi-slot layer-chain preset packs + shareable URLs
  Serialize the full multi-slot chain (up to 6 slots: shaders + params + blend) into a compact shareable URL/JSON, with a curated "preset pack" gallery. Makes presets shareable beyond localStorage, unlocking the public audience. Half-to-full-day. (Generated 2026-05-30 — runner-up new idea.)
  → Completed 2026-06-13 (kimi-cli swarm). `src/services/layerChainShare.ts` (versioned SHARED_CHAIN_VERSION=1; `buildSharedChain`/`expandSharedChain`/`encodeChain`/`decodeChain`, default-param diffing + re-expansion, MAX_SHARED_SLOTS=6); `PresetPackGallery.tsx` + `public/preset_packs.json` (curated named chains); App.tsx hydrates from `?chain=` on load, `applySharedChain` + copy-share-link + share modal (Twitter/TikTok); Controls.tsx "Copy Chain Share Link" button + gallery. 21/21 tests PASS via `react-scripts test`. Committed f5edd00.

<!-- New ideas generated 2026-06-13 (New Idea mode; #1 WGSL authoring gate was that day's kimi-cli focus). -->
- [x] AI VJ → shareable chain bridge [Copilot track — 2026-06-13]
  Wire `Alucinate.getActiveShaderIds()` / `getCurrentParams()` through `buildSharedChain`+`encodeChain` so a generated VJ stack yields a one-click share link and an optional "save as preset pack" entry. AI-VJ generation and shareable URLs both just shipped — the bridge is the public-audience unlock. Half-day. Frontend-only (src/), decoupled from the scripts/ authoring track.
  → Completed (Copilot track, by 2026-06-20). `src/services/vjToSharedChain.ts` (`mapVJStackToSharedChain`, `buildCatalogDefaultsLookup`) wired into `src/App.tsx` + `src/AutoDJ.ts`. Verified present on main during 2026-06-20 audit.
- [x] Multi-slot chain "remix" / variation explorer
  From any shared chain or preset pack, generate N param/shader variations (reuse `randomizeParams`) into a live thumbnail grid for A/B selection and one-click adopt. Share + randomizer + preset packs are all in place; this is the creative-exploration layer on top. Full-day. (kimi-cli focus 2026-06-20.)
  → Completed 2026-06-20 (kimi-cli swarm, green) and **merged to `main`** (commits `4c8a4d2`, `8f4a06a` — verified ancestors of `origin/main` this audit). `src/services/variationExplorer.ts` (`generateChainVariations`, deterministic mulberry32 PRNG, reuses `randomizeParams`/`buildSharedChain`/`buildCatalogDefaultsLookup`/`mapOrderedParamsToSlotParams`/`shaderCatalog`) + `variationExplorer.test.ts`; `src/components/VariationGrid.tsx` (6-cell grid, param summary, A/B toggle, Adopt via `onApplySharedChain`→`applySharedChain`, shuffle) + `VariationGrid.test.tsx`; "Remix Chain" button wired into `Controls.tsx`. `npx tsc --noEmit` clean, `react-scripts test` 17 suites / 140 tests PASS, prod build compiled.

<!-- New ideas generated 2026-06-27 (New Idea mode; #1 Variation Explorer evolve/breed is this week's kimi-cli focus — picked & set in Today's focus). Below are the two runners-up. -->
- [x] Live-performance control surface for the AI-VJ stack (Web MIDI + keyboard)
  Map MIDI CC / keyboard keys to slot params, randomize triggers, and transition triggers — reuse `startAutoTransition`/`triggerNextTransition`/`randomizeActiveParams` (AutoDJ.ts) and the preset/randomizer infra.
  → COMPLETE 2026-07-11 (in **open PR #936** `feat/midi-control-and-wasm-parity`, not yet merged). Delivered `src/services/controlBindings.ts` + `src/services/midiControl.ts` (Web MIDI, no new npm deps) + unit tests + Controls learn/bind UI; **217 tests pass** per the PR. Landed as a clean single commit on latest `main` after being trapped on the `radiant-seraph-plan-…` branch. Action for Noah: review + merge #936.
- [x] Community preset-pack gallery backed by the Storage Manager backend (whole-stack)
  → **COMPLETE + LIVE 2026-07-18** (backend + frontend MERGED to main; UI wiring landed + gallery mounted in `AiVjStudioPanel.tsx`, fed by `getCurrentChain`). `.swarm-state.md`: 31 suites / 271 Jest, 95 pytest; play-count sort + thumbnails + empty/error states all shipped. Reuses `layerChainShare` encode/decode + `vjToSharedChain`. **This was the last remaining user-written Idea — the Ideas list is now exhausted; routine switches to New Idea mode.**

<!-- New ideas generated 2026-07-25 (New Idea mode; Ideas list exhausted. #1 generative updatedParams campaign + audit tooling (#1011) was that week's kimi-cli focus — SHIPPED, MERGED, #1011 CLOSED 2026-07-26. Below were the two runners-up, grounded in Noah's 2026-07-22 progress audit.) -->
- [~] First real multipass physics stack on GraphRunner — ripple tank (#1009) → **SUPERSEDED 2026-08-01** by Noah's richer 2026-07-29 audit issue **#1041** (multipass physics library polish — fabric/caustics/ripple-tank flagship polish + discoverability). Now GPU-adjacent (60fps discrete @ balanced target). Tracked as #1041; frontend discoverability slice is today's Copilot track (section B).
  Ship one complete user-selectable multipass effect (inject droplets → N wave steps with `repeat≥2` → shade) to validate the GraphRunner content path end-to-end. Scope: full-day-to-multi-day; touches `public/shaders/`, `shader_definitions/simulation/`, `scripts/buildMultipassRegistry.js`.
- [~] Texture format + resolution tiering — kill the rgba32float bandwidth tax (#1008) → **SUPERSEDED 2026-08-01.** Format-tier code (`formatPolicy.ts`/`performancePolicy.ts`/`wgslFormatRewrite.ts` + C++ parity) is now **in tree**; what's left is real-GPU validation = **#1039**, which is **GPU-blocked** (Cloud VM has no adapter). Parked until Noah runs it on a discrete-GPU host.
  Add quality tiers (ultra rgba32float / balanced rgba16float / battery rgba8unorm) driven off a device-capability probe. Scope: multi-day. Now tracked as #1039.

<!-- 2026-08-01 (User Idea mode): the two 07-25 runner-ups above are both superseded/GPU-blocked by Noah's 2026-07-29 audit. That audit (#1038–#1044) is the freshest in-context user signal and is treated as the live Ideas source. Picked: #1038 (Uniforms SoT) — highest-leverage, non-GPU-blocked, content-swarm-multiplier. Runners-up left for future runs: #1043 (finish WebGPU modularization — present/blit + slotDispatch out of frame.ts), #1042 (toolchain modernization — bundle budget CI + optional CRA→Vite). GPU-blocked (need discrete GPU host, not this routine): #1039, #1040, #1044. -->
- [x] Uniforms Single-Source-of-Truth — kill `config.y = delta_time` folklore (#1038) → **DONE 2026-08-08** (`src/contracts/uniforms_layout.json` + `verify:uniforms`/`audit:config-y`; `BINDING_CONTRACT.md`/`WGSL_BUILTINS_GENERATIVE.md` corrected to `rippleCount`; commits `639cd31`/`67a8eb7`, #1055/#1056 on `main`).
- [~] Finish WebGPU modularization + toolchain modernization (old #1043/#1042) → **RENUMBERED/SUBSUMED 2026-08-08** by Noah's 2026-08-07 audit: WebGPU/frame modularization is now largely done (Wave 2 split device/resources/pipeline/frame/present/slotDispatch per epic #1076 table); residual god modules are `RendererManager`/`StoragePanel`/`wasm_bridge` (**#1079**, today's focus). Toolchain (bundle budget CI + TS 5.x, stay CRA/CRACO — no Vite) is now **#1083**.

<!-- 2026-08-08 (User Idea mode): Noah's fresh 2026-08-07 progress audit is the live Ideas source (epic #1076 + children #1077–#1084), superseding the 08-01 #1038–#1044 set. Strategic call: interleave foundation residual with content; prefer multipass excellence + discoverability over volume; stay CRA/CRACO; no GraphRunner C++ port until the WASM decision. Picked #1079 (residual god-module strangler) — highest-leverage, non-GPU-blocked, self-verifiable headless. Non-picks left for future runs below. GPU-blocked (need discrete-GPU host, not this routine): #1078 thumbnails campaign, #1080 WASM promotion evidence, #1081 multipass QA slice. -->
- [x] Residual god-module strangler — modularize `RendererManager.ts` (986→344 LOC) + `StoragePanel.tsx` (737→200 LOC) (#1079, epic #1076) → **DONE + MERGED 2026-08-15** (commit `ffb8eef` via PR #1092; seams `backendLifecycle`/`slotOrchestration`/`inputSourceBridge`/`performanceStatus` + `storage/panels/*` on `main`; `.swarm-state.md` 76 suites / 506 passed, build compiled, `verify:dependency-boundaries` green).

<!-- 2026-08-15 (User Idea mode): #1079 shipped+merged clean → not Fix First. Noah authored TWO fresh foundation/architecture issues 2026-08-14 (#1107 + #1106) — the newest in-context signal, superseding the 08-07 Ideas items as the live source (precedent: prior runs treat Noah's freshest authored issues as the live Ideas source). Picked #1107 (hard-fail WebGPU boot probe) — tighter, fully headless-self-verifiable, direct continuation of #1079's backendLifecycle seam. #1106 (gpu-chores) is bigger/cross-app/GPU-gated → runner-up, future/hands-on. GPU-blocked (need discrete-GPU host, not this routine): #1078 thumbnails Wave B, #1080 WASM promotion, #1081 multipass visual QA. -->
- [x] ~~[in progress — 2026-08-15]~~ **DONE 2026-08-15 (see the [x] entry below and the Done section)** — WebGPU required: hard-fail boot probe — fatalize adapter/device ladder exhaustion, `window.webgpuProbe` breadcrumbs (Chrome vs Edge), blocking canvas-slot overlay, **no WebGL fallback** (#1107, epic #1076)
  Builds on #1079's `backendLifecycle`; adapter ladder already in `src/renderer/webgpuDevicePolicy.ts`. Make exhaustion fatal (no empty canvas / no GL branch), publish structured probe JSON, block only the canvas slot. Non-GPU, fully build+test verifiable (mock `navigator.gpu`). Today's kimi-cli focus.
- [ ] gpu-chores: shared pre-FX analysis kernel layer — histogram/reduce/LUT/downsample facade adopting the renderer's single `GPUDevice`, WebGPU→WASM→TS fallback, parity vs CPU/WASM goldens (#1106, epic #1076; Tier A of a cross-app rollout)
  Bigger, cross-app, correctness is GPU-gated (kernels need a device); parity scaffolding is CPU-testable. Runner-up to #1107 — future/hands-on or a multi-day swarm. Freshest 2026-08-14 signal alongside #1107.
- [ ] Multipass library expansion on TS GraphRunner — 2–3 new Physics Lab flagship stacks (#1081, epic #1076)
  Chromatographic fluid / true reaction-diffusion / soft-body-metaball tank / Poincaré post / optical-flow feedback. **GPU-gated acceptance** (visual QA on real GPU) → picked as a Noah-hands-on / future run, not a headless routine pick. Strategic priority per the 08-07 audit ("multipass is where the engine differentiates from a Shadertoy dump").
- [ ] VJ Studio 2.0 — set recorder + export, WebMIDI/OSC harden, 6-slot chain deep-links, mobile performer layout (#1082, epic #1076)
  Round-trip VJ-set JSON, non-blank TS-path recording, lazy OSC chunk, mobile 390×844 layout. Frontend-heavy, partly recording/GPU-gated. Multi-day product track; future run.
- [ ] Toolchain hygiene — bundle-budget CI + TS 5.x, stay CRA/CRACO (#1083, epic #1076)
  `verify:bundle-size` budget without AI chunks, TS 5.x bump, root-junk hygiene. Explicitly **no Vite migration** per the 08-07 call. Runner-up.
- [x] WebGPU required: hard-fail boot probe — fatalize adapter/device ladder exhaustion, `window.webgpuProbe` breadcrumbs (Chrome vs Edge), blocking canvas-slot overlay, **no WebGL fallback** (#1107, epic #1076) → **DONE + MERGED + CLOSED 2026-08-15** (Noah closed #1107 completed; `webgpuBootProbe.ts`/`WebGpuProbeFailureOverlay.tsx`/`docs/WEBGPU_BOOT_PROBE.md` on `main` via `b03ac5a`; `.swarm-state.md` iter-2 = 81 suites / 535 passed, build compiled, boundaries green, `RendererManager` 348 LOC).
- [x] gpu-chores: shared pre-FX analysis kernel layer — histogram/reduce/LUT/downsample facade adopting the renderer's single `GPUDevice` (#1106, epic #1076) → **LANDED + CLOSED** (verified on `main`: `src/gpuChores/` = `GpuChoresHost.ts` (`attach(device)`-only, never `requestAdapter`), `histogram.ts`/`reduce.ts`/`lut.ts`/`downsample.ts`/`killSwitch.ts` + tests, `docs/GPU_CHORES.md`). Follow-ons in the 08-21 audit: #1127 (live catalog path) + #1126 (kill the *other* second devices — today's focus).

<!-- 2026-08-22 (User Idea mode): #1107 shipped+merged+closed clean → not Fix First. Noah ran a fresh 2026-08-21 progress audit that CLOSED the old epic #1076 + its children (#1081/#1082/#1083/#1084 etc. no longer open) and opened a new live set (#1123–#1129). Only 8 issues open total: #1123, #1124, #1125, #1126, #1127, #1128, #1129, and #1080 (WASM promotion, GPU-gated, carried from 08-07). This 08-21 set is the newest in-context signal → live Ideas source. Picked #1126 (stop second WebGPU devices) — the only labeled `bug`, foundation, fully headless-self-verifiable, direct continuation of #1107's one-device discipline. Non-picks below. GPU-gated (need discrete-GPU host, not this routine): #1124 format tiers (GPU evidence), #1080 WASM promotion. C++/toolchain blast radius (emscripten build blocked on Cloud VM, #848): #1123, #1125. Scripts/CI-side → today's Copilot track B: #1128. Explicitly deferred by Noah ("Later:"): #1129. -->
- [x] Stop second WebGPU devices — `ShaderValidator`/`ShaderScanner`/depth-loader adopt the single renderer `GPUDevice` (via `window.webgpuProbe.handoff` / a `RendererManager.getDevice()`) instead of each calling `requestAdapter()`/`requestDevice()`; adopt-or-idle, delete support-pings, Transformers prefers wasm/cpu while canvas is live (#1126, 2026-08-21 audit)
  Offenders grounded in tree: `ShaderValidator.tsx:95-98`, `ShaderScanner.tsx:100/161/167`, `depthEstimation/loader.ts:28-29`. Second devices break `?renderer=wasm` (Windows "No available adapters"), fight gpu-chores, and compile against a different feature/limit set. Non-GPU; acceptance = Jest spies asserting no `requestAdapter` when a handoff exists.
  → **DONE + MERGED + CLOSED 2026-08-29.** Verified on `main` @ `a27a627`: `src/utils/adoptedGpuDevice.ts` landed, `src/utils/requestPixelocityDevice.ts` **deleted**, `RendererManager.getDevice()` at `:305` (facade 353 LOC), `depthEstimation/loader.ts:28` carries the "Never call requestAdapter() here — the renderer owns the sole adapter/device" contract comment. Full `src/**` grep for `requestAdapter|requestDevice` outside tests returns **zero** live call sites beyond `webgpuBootProbe.ts` (sole owner) + `webgpuDevicePolicy.ts` (the ladder it calls). The whole 08-21 set (#1123–#1129) is now closed.

<!-- 2026-08-29 (User Idea mode): #1126 shipped+merged+closed clean, and the 08-24 hygiene pass's RED `audit:dead-sliders` CI gate is now GREEN on main (0 new / 28 known, exit 0) → not Fix First. Noah ran a fresh 2026-08-26 progress audit that CLOSED the entire 08-21 set (#1123–#1129) and opened a new live set (#1179–#1185). Only 8 issues open total: #1179, #1180, #1181, #1182, #1183, #1184, #1185, and #1080 (WASM promotion, GPU-gated, carried since 08-07). This 08-26 set is the newest in-context signal → live Ideas source. Picked #1180 (TS/C++ workgroup fallback + empty-placeholder packing parity) — the only labeled `bug`, foundation, parts A+B fully headless-self-verifiable, and it closes the last dispatch/upload divergence left by the #1107→#1126 device-discipline arc. Non-picks below. Scripts/CI/catalog-side → today's Copilot track B: #1184 (Noah explicitly scoped the 8×8 migration OUT of #1184 and INTO #1180, so the two tracks are author-decoupled). GPU-gated (need discrete-GPU host, not this routine): #1185 thumbnail capture, #1080 WASM promotion, and #1180's part C acceptance. Gated on #1080 by the author: #1183. Tagged "future" by the author: #1182. -->
- [ ] **[in progress — 2026-08-29]** Engine: TS/C++ workgroup fallback + empty-placeholder packing parity (#1180, 2026-08-26 audit; the only `bug` in the set)
  (A) TS `ShaderCompilation.ts:68-69` defaults unparsed shaders to `{x:8,y:8}` while C++ `wasm_internal.cpp:54-55` defaults to 16×16 — canonical dispatch is 16×16×1, so TS **under-dispatches 3/4 of the frame**; `ShaderCompilation.workgroup.test.ts:77-79` asserts the wrong value as intent. (B) 26 leftover `@workgroup_size(8,8,1)` catalog shaders (verified histogram: 1359 × 16×16×1, 26 × 8×8×1, 1 × 256×1×1, 1 × 16×16×4). (C) C++ `resources.cpp:166` creates the 1×1 `emptyTexture_` from the un-reset depth `texDesc` (r32float, 4 B/px) but `:179` uploads `bytesPerRow = 16` → real-GPU WebGPU validation error on first WASM init. Anti-drift deliverable: a new `src/contracts/workgroup_dispatch.json` validated by the existing `scripts/verify-device-policy-sync.js`. Today's kimi-cli focus.
- [ ] Contract hygiene — extraBuffer residual, dead sliders, catalog count/ID SoT (#1184, 2026-08-26 audit; ci/infrastructure/tech-debt)
  README advertises 1,291 shaders while the manifest/definitions/WGSL counts are 1,347 / 1,360 / 1,382; 22 underscore IDs need an alias map; 13 ID≠filename cases are mostly legitimate graph parents that the hygiene script should whitelist; 32 dynamic-index extraBuffer writes need human triage; the 93 known extraBuffer entries need an `engine-owned` vs `shader-bug` baseline split. Scripts + CI + README + reports only — **today's Copilot track, section B.** Author-decoupled from #1180 (he put the 8×8 migration in #1180 and out of here).
- [ ] Foundation: TypeScript the WASM JS glue, drop dead CRA deps, CI copy-drift gate (#1179, 2026-08-26 audit)
  Successor to the closed #1125. Headless-ish TS refactor, but dropping CRA deps can destabilize `npm run build` — real blast radius. Strong runner-up for a future run.
- [ ] Engine: gpu-chores live catalog auto-exposure — opt-in source normalize (#1181, 2026-08-26 audit)
  Successor to the closed #1127; now unblocked by #1126's device SoT. Real validation is GPU-gated → future/hands-on.
- [ ] Product: thumbnail coverage 26% → 50%+ with deferral manifest (#1185, 2026-08-26 audit)
  Capture needs a discrete GPU → parked for a workstation run; the deferral-manifest half is headless and could be a future Copilot track.
- [ ] Architecture: naga-WASM compile gate + Physics Lab set 2 graphs + GraphRunner C++ (#1183, 2026-08-26 audit)
  Explicitly **gated on #1080** by the author. Multi-day, cross-cutting. Not a candidate until the WASM promotion decision lands.
- [ ] Engine+Product: all-slot audio mapping + OSC/WebMIDI VJ 2.0 (#1182, 2026-08-26 audit)
  Author-tagged `future`. Frontend-heavy product track; future run.
- [ ] WASM: real-GPU promotion evidence session (or explicit demotion) — close gates 1–4 (#1080, carried from the 2026-08-07 audit)
  GPU-gated **and** a human roadmap decision. The oldest open issue in the repo. Not routine-ownable — needs Noah on a discrete-GPU host.
<!-- 2026-08-29: EVERY item below from the 2026-08-21 audit (#1123–#1129) is now CLOSED by Noah and superseded by the 2026-08-26 audit set (#1179–#1185) recorded above. Kept for lineage only — do NOT pick from this block. Mapping forward: #1128→#1184 (hygiene leftovers) + #1180 (the 8×8/16×16 half), #1127→#1181, #1125→#1179, #1123/#1124→#1180, #1129→#1182/#1183. -->
- [~] Catalog hygiene — reconcile shader ID vs filename vs graph entries, detect leftover 8×8 workgroups + wire the 16×16 dispatch fallback (#1128, 2026-08-21 audit; CI/infra/tech-debt) → **CLOSED; split into #1184 (hygiene/catalog SoT) + #1180 (8×8 migration + dispatch fallback — today's focus)**
  Scripts + CI + manifests only (decoupled from src). **Today's Copilot track, section B** — scoped to detection/reconciliation tooling, NOT bulk `public/shaders/**` edits (content stream owns those).
- [~] Engine: gpu-chores live catalog path — auto-exposure + LUT on source, opt-in (#1127, 2026-08-21 audit) → **CLOSED; succeeded by #1181**
  Depends on #1126 single-device adoption; perf/webgpu. Real validation GPU-gated → future/hands-on after #1126 lands the device SoT.
- [~] Foundation: Single WASM-bridge SoT — TypeScript the `wasm_bridge.js` glue, drop dead CRA deps (#1125, 2026-08-21 audit) → **CLOSED; succeeded by #1179**
  Headless-ish TS refactor but intersects the WASM/C++ epic + CRA build (dropping deps can destabilize `npm run build`). Runner-up; sequence after or alongside #1123.
- [~] Foundation: WASM compile SoT — CMake export drift + C++ optional-feature parity (#1123, 2026-08-21 audit) → **CLOSED (landed); residual parity folded into #1180**
  C++/CMake/emscripten — build is blocked on this routine's Cloud VM (#848). Noah-hands-on / discrete-toolchain run.
- [~] Foundation: Make rgba16float format tiers real — probe + C++ upload packing + **GPU evidence** (#1124, 2026-08-21 audit) → **CLOSED (mostly landed); the leftover 1×1 empty-placeholder packing bug is #1180 part C — today's focus**
  Explicitly GPU-gated ("GPU evidence"). Parked for a discrete-GPU host.
- [~] Later: Physics Lab set 2 + all-slot audio mapping, after format-tier foundation (#1129, 2026-08-21 audit) → **CLOSED; split into #1182 (audio/VJ) + #1183 (Physics Lab set 2, gated on #1080)**
  Noah tagged this "Later:"/future — deferred by the author. Not a candidate until the format-tier foundation lands.

## Backlog
<!--
Unfinished items, known bugs, deferred ideas.
Routine maintains this automatically — you can add items too.
-->
- [ ] **NEW 2026-08-24 — Whole-stack pipeline hygiene pass verdict: pipeline healthy / 3 issues found (2 significant, 1 reconfirmed-tracked).** Full stage-by-stage run on `claude/image-video-effects-hygiene-4s7cb9` (clean `node_modules`, real command output, no renderer device wiring touched):
  1. **INSTALL — PASS.** `npm ci --ignore-scripts` (23s, 1424 packages). `npx tsc --version` → 5.4.5 resolves clean. (`npm ci` plain still blocked by `sharp` postinstall behind the agent proxy — pre-existing backlog item below, unchanged, not re-filed.)
  2. **TYPECHECK + TEST — PASS.** `npx tsc --noEmit` clean (7.0s). `CI=true npx craco test --watchAll=false` → **84 suites, 559 passed + 1 skipped (560 total)** — up from the `.swarm-state.md` 2026-08-23 baseline (81/546), consistent with work landed since. The `CommunityGallery`/`useThumbnailManifest` fetch-mock test flagged as a 2026-08-01 backlog item (line below) is **confirmed GREEN** (20/20 in a targeted rerun) — only non-gating React `act()` console warnings remain. That backlog item is now stale and can be closed.
  3. **BUILD — PASS.** `SKIP_WASM_BUILD=1 npm run build` → "Compiled successfully" (1346 shaders/14 categories manifest). **Bundle-size backlog item (line below, ~2.7 MB gzipped main.js) has RESOLVED ITSELF**: `main.js` is now **272.5 kB gzipped** (1.0 MB raw) — code-splitting already landed since that note was written. The heavy AI deps are separate lazy chunks: `web-llm` 2.14 MB gz, `transformers` 179.9 kB gz, `hls` 159.6 kB gz. CRA's "bundle larger than recommended" warning is about the **sum across all chunks**, not `main.js` itself; no code-split work needed here.
  4. **SHADER LISTS — PASS.** `node scripts/generate_shader_lists.js` deterministic (zero git diff vs. committed output) and duplicate-id-clean (script's own inline dedup check emitted no `SKIPPED DUPLICATES`).
  5. **DEPLOY DRY-RUN — PASS, no DreamHost connection made.** `scripts/deploy_credentials.py` reads `DEPLOY_PASS`/`FTP_PASS`/`SHADER_SYNC_PASSWORD` from env/`.env.deploy`/SSH key/TTY prompt only — **no hardcoded creds** (confirms the 2026-05-30 fix holds). Computed a local-only manifest diff for `deploy_app_only.py` against the committed `.deploy_app_manifest.json` (hash comparison, zero network calls): would upload 412 of 424 build files, skip 12 unchanged. Most "uploads" are because the tracked manifest only covers 40 of 424 current build files (never captured thumbnails/wasm-bridge/shader-lists in a prior run) — informational, not a defect. Also found 7 stale manifest entries pointing at now-removed hashed bundles (`main.a6a3c33b.js`, `shader-lists/interactive.json`, `shader-lists/liquid.json`); `deploy_app_only.py` has no delete logic so these will linger in the manifest file (cosmetic only — the remote files were presumably already cleaned by a `deploy.py` run, which does support cleanup).
  6. **BACKEND — PASS, with one flagged finding.** `python -m pytest storage_manager/tests/ -q` → **95 passed, 1 warning** (StarletteDeprecationWarning, non-blocking). **Correction to the task's suggested invocation:** must run from the **repo root** (`python -m pytest storage_manager/tests/ -q`), not `cd storage_manager && python -m pytest tests/ -q` — the tests `import storage_manager.app`, which only resolves when the repo root is on `sys.path` (i.e. cwd = repo root under `python -m pytest`). AST/`py_compile` clean on `app.py` + all `routes/*.py`. **CORS finding (see dedicated item below): `ALLOWED_ORIGINS` does NOT contain `noahcohn.com`** despite a 2026-06-15 backlog note claiming it was verified present — and carries a `"*"` wildcard entry that, combined with `allow_credentials=True`, likely defeats CORS protection entirely. Flagged as its own item, not silently changed (security-sensitive, not a trivial hygiene fix).
  7. **DEPENDENCY AUDIT — PASS (reconfirmed, chain shifted).** `npm audit --production` → **4 high severity, 0 critical/moderate** (previously reported as 5: 1 critical/3 high/1 moderate via `protobufjs`/`onnxruntime-web`). The vulnerable chain is still entirely `@xenova/transformers` (→ `@huggingface/transformers`) for depth estimation, but the specific CVEs have shifted to **`adm-zip`** (via `onnxruntime-node`, 4GB-allocation crafted-ZIP DoS, no fix) and **`sharp`** (libvips CVE-2026-33327/33328/35590/35591, no fix). No new vulns outside this known chain. Not auto-fixed per instructions (fix requires downgrading `@xenova/transformers`, breaks depth estimation). Recommend updating the old protobufjs-era note to reflect the new CVE identities next time this is revisited.
  8. **VERIFY SCRIPTS — mostly PASS, one blocking gate found RED.** `npm run verify:dependency-boundaries` PASS (TS version pinned, no duplicate deps, AI packages behind dynamic loaders, no leaked test-CDN URLs). `npm run verify:uniforms` PASS (TS/C++/docs contract in sync). `npm run audit:extrabuffer` PASS (1377 files scanned, 0 new violations, 93 known-baselined, 32 dynamic-index for review). `npm run audit:config-y` informational-only, ran clean (68 reads / 58 shaders classified, non-gating). **`npm run audit:dead-sliders` FAILS (exit 1): 73 new dead sliders across ~30+ shader files** not in the triage baseline (`reports/dead_sliders_audit_baseline.json` covers only 3 shader ids). This is **not a regression from this session** — `git diff HEAD origin/main` shows zero difference in shaders/scripts/reports between this branch and current `origin/main` (`ab8e3d6`), so **this gate is genuinely red on `main` right now**. `.github/workflows/ci.yml:248-249` runs this exact unfiltered command as a step labeled "Audit dead sliders (blocking)" on every push/PR to main/develop — no path filters. The committed `reports/dead_sliders_audit.md` on disk (from commit `6d2b724`, "Batch 67") only reflects a 10-definition sample run, masking that a full-corpus run is red. See dedicated item below — this is real per-shader remediation work (wire the WGSL read or remove each dead param), explicitly out of scope for a hygiene pass.

- [ ] **NEW 2026-08-29 — Noah ran a fresh 2026-08-26 progress audit; it is the newest live backlog.** He CLOSED the entire 2026-08-21 set (**#1123–#1129**, all of them) and opened a new one. **Only 8 issues open total:** **#1179** (TypeScript the WASM JS glue, drop dead CRA deps, CI copy-drift gate — successor to #1125; CRA-build blast radius), **#1180** (TS/C++ workgroup fallback + empty-placeholder packing parity — **today's kimi-cli focus, section A**; the only `bug` in the set), **#1181** (gpu-chores live catalog auto-exposure — successor to #1127, now unblocked by #1126's device SoT; GPU-gated validation), **#1182** (all-slot audio mapping + OSC/WebMIDI VJ 2.0; author-tagged `future`), **#1183** (naga-WASM compile gate + Physics Lab set 2 + GraphRunner C++; **explicitly gated on #1080**), **#1184** (contract hygiene — extraBuffer residual, dead sliders, catalog count/ID SoT; **today's Copilot track, section B**), **#1185** (thumbnail coverage 26%→50%+ with deferral manifest; capture is GPU-gated), and **#1080** (WASM real-GPU promotion evidence, carried since 08-07; GPU-gated + human decision). **Author-sanctioned track split:** #1184's "Out of scope" explicitly hands the 8×8 workgroup migration to #1180, so today's A and B tracks cannot collide by construction.
- [ ] **NEW 2026-08-29 — #1184 catalog-count drift, quantified by Noah's 08-26 audit and confirmed in tree.** `README.md` advertises **1,291** shaders (4 occurrences: lines 3/58/136/145) while the audit counts 1,347 unified-manifest IDs / 1,360 `shader_definitions/**/*.json` / 1,382 `public/shaders/*.wgsl`. Also outstanding: 22 underscore IDs (`aurora_borealis`, `gen_mandelbulb_3d`, `kimi_flock_symphony`, …) needing a generated alias map so saved URLs don't 404; 13 ID≠filename cases that are **mostly legitimate graph parents** (`ripple-tank` → `ripple-tank-step`) and should be whitelisted via `multipass.graph` rather than reported as errors; 32 unresolved dynamic-index extraBuffer writes needing human triage; and the 93 known extraBuffer entries needing an `engine-owned` vs `shader-bug` baseline split. Note `public/shader-manifest-unified.json` is **build-generated, not committed** (`npm run build:manifest` → `scripts/build-unified-manifest.ts`), so the count check must run post-build. **Today's Copilot track, section B.**
- [x] ~~**NEW 2026-08-24 — CI gate "Audit dead sliders (blocking)" is RED on `main` right now: 73 pre-existing dead sliders across ~30+ shader files, undetected because the triage baseline only covers 3 shader ids.** Found during the 2026-08-24 hygiene pass (item above, stage 8). `python -m pytest`-style full-corpus run of `python3 scripts/audit_dead_sliders.py` (exactly what `.github/workflows/ci.yml:249` runs, unfiltered, on every push/PR to main/develop) exits 1 with 1189 definitions scanned, 73 new dead sliders (full list in `reports/dead_sliders_audit.md` after a local rerun — examples: `plasma` (4 params), `stella-orbit` (4), `gen-bioreactor-bloom` (4), `gen-celestial-weave` (4), `hex-lens` (4), `flux-core` (4), `temporal-feedback-zoom-tracer` (4), plus ~15 more shaders, mostly `zoom_params.{x,y,z,w}` never read in the WGSL). The baseline (`reports/dead_sliders_audit_baseline.json`) has only 3 entries and was last touched by commit `6d2b724` alongside a *10-definition sample* report — CI's actual invocation has no `--files`/`--category` scoping, so this has likely been silently red for a while, or the CI job itself isn't actually gating merges (branch-protection status unverified — outside this session's access). **Not fixed here** — each of the 73 needs a human/swarm judgment call (wire the WGSL read vs. remove the param), same remediation shape as the already-tracked "46-file legacy extraBuffer writes" item below. Recommend a dedicated audit-swarm pass mirroring that one, or at minimum `--write-baseline` after deliberate triage (never blind) to un-red CI in the interim.~~ → **RESOLVED, verified 2026-08-29.** Re-ran the exact unfiltered CI invocation (`python3 scripts/audit_dead_sliders.py`) on `main` @ `a27a627`: **1191 definitions scanned, 0 new dead sliders, 28 known/triaged, 0 def errors — `AUDIT PASS`, exit 0.** The gate is green; remediation landed between 08-24 and now (the baseline/report were last touched by `ac64084` "whole-stack pipeline hygiene pass + refresh audit reports (#1173)" and subsequent generative-cohort upgrades). `audit_extrabuffer.py` also PASS the same run (1380 files, 0 new, 93 known-baselined, 32 dynamic-index for review, 0 out-of-range). **Residual, now tracked as #1184 (today's Copilot track B), not as a CI-red emergency:** the 32 dynamic-index extraBuffer writes still need human triage, and Noah's 08-26 audit found the known/baselined dead-slider population needs an `engine-owned` vs `shader-bug` split rather than one flat list.
- [ ] **NEW 2026-08-24 — Storage Manager CORS: `ALLOWED_ORIGINS` (storage_manager/config.py:57-64) is missing `noahcohn.com` and carries a `"*"` wildcard that likely defeats CORS entirely when combined with `allow_credentials=True`.** Found during the 2026-08-24 hygiene pass (item above, stage 6) — read-only verification, not fixed here (security-sensitive config change needs explicit sign-off, not a silent hygiene edit). Current list: `localhost:3000`, `localhost:5173`, `test1.1ink.us`, `test.1ink.us`, `storage.1ink.us`, and `"*"`. Two problems: (a) `noahcohn.com` is absent, contradicting the 2026-06-15 backlog note below ("CORS allowlist... confirmed... `noahcohn.com`... `localhost:3000`") which was either wrong or the config has regressed since; (b) `app.py:44-50` sets `allow_credentials=True` while `"*"` is in `allow_origins` — Starlette's `CORSMiddleware`, when `"*"` is present alongside `allow_credentials=True`, reflects the request's actual `Origin` header rather than emitting a literal `*` (which credentialed responses can't legally use), meaning **any origin can make credentialed cross-site requests** to this API. Recommend: drop the `"*"` entry, add the real `noahcohn.com` origin(s), and add a regression test asserting the allowlist is finite. Needs Noah's call given the security blast radius.

- [x] ~~Working branch `…thm8fq` 92 ahead of `origin/main`~~ → **RESOLVED 2026-07-11.** breed/favorites landed on `main` (`94b0619`, verified ancestor of `origin/main`); `main` has advanced substantially since 2026-06-27 (generative-shader PRs #907–937, shader-scan fix #935). This week the routine cut a fresh `claude/charming-lamport-1e4x50` from current `main` (0/0). The long-lived-divergence risk is cleared.
- [x] ~~**Foundation epic #912: App/Controls strangler refactor IN FLIGHT (open PR #938, Wave 3).**~~ → **MERGED to `main` 2026-07-18.** `App.tsx`/`Controls.tsx` carved into `src/hooks/*` (`useShareChain`, `useDepthEstimation`, `useAudioReactiveParams`, …) + `src/components/controls/` (`ControlsContainer`, `panels/ParamSlidersPanel`, `panels/SlotStackPanel`, …); commit `f150e13` "split four 1000+ line source files under 700 lines" is on `main`. The "must NOT touch App/Controls" boundary that deferred gallery wiring is **retired** — today's focus wires the gallery. Original in-flight note retained below for history:
- [ ] _(history)_ **Foundation epic #912: App/Controls strangler refactor is IN FLIGHT (open PR #938, Wave 3).** `App.tsx` (~2397→~2076) and `Controls.tsx` (~1834→~1663) are being carved into hooks/panels (`useDepthEstimation`/`useAudioReactiveParams`/`useShareChain`, `ParamSlidersPanel`/`RendererBackendPanel`/`SlotStackPanel`, canonical `RendererSwitcher`), `tsconfig` target raised to ES2020, `docs/APP_STRUCTURE.md` added. **Direct blast-radius warning for the frontend routine:** any new frontend work must avoid editing `App.tsx`/`Controls.tsx` until #938 (and the MIDI PR #936, which also edits `Controls.tsx`) land, or it will conflict. Today's kimi-cli focus is scoped to NEW files + backend for exactly this reason.
- [x] ~~**Two large frontend PRs open & unmerged (2026-07-11): #936 (MIDI + WASM parity) and #938 (foundation Wave 3).**~~ → **BOTH MERGED to `main` 2026-07-18.** #936 landed via merge PR **#957** (`0069851` "Merge pull request #957 from …feat/midi-control-and-wasm-parity"); #938 strangler refactor landed (`f150e13`). Sequencing worked out. Both blast-radius blockers on `App.tsx`/`Controls.tsx` are cleared → gallery UI wiring is unblocked (today's focus).
- [x] ~~**`CommunityGallery` is MERGED but UNMOUNTED (dead code).**~~ → **RESOLVED / STALE-NOTE CORRECTED 2026-07-25.** The gallery IS mounted and live on `main`: `src/components/controls/panels/AiVjStudioPanel.tsx:8` imports it, `:589` renders it, fed by `getCurrentChain` from `src/hooks/useShareChain.ts` (added last week). `.swarm-state.md` records the 07-18 wiring session COMPLETE (31 suites / 271 Jest, thumbnails + play-count sort + empty/error states all done); MEMORY.md 07-22 explicitly corrected the "unmounted" claim. Gallery is gated to `inputSource === 'image'` (same as `AiVjStudioPanel`). Only residual: live browser proof against production `storage.noahcohn.com` was never run (Cloud VM has no GCS creds / network) — mocked pytest + Jest round-trip are the authoritative E2E to date.
- [ ] **NEW 2026-07-25 — one broken generative shader on main: `gen-luminescent-aether-plasma-astro-axolotl.wgsl`** naga-fails with "invalid left-hand side of assignment" (per MEMORY 07-22 Batch-14 note; full scan 1314/1315). Isolated, non-blocking (forward-only gate on changed files lets it sit), but a real defect. Fix candidate for a Copilot/Claude one-off or the next audit swarm — explicitly OUT of scope for today's kimi-cli `updatedParams` campaign.
- [x] ~~**NEW 2026-07-25 — 2026-07-22 progress-audit issue set (#1007–#1013) is the live backlog.**~~ → **SUPERSEDED 2026-08-01.** That set is largely resolved (#1011 CLOSED 2026-07-26 = last week's landed focus; #1012/#921/#890/#1013 hygiene closed per #1044/#1040 references). Noah ran a **fresh 2026-07-29 progress audit** creating the new live backlog **#1038–#1044** (below).
- [x] ~~**NEW 2026-08-01 — 2026-07-29 progress-audit issue set (#1038–#1044) is the live backlog.**~~ → **SUPERSEDED 2026-08-08.** #1038 (Uniforms SoT) **DONE + MERGED** (see Done). Noah ran a **fresh 2026-08-07 progress audit** creating the new live backlog — epic **#1076** + children **#1077–#1084** (below). The old #1038–#1044 items map forward: #1039/#1040→#1080 (WASM/GPU promotion), #1041→#1081 (multipass), #1042→#1083 (toolchain), #1043→#1079 (modularization residual), #1044→#1078 (thumbnails).
- [ ] **NEW 2026-08-15 — two fresh Noah-authored foundation issues 2026-08-14 are the newest live signal: #1107 + #1106.** #1107 (WebGPU required: hard-fail boot probe — fatalize the adapter/device ladder, `window.webgpuProbe` breadcrumbs, blocking canvas-slot overlay, **explicitly no WebGL fallback**) = **today's kimi-cli focus, section A**, a direct continuation of #1079's `backendLifecycle` seam; fully headless-self-verifiable. #1106 (gpu-chores: shared pre-FX analysis kernel layer — histogram/reduce/LUT/downsample facade adopting the renderer's single `GPUDevice`, WebGPU→WASM→TS fallback, parity vs CPU/WASM goldens; Tier A of a cross-app rollout with `Chromashift` as the extract source) = runner-up, bigger/cross-app/GPU-gated → future or Noah-hands-on. Both reference epic #1076. Also open on the epic and **GPU-blocked on this routine's Cloud VM**: #1078 thumbnails Wave B (capture needs a discrete GPU — but Wave A coverage-regression gate is headless and is **today's Copilot track, section B**), #1080 WASM promotion evidence, #1081 multipass visual QA.
- [ ] **NEW 2026-08-22 — Noah ran a fresh 2026-08-21 progress audit; it is the newest live backlog.** He CLOSED the old epic **#1076 + its children** (#1081 multipass, #1082 VJ Studio 2.0, #1083 toolchain, #1084 generative queue, etc. — no longer open) and opened a new set. **Only 8 issues open total:** **#1123** (WASM compile SoT — CMake export drift + C++ optional-feature parity; C++/emscripten, build-blocked on Cloud VM #848), **#1124** (make rgba16float format tiers real — probe + C++ upload packing + **GPU evidence**; GPU-gated), **#1125** (single WASM-bridge SoT — TypeScript the `wasm_bridge.js` glue + drop dead CRA deps; TS refactor but CRA-build blast radius), **#1126** (stop second WebGPU devices — Validator/Scanner/depth adopt the renderer `GPUDevice`; **today's kimi-cli focus, section A** — the only `bug`, headless-self-verifiable), **#1127** (gpu-chores live catalog path — auto-exposure + LUT on source, opt-in; depends on #1126, GPU-gated validation), **#1128** (catalog hygiene — ID vs filename vs graph entries, leftover 8×8 workgroups, 16×16 dispatch fallback; CI/scripts — **today's Copilot track, section B**), **#1129** (Noah-tagged "Later:" — Physics Lab set 2 + all-slot audio mapping; deferred), and **#1080** (WASM real-GPU promotion evidence, carried from 08-07; GPU-gated + human decision). The old 08-14 #1106/#1107 pair are both **closed** (#1107 shipped; #1106 gpu-chores kernels landed as `src/gpuChores/**`).
- [x] ~~**NEW 2026-08-15 — two fresh Noah-authored foundation issues 2026-08-14 are the newest live signal: #1107 + #1106.**~~ → **RESOLVED 2026-08-22.** #1107 shipped+merged+closed (see Done); #1106 kernels landed on `main` (`src/gpuChores/**`) and the issue is closed. Both superseded by the 08-21 audit above. #1107 (WebGPU required: hard-fail boot probe — fatalize the adapter/device ladder, `window.webgpuProbe` breadcrumbs, blocking canvas-slot overlay, **explicitly no WebGL fallback**) = **today's kimi-cli focus, section A**, a direct continuation of #1079's `backendLifecycle` seam; fully headless-self-verifiable. #1106 (gpu-chores: shared pre-FX analysis kernel layer — histogram/reduce/LUT/downsample facade adopting the renderer's single `GPUDevice`, WebGPU→WASM→TS fallback, parity vs CPU/WASM goldens; Tier A of a cross-app rollout with `Chromashift` as the extract source) = runner-up, bigger/cross-app/GPU-gated → future or Noah-hands-on. Both reference epic #1076. Also open on the epic and **GPU-blocked on this routine's Cloud VM**: #1078 thumbnails Wave B (capture needs a discrete GPU — but Wave A coverage-regression gate is headless and is **today's Copilot track, section B**), #1080 WASM promotion evidence, #1081 multipass visual QA.
- [ ] **NEW 2026-08-08 — 2026-08-07 progress-audit issue set (epic #1076 + #1077–#1084) is the live backlog.** #1079 now **DONE + MERGED** (see Done). #1076 epic (foundation residual + discoverability before next content wave); #1077 catalog `--base-url` hardcode drift + prestart hygiene (**today's Copilot track, section B** — env toggle so prestart/prebuild don't force absolute test-CDN URLs into local lists); #1078 thumbnail coverage ~27%→60–80% (**GPU-blocked** — needs discrete-GPU host); **#1079 modularize `RendererManager` (986 LOC) + `StoragePanel` (737 LOC)** (**today's kimi-cli focus, section A**); #1080 real-GPU WASM promotion evidence or documented demotion (**GPU-blocked + human decision**; policy stays Tier B); #1081 multipass library expansion on TS GraphRunner, 2–3 new flagships (**GPU-QA-gated**; strategic priority, Noah-hands-on/future run); #1082 VJ Studio 2.0 (recorder/export, OSC/WebMIDI harden, chain deep-links, mobile layout); #1083 toolchain hygiene (bundle-budget CI + TS 5.x, **stay CRA/CRACO — no Vite**); #1084 finish the generative single-pass queue (~100 pending, Batch 40+; content track, Jules/swarm-owned). Mechanical 700-LOC splits #1058–#1063 are lower leverage than #1079 per the epic.
- [ ] **NEW 2026-08-08 — content stream is running autonomously (Jules + generative swarm).** `.swarm-state.md` = updatedParams campaign complete through tracker #355 / Batch 39 (2026-08-06); ~100 single-pass generatives still in the clean queue (#1084). Recent merges #1085–#1088 (new generative shaders) + open **draft PR #1089** (Jules `gen-gravitational-ferrofluid-singularity-engine`). This flow is **decoupled from today's src-refactor focus** — flagged so the kimi/#1079 src track stays aware that `public/shaders/**` + `shader_definitions/**` + `shader_plans/queue.json` are actively churning (do NOT touch them in the refactor).
- [x] ~~**extraBuffer reserved-zone footgun is a recurring swarm regression.**~~ → **MECHANICAL GUARD LANDED 2026-07-26.** `scripts/audit_extrabuffer.py` (+ test) ships, wired as a **blocking** CI gate + `npm run audit:extrabuffer`; `wgsl_precommit_gate.py` does a baseline-aware extraBuffer scan on changed files. `[0..4]` reserved, `[5..132]` = engine FFT bins (stomped every audio frame), `[133..255]` = safe. New violations now fail CI.
- [x] ~~**dead-slider pattern (JSON params never read in WGSL).**~~ → **AUDITOR LANDED 2026-07-26.** `scripts/audit_dead_sliders.py` (+ test) ships, blocking CI gate + `npm run audit:dead-sliders`; generative subset baselined to **0**. `npm run audit:generative` runs both.
- [ ] **NEW 2026-08-01 — legacy extraBuffer[0..132] writes remain in ~46 files / 137 sites** (`reports/extrabuffer_write_audit_baseline.json`, baselined so they don't fail CI). Latent audio-corruption bugs pre-dating the guard (e.g. `cyber-rain` writes `[1]/[4]/[6]`). NOT in today's #1038 scope. Candidate for a future audit-swarm remediation pass, batched + gate-verified.
- [x] ~~**NEW 2026-08-01 — Jest status on `main` UNVERIFIED this run.**~~ → **RESOLVED 2026-08-24 (hygiene pass).** `.swarm-state.md` (07-26) recorded 2 failing tests in `src/components/CommunityGallery.test.tsx` (`useThumbnailManifest` fetch-mock TypeError) as src-track WIP. Confirmed GREEN this run: targeted rerun `--testPathPattern="CommunityGallery|useThumbnailManifest"` → **20/20 passed** (only non-gating React `act()` console warnings remain). Full suite is green too (84 suites / 559 passed + 1 skipped). Whatever fixed `_resetCache` export landed cleanly.
- [ ] **NEW 2026-08-01 — stale doc `docs/BINDING_CONTRACT.md` (`config.y=delta_time`) is the #1038 target.** Engine truth is `config=[time,rippleCount,resW,resH]` (`src/renderer/UniformBuffer.ts:17` `config_rippleCount:4`), but `agents/WGSL_BUILTINS_GENERATIVE.md:31` still says `.y = delta_time`. Being fixed by **today's kimi-cli focus (#1038, section A)**.
- [ ] **#983/#984 — promote WGSL authoring gate to blocking (finish #858 phase 2).** Still open. `workgroup_warnings` print `[ERROR]` but don't fail the gate; orphan `likely-broken` classification doesn't fail CI. `scripts/` + `.github/` + `reports/` + one `gen-showcase-nebula-core.wgsl` fix only. **Decoupled from both of today's tracks** — a good future Copilot candidate, but NOT this week's B (kept off `wgsl_precommit_gate.py` so it can't collide with kimi's new audit scripts).
- [ ] **#976 — Shadertoy/GLSL importer productionization + generative attract-mode + audio→param binding.** Content-facing; sequenced after #966/#967 (now done). Product track.
- [ ] **#858 (WGSL gate: workgroup arg-count + orphan auditor) CLOSED-completed 2026-06-27.** Outputs verified present: `scripts/wgsl_precommit_gate.py` (has the `@workgroup_size` arg-count check), `scripts/audit_orphan_shader_defs.py`, `reports/orphan_shader_defs.{md,json}`. Both new checks landed as **non-blocking `[WARN]`** per the issue's decision. **Today's decoupled Copilot track (section B) = the promote-to-blocking follow-on** (fix `gen-showcase-nebula-core`'s 2-arg `@workgroup_size`, flip the workgroup check to `sys.exit(1)` after grace, make `likely-broken` orphan classifications fail CI). `scripts/`+`.github/`+`reports/`-only — zero overlap with the frontend track. **CORRECTED 2026-07-18 (verified in code + full-tree scan; supersedes earlier notes on this line):** the promote-to-blocking is essentially already DONE. 2-arg `@workgroup_size` already hard-fails (`split_workgroup_issues`: `arg_count==2`→blocking); the orphan `likely-broken`/`parse-error` classification already fails CI forward-only (`audit_orphan_shader_defs.py --base origin/main` at `ci.yml:219`); `gen-showcase-nebula-core` is already `@workgroup_size(16,16,1)` and pinned by `test_gen_showcase_nebula_core_if_present`. Full-tree scan = **0 one-arg, 0 two-arg** across 1304 shaders; orphan report = 0 likely-broken/0 orphan. The only remaining soft class is **1-arg override forms, which are VALID WGSL** (naga accepts; Z defaults to 1) — banning them outright is wrong. **Real residual hygiene** (the corrected Copilot track): (1) fix the `split_workgroup_issues` docstring↔code contradiction + add a regression test locking 2-arg→blocking/1-arg→warning; (2) run the cheap regex convention scan full-tree per-PR (sub-second) to close the forward-only coverage gap; (3) remove the committed stale `reports/orphan_shader_defs.json` (already a CI artifact) or add a drift check; (4) fix the misleading `[ERROR]` label + no-op `--fix` hint on 1-arg warning output; (5) NEW higher-value check (per Grok): after naga validate, assert workgroup dims against portable minimum limits (invocations≤256, X/Y≤256, Z≤64) — a real correctness gate, unlike the style rule. Grace-window/allowlist machinery is unnecessary (zero backlog).
- [ ] **WASM/C++ renderer thread is now ACTIONABLE (Tier-B decision resolved).** Per `MEMORY.md`, the #850-equivalent product call was made 2026-06-27: **WASM = experimental opt-in, TS WebGPU = production default** (policy in `WASM_BACKEND_POLICY.md`). #845–849 closed; a fresh epic **#885** (umbrella) + sub-issues **#886** (recording bypasses internal capture → blank WebM under WASM), **#887** (RendererManager forwarding / `instanceof`+`as any` audit), **#888** (FFT bins / GPU timings / slot-state parity), **#889** (Playwright `?renderer=wasm` smoke + bench), **#890** (docs refresh + Tier-A promotion tracking) are open (all 2026-07-02). These touch `src/App.tsx` + `src/renderer/` + `wasm_renderer/` + C++ — **still NOT owned by this weekly routine** (that's a Copilot/Jules/Noah C++ thread), but it is no longer decision-blocked. Flagged so the kimi/Copilot src-vs-scripts split stays aware of the App.tsx/renderer blast radius.
- [ ] ~~Storage Manager: verify `/api/images` and `/api/videos` streaming endpoints under load (thread-pool saturation, GCS token refresh edge cases)~~ → **CLOSED 2026-06-13** (see Done): hardened via PR #809 — signed-URL redirect + proxy fallback (`fd7bf09`), review fixes `12b2407`. Follow-up if pain persists: dedicated load test under concurrent token-refresh.
- [ ] ~~Confirm CORS allowlist (`https://test1.1ink.us`) shipped to the VPS~~ → **RESOLVED 2026-06-15** (hygiene pass): the real frontend origin is `https://test.1ink.us` (with a dot), confirmed by `storage_manager/app.py:896` `--base-url=https://test.1ink.us/image_video_effects` — and it **is** in `ALLOWED_ORIGINS` (app.py:412-417, alongside `go.1ink.us`, `noahcohn.com`, `localhost:3000`). `git log -S "test1.1ink.us" -- storage_manager/app.py` returns empty → `test1.1ink.us` was a **typo** in prior notes; it never existed in the backend. CORS is correct. (Live VPS confirmation still only verifiable from the deployed host.)
- [x] ~~`tsc --noEmit` RED — `WASMRenderer.input.test.ts` GPUCanvasContext error~~ → **FIXED 2026-07-11**: line 59 now casts `as unknown as GPUCanvasContext`. Typecheck gate clean on that line. (Likely folded into PR #936's WASM-parity work.)
- [ ] **Orphan/missing-WGSL count drifted 37 → 39** (hygiene pass 2026-06-27 `sync:shaders:dry`): `gen-luminescent-quantum-void-anglerfish` pending upload + 39 defs referencing absent local WGSL. The +2 are this week's merged generative shaders. Still folded into #858 Phase 2 (orphan auditor).
- [ ] **`npm ci` cannot complete clean-room** (hygiene pass 2026-06-27): `sharp` postinstall downloads its libvips binary from a GitHub release, which is blocked behind the agent proxy ("no credentials") → whole install aborts, `typescript` missing. `npm ci --ignore-scripts` works around it. Risk: any fresh/proxied CI env without a cached `sharp` binary fails `npm ci`. Triage: pin a sharp version whose binary is registry-reachable, vendor it, or demote to optional/devDependency if it's build-time-only.
- [x] ~~**2.7 MB gzipped `main.js`** (CRA "significantly larger than recommended" warning, hygiene pass 2026-06-27).~~ → **RESOLVED, verified 2026-08-24 (hygiene pass).** `main.js` is now **272.5 kB gzipped** (1.0 MB raw) — code-splitting already landed since this note was written: `@xenova/transformers` (179.9 kB gz) and `@mlc-ai/web-llm` (2.14 MB gz, as `web-llm.*.chunk.js`) are both separate lazy chunks, plus `hls.js` (159.6 kB gz). CRA's bundle-size warning is now about the *sum across all chunks*, not `main.js` itself — no further code-split work needed.
- [ ] **npm prod-dependency vulns, RECONFIRMED + shifted 2026-08-24 (hygiene pass): now 4 high / 0 critical / 0 moderate** (was 5: 1 critical/3 high/1 moderate, hygiene audit 2026-06-15) — still entirely the `@xenova/transformers` (→ `@huggingface/transformers`) depth-estimation chain, but the specific CVEs changed: `adm-zip` (via `onnxruntime-node`, crafted-ZIP 4GB-allocation DoS) and `sharp` (libvips CVE-2026-33327/33328/35590/35591) — the old `protobufjs`/`onnxruntime-web`/`onnx-proto` critical/moderate entries are gone (upstream dep tree changed), no fix available for the new ones either (`npm audit --production --json` confirms `fixAvailable: false` on all 4). Not auto-fixed. Triage: upgrade/vendor path vs. risk, same as before.
- [ ] **1 shader workgroup-size warning: `gen-showcase-nebula-core`** uses `@workgroup_size(8, 8)` (expected `8x8x1` / `16x16x1`) — surfaced by `generate_shader_lists.js` during build 2026-06-15. Non-blocking (build compiles). **Now tracked by open issue #858** (WGSL gate arg-count `@workgroup_size` check, scripts/-only) — this week's decoupled Copilot/expansion track candidate. Still present on main 2026-06-27.
- [ ] **WASM/C++ renderer backend — open roadmap thread (issues #845–#850, opened 2026-06-18).** New emergent focus area: a working-but-incomplete C++ renderer compiled to WASM via Emscripten + emdawnwebgpu. #850 is a *decision* issue (invest in full production support vs. experimental toggle vs. native-app split). #845–847 = RendererManager/input/interface-method parity (touches `src/` + `wasm_renderer/renderer.cpp`); #848 = Emscripten CI/build reliability; #849 = WASM testing/visual-parity/benchmark. **Not owned by this routine** — needs Noah's roadmap call on #850 first. Flagged so future runs and the kimi/Copilot src-vs-scripts split stay aware of the `wasm_renderer/` blast radius.
- [ ] ~~`sync-images` / `sync-videos` admin endpoints need a dry-run mode~~ → **CLOSED 2026-06-06**: superseded by the two-phase **plan/apply intent flow** (`POST /api/admin/sync-images/plan` creates a no-write intent = dry-run; `/apply` executes). Old single-shot `POST /api/admin/sync-images` now returns a "gone" pointer to plan/apply.
- [ ] ~~Bind-group compatibility report triage pass~~ → **CLOSED 2026-06-06** (see Done): 966/971 compatible, 15 auto-fixed (naga-OK), 4 deferred to human redesign, 1 false positive.
- [ ] **4 bind-group layout-conflict shaders need human redesign** (deferred by the triage swarm — not auto-fixable): `plasma` (binding 12 uses `array<PlasmaBall,50>` instead of `array<vec4<f32>>`), `tone-histogram` + `tone-histogram-apply` (binding 10 `array<atomic<u32>>` histogram atomics need architectural fix), `deep-workgroup-multi-effect-blend` (`workgroup_size(16,16,4)` w/ `shared_tile[16][16][4]` — can't shrink wg without redesign).
- [x] ~~Add `_hash_library` to `TEMPLATE_FILES` in `scripts/bindgroup_checker.py`~~ → **CLOSED 2026-06-20** (verified): `_hash_library.wgsl` is now line 47 of `TEMPLATE_FILES` — recurring false positive eliminated. Landed alongside last week's WGSL authoring-gate work.
- [ ] **37 shader definitions reference missing local WGSL files** (surfaced by sync dry-run 2026-05-30) — triage whether these are intentional storage-only entries or broken refs. **Now folded into open issue #858** (Phase 2: offline orphan-definition auditor `scripts/audit_orphan_shader_defs.py`, classifies `storage-only` vs `likely-broken`).
- [ ] ~~Test runner broken: `typescript` missing from `node_modules`~~ → **CLOSED 2026-05-30**: `npm ci` restores it; CI already enforces `npm ci` before test via `ci.yml`.
- [ ] ~~`layerChain.spec.ts` — add a CI step to enforce `npm ci` before test~~ → **CLOSED 2026-05-30**: `ci.yml` already has `npm ci` + explicit TypeScript resolution gate before `npm test`.

## Done
<!--
Completed items, routine archives here with date.
Prune occasionally when this gets long.
-->
- 2026-08-29: **Stop the second WebGPU devices SHIPPED + MERGED + CLOSED — #1126** (last week's 2026-08-22 focus, kimi-cli). Verified on `main` @ `a27a627` this run: NEW `src/utils/adoptedGpuDevice.ts` (the device registry) landed and `src/utils/requestPixelocityDevice.ts` was **deleted** outright rather than aligned — the stronger of the two options the plan allowed; `RendererManager.getDevice(): GPUDevice | null` at `:305` with adopt/release on backend switch, facade **353 LOC** (+5 over the ≤350 target for the accessor + adopt hooks — accepted); `ShaderScanner.tsx`/`ShaderValidator.tsx` now adopt the registry device and idle behind `WebGpuProbeFailureOverlay` when the probe failed; `src/services/depthEstimation/loader.ts:28` carries the enforced contract comment "Never call requestAdapter() here — the renderer owns the sole adapter/device" and gates on `window.webgpuProbe`, with `rendererDeviceActive` threaded through `useDepthEstimation.ts`. **Acceptance proven by grep:** a full `src/**` scan for `requestAdapter|requestDevice` outside tests returns **zero** live call sites beyond `webgpuBootProbe.ts` (the sole owner) and `webgpuDevicePolicy.ts` (the ladder it calls) — every other hit is a `failedStage` string literal, a comment, or a type. Per `.swarm-state.md` closeout: `tsc` clean, **84 suites / 550 passed / 1 skipped** (+3 suites / +5 tests), `SKIP_WASM_BUILD=1 npm run build` compiled, `verify:dependency-boundaries` green. This completes the `#1107 → #1126` arc: one `GPUDevice` from the boot probe, enforced across the entire UI, so `?renderer=wasm` no longer dies on Windows Chrome "No available adapters" and nothing compiles against a different feature/limit set than the live renderer. **Also cleared this run:** the 2026-08-24 hygiene pass's RED `audit:dead-sliders` CI gate is now GREEN on `main` (0 new / 28 known, exit 0). **Backlog shift:** Noah ran a fresh 2026-08-26 progress audit — closed the whole 08-21 set (#1123–#1129) and opened #1179–#1185.
- 2026-08-22: **WebGPU hard-fail boot probe SHIPPED + MERGED + CLOSED — #1107** (last week's 2026-08-15 focus, kimi-cli). Noah closed #1107 as completed 2026-08-15. Verified on `main` this run: `src/renderer/webgpuBootProbe.ts` (12.3KB) + `webgpuBootProbe.test.ts`, `src/components/WebGpuProbeFailureOverlay.tsx`/`.css`/`.test.tsx`, `docs/WEBGPU_BOOT_PROBE.md`, `RendererInitOptions`/`WebGpuProbeHandoff` re-exports on `backendLifecycle.ts`, `RendererDiagnostics.webgpuProbe` field — all landed via commit `b03ac5a`. Acceptance met: failed probe → canvas-slot overlay + full attempt log; no WebGL / no auto Canvas2D fallback (explicit `?renderer=js` only); Chrome/Edge delta legible from `window.webgpuProbe` (`userAgentBrands` + attempts); success path = single `requestDevice` via handoff. `.swarm-state.md` iter-2 closeout: `tsc` clean, **81 suites / 535 passed / 1 skipped**, `SKIP_WASM_BUILD=1 npm run build` compiled, `verify:dependency-boundaries` green, `RendererManager.ts` 348 LOC. Established the gpu-chores "one `GPUDevice` from the boot probe" policy that #1126 now enforces across the rest of the UI. Real-GPU visual QA of the overlay remains workstation-side (Cloud VM has no adapter). **Backlog shift:** Noah ran a fresh 2026-08-21 progress audit — closed the old epic #1076 + children, opened the new live set #1123–#1129.
- 2026-08-15: **Residual god-module strangler SHIPPED + MERGED — #1079** (last week's 2026-08-08 focus, kimi-cli). Verified on `main` this run: commit `ffb8eef` "refactor: strangler RendererManager + StoragePanel into tested seam modules (#1079)" via PR #1092 (branch even with `origin/main`, 0/0). `src/renderer/RendererManager.ts` **344 LOC** facade (≤350 target), `src/components/storage/StoragePanel.tsx` **200 LOC** panel-composed (≤400 target); NEW seams `backendLifecycle.ts`/`slotOrchestration.ts`/`inputSourceBridge.ts`/`performanceStatus.ts` (+ `rendererDiagnostics`/`rendererTypes`) with colocated Jest; `src/components/storage/panels/*` (`StorageListBrowser`/`StorageDetailPanel`/`StorageRatings`/`StorageUploadPanel`/`OperationsPanel`), Library tab now via `StorageClient.listLibrary()`. Contracts preserved: no WASM auto-fallback, feedback order untouched, all public `IRenderer`/app-API methods retained. Per `.swarm-state.md`: `tsc` clean, **76 suites / 506 passed / 1 skipped** (+5 suites/+20 tests over baseline), `SKIP_WASM_BUILD=1 npm run build` compiled, `verify:dependency-boundaries` green. A content-swarm multiplier — the two remaining god modules that taxed every new backend/shader feature are now thin, tested seams. **Backlog shift:** Noah opened two fresh foundation issues 2026-08-14 (#1107 boot probe + #1106 gpu-chores) that become the newest live signal.
- 2026-08-08: **Uniforms Single-Source-of-Truth SHIPPED + MERGED — #1038** (last week's 2026-08-01 focus, kimi-cli). Killed the `config.y = delta_time` folklore. Verified on `main` this run: `src/contracts/uniforms_layout.json` present (5.8KB); `docs/BINDING_CONTRACT.md` now carries the authoritative Uniforms table (`config.y = rippleCount`, `:61/73/91`, plus explicit "not delta time / not audio / not a click count" callouts); `agents/WGSL_BUILTINS_GENERATIVE.md:31` corrected to `.y = rippleCount`; `npm run verify:uniforms` + `npm run audit:config-y` scripts landed (machine-checkable drift check vs `UNIFORM_OFFSETS`). Commits `639cd31` ("docs(contract): make Uniforms struct a verified source of truth", #1055) + `67a8eb7` ("Claude/uniforms docs audit", #1056). A content-swarm multiplier — recon agents can no longer trust a stale preamble over the engine. **Backlog shift:** the #1038–#1044 set is superseded by Noah's fresh 2026-08-07 progress audit (epic #1076 + #1077–#1084).
- 2026-08-01: **Generative `updatedParams` campaign + audit tooling SHIPPED + MERGED — #1011 CLOSED** (last week's 2026-07-25 focus, kimi-cli swarm; Noah closed the issue 2026-07-26). TOOLING: `scripts/audit_extrabuffer.py` + `scripts/audit_dead_sliders.py` (+ `test_audit_*.py`), `npm run audit:extrabuffer`/`audit:dead-sliders`/`audit:generative`, both wired as **blocking** CI gates in `wgsl-precommit-gate`; `wgsl_precommit_gate.py` baseline-aware extraBuffer scan on changed files; guardrail callouts added to `scripts/AUTHORING.md` + `agents/WGSL_BUILTINS_GENERATIVE.md`; `scripts/batch_finalize_generative_pool.py` + `finalize_updated_params.py`. CONTENT: batches 15–19 (`1cf468c`/`7772bb3`/`77b5324`/`818a21b`/`b919bda`), plus a 40-def batch-finalize pass — empty-`updatedParams` generative pool now **0** (`gen_capabilities` system-monitor excluded by design; `gen_grid` upgraded 241→307). CATALOG: `src/utils/audioReactiveBadge.ts` + ShaderBrowser badge. Verified per `.swarm-state.md`: `audit_extrabuffer`/`audit_dead_sliders` PASS, gate green, Jest up to **~397–403 (59–61 suites)**. Stragglers logged to Backlog (46-file legacy extraBuffer writes, CommunityGallery Jest uncertainty, stale BINDING_CONTRACT → now #1038).
- 2026-07-25: **Community gallery UI wiring SHIPPED + MERGED + confirmed LIVE** (last week's 07-18 focus — reconciled this run). `CommunityGallery` mounted in `src/components/controls/panels/AiVjStudioPanel.tsx` (import `:8`, render `:589`), fed by `getCurrentChain` added to `src/hooks/useShareChain.ts`; `AppShell.tsx` threads it down. Per `.swarm-state.md`: 31 suites / 271 Jest, 95 pytest, play-count sort + first-slot thumbnails + optimistic publish + empty/error states all done. Closes the last user-written Idea → Ideas list now exhausted.
- 2026-07-25: **Reconciled the 07-19 → 07-22 work that landed outside the weekly loop.** Foundation Wave 2 (#965–970): `App.tsx` 2,207→562 LOC (#966), WebGPU modularization `WebGPUDeviceInit`/`ResourceManager`/`ShaderManager`/`Timing` (#967), `docs/BINDING_CONTRACT.md` + binding-13 C++ parity (#968/#969), `multipassGraph.ts` + `GraphRunner.ts` wired in `dispatchSlot` (#970), thumbnail pipeline `npm run thumbs:generate` at 26.6% (#921). Six generative upgrade swarm batches (9–14, ~150 shaders → `updatedParams` from existing params, technique upgrades, feedback-bug fixes; Jest up to 339). Mouse-Y convention fix (~70 WGSL files, `scripts/fix_mouse_y_compensation.py`). Live rating API corrected to JSON `{rating}` (`postShaderRating`). WASM reaffirmed **Tier B** 2026-07-19.
- 2026-07-18: **Community preset-pack gallery vertical slice SHIPPED + MERGED to `main`** (last week's whole-stack focus, kimi-cli swarm). BACKEND `storage_manager/app.py`: `preset_pack` STORAGE_MAP entry + Pydantic models (`PresetPackPublishPayload`/`PresetPackMeta`) + `_decode_shared_chain` validator + `POST /api/preset-packs` (publish, rejects undecodable chains), `GET /api/preset-packs` (`?limit`/`?offset`), `GET /api/preset-packs/{id}`, `POST /api/preset-packs/{id}/play`; NEW `storage_manager/tests/test_preset_packs.py` (22 tests incl. round-trip `test_published_chain_decodes_to_original`). FRONTEND `src/services/communityGallery.ts` (`publishPack`/`listCommunityPacks`/`getCommunityPack`/`recordPresetPackPlay`, reuses `STORAGE_API_URL` + `encodeChain`/`decodeChain`) + `communityGallery.test.ts` (8, incl. round-trip); `src/components/CommunityGallery.tsx` (live-fetch list + inline publish form, mirrors `PresetPackGallery`) + `CommunityGallery.test.tsx` (8). Verified per `.swarm-state.md`: backend `pytest` **94 passed**, `tsc --noEmit` clean, `react-scripts test` **27 suites / 241 tests passed**. **Remaining slice (today):** component is unmounted — UI wiring was deferred behind #936/#938, both now merged.
- 2026-07-18: **PR #936 (Web-MIDI live-control + WASM API parity) MERGED to `main`** via merge PR #957 (`0069851`). Closes last week's "review + merge #936" action. `controlBindings.ts` + `midiControl.ts` + Controls learn/bind UI now on `main`.
- 2026-07-18: **PR #938 (foundation epic #912 — App/Controls strangler refactor, Wave 3) MERGED to `main`** (`f150e13` "split four 1000+ line source files under 700 lines"). `App.tsx`/`Controls.tsx` decomposed into `src/hooks/*` + `src/components/controls/panels/*` (`ControlsContainer`, `ParamSlidersPanel`, `SlotStackPanel`, `useShareChain`, …). Retires the "must NOT touch App/Controls" boundary that had gated frontend work for three weeks.
- 2026-07-11: Variation Explorer breed/favorites **MERGED to `main`** (`94b0619` "push fix" is now an ancestor of `origin/main`). `src/services/variationFavorites.ts` + `breedVariations` in `variationExplorer.ts` confirmed present on `origin/main` this audit. Closes last week's "92-ahead unmerged branch" backlog risk — the working history landed. (The routine's designated branch this week is a fresh `claude/charming-lamport-1e4x50` cut from current `main`, 0 ahead / 0 behind.)
- 2026-07-11: Web-MIDI + keyboard live-control surface COMPLETE (last week's focus) — in **open PR #936** `feat/midi-control-and-wasm-parity`. `src/services/controlBindings.ts` + `src/services/midiControl.ts` (Web MIDI API, no new deps) + unit tests + Controls learn/bind UI; PR also folds in RendererManager/WASM API parity (`GPUTimings`/`timingSource`, `getAudioData`, `isRecording`) + a WASM smoke harness. **217 tests pass** per the PR. Pending Noah's review/merge (the work exists + is green; it just needs landing).
- 2026-07-11: `tsc --noEmit` WASM-test error RESOLVED — `src/__tests__/WASMRenderer.input.test.ts:59` now casts `as unknown as GPUCanvasContext` (was the `CanvasRenderingContext2D`/GPUCanvasContext overload mismatch). The typecheck gate is no longer red on that line. Removes last week's designated Claude Code hygiene fix.
- 2026-07-04: Variation Explorer "evolve / breed" + favorites persistence COMPLETE (last week's kimi-cli focus — verified green on the working branch this audit; merged to `main` 2026-07-11 per entry above). Per `.swarm-state.md` 4-iteration log + present files: `src/services/variationExplorer.ts` gained `breedVariations(parentA, parentB, count, catalog, options)` — pure seedable mulberry32 crossover (slot count = min(A,B,MAX_SHARED_SLOTS); 50/50 per-slot shader pick; per-param linear blend when both parents share the shader; optional 25% same-category mutation; children built through `buildSharedChain` so they round-trip `expandSharedChain`/`applySharedChain`). NEW `src/services/variationFavorites.ts` (localStorage CRUD modeled on `vjHistory.ts`/`vjPresets.ts`: `VariationFavorite`, save/load/delete/clear, `MAX_FAVORITES=50`, `generateId` with `crypto.randomUUID`+jsdom fallback). `VariationGrid.tsx` extended: editable Seed input, Shuffle (fresh seed), Reseed-from-chain, per-card star→favorite, "Breed selected" (enabled on exactly two A/B-selected cells), favorites strip with Load/delete. New tests: 7 breed + 6 favorites + 4 grid. Verified: `react-scripts test` **21 suites / 173 tests PASS** (baseline 17/140); `npx tsc --noEmit` clean except the pre-existing `WASMRenderer.input.test.ts` GPUCanvasContext error (WASM area, unrelated); `react-scripts build` compiled. Committed on branch `claude/charming-lamport-thm8fq` (`94b0619`). **Action for Noah: run the Jules wrap-up (section F) to land it on `main`.**
- 2026-06-20: Multi-slot chain "remix" / variation explorer SHIPPED (kimi-cli swarm, last week's focus — verified green + merged to `main` this audit). `src/services/variationExplorer.ts` (`generateChainVariations` + deterministic mulberry32 seedable PRNG, reuses `randomizeParams`/`buildSharedChain`/`buildCatalogDefaultsLookup`/`mapOrderedParamsToSlotParams`/`shaderCatalog`) + `variationExplorer.test.ts`; `src/components/VariationGrid.tsx` (6-cell A/B grid, param summary, Adopt routed through `onApplySharedChain`→`applySharedChain`, shuffle) + `VariationGrid.test.tsx`; "Remix Chain" button in `Controls.tsx`. Verified: `npx tsc --noEmit` clean, `react-scripts test` 17 suites / 140 tests PASS, prod build compiled. Commits `4c8a4d2` + `8f4a06a` confirmed ancestors of `origin/main`. Known unrelated failure: `wasm_renderer/build.sh` `--use-port=emdawnwebgpu` (pre-existing toolchain, issue #848).
- 2026-06-20: WGSL authoring scaffold + pre-commit canonical-binding gate SHIPPED (kimi-cli swarm — verified on main this audit). `scripts/new_shader.py` (canonical-binding scaffolder), `scripts/wgsl_precommit_gate.py` (changed-file naga + bindgroup check), `public/shaders/_template_canonical_compute.wgsl`, docs `scripts/AUTHORING.md` + `scripts/NAGA_README.md`. Wired into CI at `.github/workflows/ci.yml:191` (`python3 scripts/wgsl_precommit_gate.py --base origin/main`, with naga-cli install step). Mid-week Jules hardening (commit 44e311a, 2026-06-15): gate now locates naga via `shutil.which` (fixes CI Permission-Denied), Playwright e2e timeouts bumped to 120s, stale `harborglow` submodule uncached. Shift-left authoring path is live — reactive fix swarms should taper.
- 2026-06-20: AI VJ → shareable chain bridge SHIPPED (Copilot track — verified on main this audit). `src/services/vjToSharedChain.ts` (`mapVJStackToSharedChain`, `buildCatalogDefaultsLookup`) bridges a generated VJ stack to the share-link encode path; wired into `src/App.tsx` + `src/AutoDJ.ts`. Closes the 2026-06-13 Copilot-track idea.
- 2026-06-13: Multi-slot layer-chain preset packs + shareable URLs SHIPPED (kimi-cli swarm, commit f5edd00). `src/services/layerChainShare.ts` — versioned (`SHARED_CHAIN_VERSION=1`, `MAX_SHARED_SLOTS=6`) URL-safe encode/decode with default-param diffing on encode + re-expansion on decode (`buildSharedChain`/`expandSharedChain`/`encodeChain`/`decodeChain`, per-shader defaults lookup). `PresetPackGallery.tsx` + curated `public/preset_packs.json`. App.tsx hydrates the chain from a `?chain=` link on first load, `applySharedChain` restores modes+params (skips unknown shader ids), copy-share-link, and a share modal (Twitter/TikTok). Controls.tsx "🔗 Copy Chain Share Link" + gallery wiring. Verified 21/21 tests PASS via `react-scripts test` (`layerChainShare.test.ts` 16 + `PresetPackGallery.test.tsx` 5).
- 2026-06-13: Storage Manager image/video endpoint hardening SHIPPED (Copilot track, PR #809 — `fd7bf09` feat: signed-URL redirect + proxy fallback for `/api/images` & `/api/videos`; `12b2407` review fixes: public-constant rename, logging, type annotation, test assertions). Closes last week's backend streaming-under-load backlog item.
- 2026-06-06: Bind-group compatibility triage + auto-repair pass COMPLETE (kimi-cli swarm). Fresh checker run found 20 incompatible (the May-9 report was stale); 15 auto-fixed and all naga-OK via `scripts/fix_bindgroups.py` (+binding12 stubs, workgroup `(16,16)`→`(16,16,1)`, `custom_params`→`ripples`, +missing bindings 4–12). 4 deferred to human redesign (plasma, tone-histogram, tone-histogram-apply, deep-workgroup-multi-effect-blend — now backlog). 1 false positive (`_hash_library`). Final: 966 compatible / 5 incompatible / 3 templates / 971 total. Artifacts: `reports/bindgroup_{triage,fix_summary,fix_queue}.{md,json}`, `binding_fix_review.json`; scripts `triage_bindgroup_report.js`, `bindgroup_checker.py`, `fix_bindgroups.py`. Full log in `.swarm-state.md`.
- 2026-05-30: AI VJ "auto-transition" / beat-aware sequencer SHIPPED (commits 812b5ab feat + 60853dd fix). Services `beatDetector.ts` (adaptive bass-energy threshold, refractory debounce), `transitionOrchestrator.ts` (IDLE/WAITING/TRANSITIONING state machine, timer + beat sources, eased param lerp), `audioGraph.ts` (shared AudioContext, lowpass-isolated kick band), `utils/transitionMath.ts` — all with unit tests. `AutoTransitionConfig` + `startAutoTransition`/`stopAutoTransition`/`triggerNextTransition`/`buildTransitionTarget` on Alucinate; Auto-Transition panel in Controls.tsx (timer/beat, interval, duration, randomize/cyclePresets). Confirmed via repo audit 2026-06-06.
- 2026-05-30: Hardcoded SFTP password removed from `scripts/deploy.py` (line 251) and `scripts/deploy_app_only.py` (line 38). Both now read `DEPLOY_PASS` env var; `deploy.py` falls back to `getpass.getpass()` interactive prompt; `deploy_app_only.py` already had that fallback. **Rotate the DreamHost credential** — it was committed in plaintext to a public repo.
- 2026-05-30: Pipeline hygiene pass (Claude Code E task) — all 6 stages green: `npm ci` restored typescript@4.9.5; `npm run build` clean (963 shaders, Compiled successfully); `layerChain.spec.ts` 25/25 PASS; sync dry-run (34 to upload, no writes); deploy scripts verified correct; storage manager AST-clean. Closed two stale backlog items (test runner / CI enforcement — both already solved in ci.yml).
- 2026-05-23: Per-shader param presets + AI VJ randomizer — `vjPresets.ts` (VJPreset interface; savePreset/loadPresets/deletePreset, localStorage, max 50; `randomizeParams(shaderIds, catalog)` samples uniform [min,max], snaps to step, clamps overshoot), `randomizeActiveParams()` public method on `Alucinate` (`src/AutoDJ.ts`), and Controls.tsx UI: Randomize Params + Randomize Slot + Randomize All Slots buttons and collapsible Presets panel (save named / restore / delete). tsc PASS. Confirmed via repo audit 2026-05-30 (commits 1969be8, be1ccfa).
- 2026-05-23: AI VJ stack persistence + history panel — vjHistory.ts service (save/load/clear/removeEntry, max 20 entries, localStorage), saveVJStack wired into AutoDJ.generateFromVibe, collapsible "VJ History" panel in Controls.tsx with per-entry Restore (exact stack+params) and Regen (re-fires generateFromVibe with same vibe string) buttons, Clear History button.
- 2026-05-16: WGSL runtime-error fix pass — all 49 critical shaders in `reports/runtime_errors_report.json` fixed and validated via naga. Patterns: bulk canonical binding renames (videoSampler→u_sampler, outTex→writeTexture, etc.) resolved the majority; false-positive invalid_binding flags cleared for 8 multi-pass shaders; array_bounds ripple clamps added where needed. 49/49 naga OK per `.swarm-state.md`.
- 2026-05-09: Shader metadata normalization — `shaderCatalog.ts` service built (merges 15 category JSONs + shader_params_extracted.json, in-memory full-text index, module-level cache), wired into `ShaderBrowserWithRatings` (search) and `AutoDJ.buildShaderManifest()` (AI VJ path). Single source of truth confirmed in code audit.
- 2026-05-02: AI VJ Mode — Alucinate full-library manifest (all 15 categories, 918 shaders), LLM param suggestion (`selectShadersFromLLM` returns `{id, params}[]`), `onUpdateParams` callback, `generateFromVibe()` one-shot method, vibe-prompt text input + Generate button in Controls.tsx (kimi-cli swarm, confirmed via `.swarm-state.md` + code audit)
- 2026-04-18: Storage Manager CORS fix for `test1.1ink.us`; image/video streaming endpoints; sync-images/sync-videos admin endpoints (archived from prior notes)
- 2026-04-18: Obsidian Echo-Chamber generative shader merged (#536)
- 2026-04-18: Hyper-Refractive Rain-Matrix generative shader merged (#534)
- 2026-04-18: Bioluminescent Aether Pulsar generative shader merged
- 2026-04-18: WebGPU renderer polish — 405 play-count POST fix, bitonic-sort built-ins, sine-wave UnfilterableFloat (#532)
- 2026-04-18: Startup race, event leaks, rating cache stability fixes (#530)

## Last run
<!-- Routine writes summary here each run. Overwrites previous. -->
Date: 2026-08-15
Mode: User Idea (Noah's freshest in-context signal is the two 2026-08-14 foundation/architecture issues #1107 + #1106, which supersede the 08-07 Ideas items as the live source — consistent with prior runs treating Noah's newest authored issues as the live Ideas source. Picked #1107 = hard-fail WebGPU boot probe: tighter than #1106, fully headless-self-verifiable, and a direct continuation of last week's #1079 `backendLifecycle` seam. Foundation healthy → not Fix First.)
Focus: **#1107 WebGPU required — hard-fail boot probe.** Fatalize adapter/device ladder exhaustion (`ADAPTER_ATTEMPT_LADDER`/`requestAdapterWithFallback` in `src/renderer/webgpuDevicePolicy.ts`, consumed at `src/renderer/webgpu/device.ts:145`) instead of silently absorbing it via backend switching; publish structured `window.webgpuProbe` breadcrumbs (per-attempt powerPref/adapter info/limits/error + Chrome-vs-Edge brand); render a blocking overlay in the canvas slot only (gallery/storage stay visible); **no WebGL fallback anywhere**. kimi-cli threads the fatal up through `backendLifecycle.ts` → `RendererManager` facade (keep ≤~350 LOC). Verifiable via `tsc`/Jest (mock `navigator.gpu`)/`SKIP_WASM_BUILD=1 npm run build`/`verify:dependency-boundaries` — no GPU needed.
Outcome: pending. (Reconciliation this run: **last week's #1079 focus SHIPPED + MERGED** — commit `ffb8eef` via PR #1092; `RendererManager.ts` 344 LOC + `StoragePanel.tsx` 200 LOC on `main`; seams + `storage/panels/*` present; `.swarm-state.md` 76 suites / 506 passed, build compiled, `verify:dependency-boundaries` green. Designated branch `claude/charming-lamport-9p1q7r` even with `origin/main` (0/0). **Backlog reality shift:** Noah opened #1107 (boot probe) + #1106 (gpu-chores) on 2026-08-14 — newest live signal; #1107 picked, #1106 appended to Ideas as runner-up. **Tracks:** A=kimi #1107 (`src/renderer/webgpuDevicePolicy.ts` + `backendLifecycle.ts` + `RendererManager` facade + NEW probe seam + NEW `src/components/renderer/WebGpuFailureOverlay.tsx` + tests — does NOT touch `webgpu/**` engine internals beyond the one `device.ts` call site, `wasm_renderer/**`, shaders, scripts, backend, or the just-landed `storage/**` split; **no WebGL branch**); B=Copilot #1078 Wave A (thumbnail coverage-*regression* CI gate — `scripts/`/`.github/workflows/`/baseline JSON only, fully decoupled from A's `src/` work). **Fix First not triggered:** #1079 landed clean on `main`; production path healthy. **Context gaps:** `recent_chats`/`conversation_search` UNAVAILABLE (headless scheduled run) — context reconstructed from repo tree, `weekly_plan.md`, `.swarm-state.md`, and GitHub issues #1076–#1108 + open PR list (only draft PR #1105, a Jules generative shader, open). **Jest count carried from `.swarm-state.md`** (`node_modules` absent on-VM, `npm ci` blocked by `sharp` postinstall) — kimi re-establishes the baseline on iteration 0. **Content stream running in parallel** (Jules/generative swarm — recent merges #1085–#1108, open draft PR #1105) — decoupled from today's renderer focus; `public/shaders/**` + `shader_definitions/**` churning, kept out of scope. **Copilot track B pivoted:** thumbnail *discoverability* is already built (`ShaderThumbPlaceholder.tsx`, `thumbnailPlaceholder.ts`, `thumbnail-coverage-status.yml`), so B targets the genuinely-missing piece — a coverage-*regression* gate.)
Date: 2026-08-22
Mode: User Idea (Noah's freshest in-context signal is the 2026-08-21 progress audit — he closed the old epic #1076 + children and opened a new live set #1123–#1129; per standing precedent that set is the live Ideas source, superseding the 08-14 items. Picked #1126 = stop second WebGPU devices: the only labeled `bug` in the set, foundation-tagged, fully headless-self-verifiable, and a direct continuation of #1107's "one `GPUDevice` from the boot probe" discipline. Foundation healthy → not Fix First.)
Focus: **#1126 — stop the second WebGPU devices.** Make `src/components/ShaderValidator.tsx` (`:95-98` per-shader `requestAdapter()`+`requestDevice()`), `src/components/ShaderScanner.tsx` (`:100/161/167` support-ping + second adapter + `requestPixelocityDevice()`), and `src/services/depthEstimation/loader.ts` (`:28-29` `requestAdapter()` availability probe) **adopt the single renderer `GPUDevice`** via `window.webgpuProbe.handoff` / a new `RendererManager.getDevice()` accessor. Adopt-or-idle (if probe `ok !== true`, show the existing `WebGpuProbeFailureOverlay` reason, no second device); delete the "is WebGPU supported?" pings (`window.webgpuProbe` already answered); depth prefers wasm/cpu while the canvas is live and documents that Transformers may allocate internally; align-or-delete `requestPixelocityDevice`. Verifiable via `tsc`/Jest spies (assert no `requestAdapter` when a handoff exists)/`SKIP_WASM_BUILD=1 npm run build`/`verify:dependency-boundaries` — no GPU needed. Facade stays ≤~350 LOC (currently 348).
Outcome: **SHIPPED + MERGED + CLOSED** — see the 2026-08-29 Done entry. `adoptedGpuDevice.ts` landed, `requestPixelocityDevice.ts` deleted, `RendererManager.getDevice()` at `:305`, zero live `requestAdapter`/`requestDevice` outside the boot probe + ladder, 84 suites / 550 passed. (Original reconciliation note: **last week's #1107 focus SHIPPED + MERGED + CLOSED** — `webgpuBootProbe.ts`/`WebGpuProbeFailureOverlay.tsx`/`docs/WEBGPU_BOOT_PROBE.md` on `main` via `b03ac5a`, Noah closed #1107 completed; `.swarm-state.md` 81 suites / 535 passed, build compiled, boundaries green, `RendererManager` 348 LOC. `origin/main` was **stale in the fresh clone** — a `git fetch origin main` advanced it `ac59e65..d0ac526` (2026-08-22); designated branch `claude/charming-lamport-ozpks1` is then **even with `origin/main` (0/0)**. **Backlog reality shift:** Noah's 2026-08-21 audit closed epic #1076 + children (#1081/#1082/#1083/#1084 gone) and opened #1123–#1129; only 8 issues open total (+#1080 carried). #1106 gpu-chores kernels already landed as `src/gpuChores/**` (`GpuChoresHost.attach(device)`-only) and #1106 is closed. **Tracks:** A=kimi #1126 (`ShaderValidator.tsx` + `ShaderScanner.tsx` + `depthEstimation/loader.ts` + `RendererManager.getDevice()` accessor + `requestPixelocityDevice.ts` align/delete + Jest spies — does NOT touch the boot probe/overlay/ladder, `webgpu/**` engine internals, `gpuChores/**`, `wasm_renderer/**`, shaders, scripts, or backend; no WebGL fallback); B=Copilot #1128 (catalog hygiene — ID/filename/graph reconciliation + 8×8-workgroup detector + 16×16 dispatch fallback; `scripts/`/`.github/workflows/`/manifests only, decoupled from A's `src/` work and from the content stream's `public/shaders/**`). **Fix First not triggered:** #1107 landed clean + closed; production path healthy. **Context gaps:** `recent_chats`/`conversation_search` UNAVAILABLE in this headless scheduled run — context reconstructed from repo tree, `weekly_plan.md`, `.swarm-state.md`, and GitHub (issue #1126/#1107 bodies + open-issue/PR lists). **Jest count carried from `.swarm-state.md`** (`node_modules` absent on-VM, `npm ci` blocked by `sharp` postinstall) — kimi re-establishes the baseline on iteration 0. **Content stream running in parallel** (Jules/generative swarm — merges through #1131, open draft PRs #1132/#1118/#1105) — decoupled from today's renderer focus; `public/shaders/**` + `shader_definitions/**` + `shader_plans/**` churning, kept out of scope.)
Date: 2026-08-29
Mode: User Idea (Noah ran a fresh 2026-08-26 progress audit that CLOSED the entire 2026-08-21 set (#1123–#1129) and opened a new live set #1179–#1185; per standing precedent that set is the live Ideas source. Picked #1180 = TS/C++ workgroup fallback + empty-placeholder packing parity: the only labeled `bug` in the set, foundation-tagged, parts A+B fully headless-self-verifiable, and the last dispatch/upload divergence left by the #1107→#1126 device-discipline arc. Foundation healthy → not Fix First.)
Focus: **#1180 — TS/C++ dispatch + upload parity.** (A) Flip `src/renderer/ShaderCompilation.ts:68-69`'s unparsed-shader fallback from `{x:8,y:8}` to the canonical **16×16** (C++ `wasm_internal.cpp:54-55` is already 16×16) and update `ShaderCompilation.workgroup.test.ts:77-79`, which currently asserts the wrong value as intent — today TS **under-dispatches 3/4 of the frame** on any shader whose workgroup size won't parse, while WASM renders it correctly. (B) Migrate the **26** verified leftover `@workgroup_size(8,8,1)` catalog shaders to 16×16×1 with bounds guards (histogram: 1359 × 16×16×1, 26 × 8×8×1, 1 × 256×1×1, 1 × 16×16×4); leave the 1D helpers and `src/gpuChores/shaders.ts`'s 8×8 downsample kernels alone. (C) Fix C++ `wasm_renderer/resources.cpp`: `:166` creates the 1×1 `emptyTexture_` from the **un-reset** depth `texDesc` (r32float, 4 B/px) but `:179` uploads `bytesPerRow = 16` → a real-GPU WebGPU validation error on first WASM init. **The anti-drift deliverable is the point:** a NEW `src/contracts/workgroup_dispatch.json` that `ShaderCompilation.ts` reads and that the existing `scripts/verify-device-policy-sync.js` validates TS ↔ `wasm_internal.cpp` ↔ JSON — mirroring how it already guards `webgpu_limits.json`/`webgpu_optional_features.json`/`wasm_exports.json`. Verifiable via `tsc` / Jest / `SKIP_WASM_BUILD=1 npm run build` / `verify:device-policy` / `wgsl_precommit_gate.py` on all 26 — no GPU needed for A+B.
Outcome: pending. (Reconciliation this run: **last week's #1126 focus SHIPPED + MERGED + CLOSED** — `adoptedGpuDevice.ts` on `main`, `requestPixelocityDevice.ts` deleted, `RendererManager.getDevice()` at `:305` (facade 353 LOC), and a full `src/**` grep confirms **zero** live `requestAdapter`/`requestDevice` call sites outside `webgpuBootProbe.ts` + `webgpuDevicePolicy.ts`; `.swarm-state.md` closeout 84 suites / 550 passed / 1 skipped, build compiled, boundaries green. Designated branch `claude/nice-bardeen-50zdxv` is **even with `origin/main` @ `a27a627`** (0/0) after `git fetch origin main` advanced it `65f354a..a27a627`. **Backlog reality shift:** Noah's 2026-08-26 audit closed #1123–#1129 wholesale and opened #1179–#1185; only 8 issues open total (+#1080 carried since 08-07). **The 08-24 hygiene pass's RED CI gate has CLEARED** — `audit_dead_sliders.py` now exits 0 (1191 scanned, 0 new, 28 known) and `audit_extrabuffer.py` PASSes (1380 files, 0 new, 93 known, 32 dynamic-index for review), so Fix First was assessed and explicitly **not** triggered. **Tracks:** A=kimi #1180 (`ShaderCompilation.ts` + its workgroup test + NEW `src/contracts/workgroup_dispatch.json` + `verify-device-policy-sync.js` extension + the 26 listed `.wgsl` + the `resources.cpp` `emptyTexture_` block — does NOT touch the adapter ladder, boot probe, `adoptedGpuDevice.ts`/`getDevice()`, `gpuChores/**`, `device.cpp`, backend, or any shader outside the 26); B=Copilot #1184 (contract hygiene — README/manifest/definitions count SoT, underscore-ID alias map, graph-parent whitelist, extraBuffer baseline split; `scripts/`/`.github/workflows/`/`README.md`/`reports/` only). The A/B split is **author-sanctioned**: #1184's "Out of scope" hands the 8×8 migration to #1180. **Context gaps:** `recent_chats`/`conversation_search` UNAVAILABLE in this headless scheduled run — context reconstructed from the repo tree, `weekly_plan.md`, `.swarm-state.md`, live audit-script runs, and GitHub (issue #1180/#1184 bodies + open issue/PR lists). **Jest count carried from `.swarm-state.md`** — `node_modules` is absent on this VM and plain `npm ci` is still blocked by the `sharp` postinstall behind the proxy (`npm ci --ignore-scripts` is the workaround); kimi re-establishes the baseline on iteration 0. **Content stream running in parallel** (Jules/generative swarm — merges through #1189, open draft PRs #1174/#1190/#1191/#1192, plus non-draft plan PR #1177) — decoupled from today's focus, but note it churns `public/shaders/**`, so the 26-file workgroup migration should be rebased late and gated per-file.)
