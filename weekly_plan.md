# image_video_effects — Weekly Plan

## Today's focus
<!-- Routine writes here each run. You can delete after day ends, or keep as history. -->
**2026-07-18 — Land the community gallery for real: wire `CommunityGallery` into the live UI now that #936/#938 have merged (User Idea mode).**
Last week's whole-stack vertical slice **shipped and merged to `main`** (backend `POST/GET /api/preset-packs`, `src/services/communityGallery.ts`, `src/components/CommunityGallery.tsx`, 94 backend + 241 frontend tests green) — but the component is **unmounted**: it's referenced only by its own test, so no user can actually publish or browse. The two PRs that forced the "must NOT touch App.tsx/Controls.tsx" deferral — **#936 (MIDI, merged via PR #957) and #938 (foundation strangler refactor, merged)** — have both landed. App.tsx/Controls.tsx are now carved into `src/hooks/*` + `src/components/controls/panels/*`, so the merge-contention reason to defer is gone. **Today closes the loop: mount the gallery, prove the round trip end-to-end in the running app, then take the deferred stretch goals.**
- **Vertical slice completion (kimi-cli, section A):** mount `CommunityGallery` in `src/components/controls/ControlsContainer.tsx` (mirror how `PresetPackGallery` is passed `onApplySharedChain` at line ~324), thread the current-chain getter + `onApplySharedChain`/`onApplyPack` down from `App.tsx`, add a "Publish current chain → browse community" happy path, then the deferred stretch (play-count sorting on the list, thumbnail/param-summary cards, error/empty states). Half-to-full-day for the wiring; stretch is the long-horizon swarm tail.
- **Allowed to touch:** `src/components/controls/ControlsContainer.tsx`, `src/components/controls/panels/*` (only if a new gallery panel is the clean fit), `src/App.tsx` (now post-refactor and stable — safe to wire props), `src/components/CommunityGallery.tsx`, `src/services/communityGallery.ts`, colocated `*.test.ts(x)`. Backend play-count-sort tweaks in `storage_manager/app.py` + `storage_manager/tests/test_preset_packs.py` are **allowed as stretch only** (kimi-cli owns backend this week so it doesn't collide with the Copilot scripts/ track).
- **Must NOT touch:** `src/renderer/` (immutable engine), `wasm_renderer/` (C++/WASM epic #885–890), `scripts/` + `.github/workflows/` + `reports/` (today's decoupled Copilot WGSL-gate track, section B), `public/shaders/*.wgsl`, `shader_definitions/`. Publish/decode MUST keep routing through `layerChainShare` encode/decode and the existing `STORAGE_API_URL` from `src/config/appConfig` — do not fork the chain format or hardcode a host.
- **Verify each iteration:** `npx tsc --noEmit` clean on `src/`, then `CI=true npx react-scripts test --watchAll=false` green (baseline **~254 test calls across 29 files** on current `main`; record the new count and keep it green). If backend touched: `cd storage_manager && python -m pytest tests/ -q` green (baseline 94). **End-to-end proof (the point of "landed"):** run the app, open the gallery panel, publish the active chain, confirm it appears in the live list, apply it back, and confirm the rendered result matches — screenshot or console-log the round trip. Ignore the pre-existing `wasm_renderer/build.sh --use-port=emdawnwebgpu` failure (issue #848).

---
<details><summary>Archived — 2026-07-11 focus (community gallery vertical slice, now merged)</summary>

**2026-07-11 — Community preset-pack gallery backed by the Storage Manager backend: whole-stack (User Idea mode).**
Last remaining user-written Idea, and the genuine whole-stack unlock. Share links + preset packs + AI-VJ→chain bridge + variation breed/favorites are all shipped; the missing public-audience piece is letting users **publish** a shared chain to the FastAPI backend and **browse a live community gallery** — upgrading today's static `public/preset_packs.json` into a real backend-backed feature. Reuse `encodeChain`/`decodeChain` (`src/services/layerChainShare.ts`) + `mapVJStackToSharedChain` (`vjToSharedChain.ts`); mirror the existing `PresetPackGallery.tsx` component shape and the backend's `/api/shaders/upload` + ratings/play-count patterns in `storage_manager/app.py`. kimi-cli swarm drives the vertical slice; UI wiring into `Controls.tsx` is deferred because that file is under active strangler refactor (open PRs #936, #938).
- **Ship the first vertical slice:** publish → list → fetch → render + apply. Moderation, pagination polish, and play-count sorting are stretch. Multi-day scope — kimi-cli's save-state loop is the right vehicle.
- **Allowed to touch — BACKEND:** `storage_manager/app.py` (NEW endpoints mirroring existing patterns: `POST /api/preset-packs` publish — validate by decoding the chain, reject undecodable; `GET /api/preset-packs` list with limit/offset; `GET /api/preset-packs/{id}` fetch; optional `POST /api/preset-packs/{id}/play`. Reuse the existing GCS/store + Pydantic-model + CORS conventions already in the file — do NOT invent a new persistence layer), NEW `storage_manager/tests/test_preset_packs.py` (pytest, mirror `test_streaming_endpoints.py`/`test_sync_endpoints.py`).
- **Allowed to touch — FRONTEND (NEW files only):** NEW `src/services/communityGallery.ts` (API client: `publishPack`, `listCommunityPacks`, `getCommunityPack`; reuse `encodeChain`/`decodeChain` from `layerChainShare.ts`; **reuse the existing storage-manager fetch base URL** that `ShaderBrowserWithRatings`/the ratings client already use — find and import it, do NOT hardcode a host), NEW `src/components/CommunityGallery.tsx` (mirror `PresetPackGallery.tsx` props `open/onToggle/onApplyPack`/`onApplySharedChain`; live-fetch list; "Publish current chain" action driven by a passed-in current-chain getter), NEW colocated `communityGallery.test.ts` (mock `fetch`) + `CommunityGallery.test.tsx`.
- **Must NOT touch:** `src/App.tsx` and `src/components/Controls.tsx` (**both under active strangler refactor in open PRs #936 MIDI + #938 foundation-epic #912 — editing them here guarantees merge conflict**; leave a `// TODO(wire): mount CommunityGallery once #936/#938 land` marker instead of wiring), `src/renderer/` (immutable engine), `wasm_renderer/` (C++/WASM epic #885–890), `scripts/` + `.github/workflows/` + `reports/` (today's decoupled Copilot track), `public/shaders/*.wgsl`, `shader_definitions/`. Publish/decode MUST route through the existing `layerChainShare` encode/decode — do not fork the chain format.
- **Verify each iteration:** FRONTEND — `npx tsc --noEmit` clean, then `CI=true npx react-scripts test --watchAll=false` green (baseline is now **217 tests** once #936 lands, ~180 on current main; keep whatever you branch from green and record the new count). BACKEND — `cd storage_manager && python -m pytest tests/ -q` green including the new suite. Round-trip contract: a chain published via `POST /api/preset-packs` must come back from `GET /api/preset-packs/{id}` and `decodeChain` to an equal `SharedChain`. Ignore the pre-existing `wasm_renderer/build.sh --use-port=emdawnwebgpu` failure (issue #848).
</details>

## Ideas
<!--
Write ideas here during the week as they come to you.
Routine prioritizes these over generated ideas.
Format: - [ ] Short description (optional: more context on next line indented)
Routine will mark picked items as "[in progress — YYYY-MM-DD]".
-->
- [x] AI VJ Mode — prompt-to-shader-stack generation via in-browser Gemma-2-2b
  Feed user vibe prompt → LLM picks N shaders + params from the 700+ library → renders a live VJ stack. Half-day prototype, multi-day polish.
  → Completed 2026-04-25 (buildShaderManifest all 15 categories, selectShadersFromLLM with params, vibe-prompt UI wired in Controls.tsx)
- [x] Shader metadata normalization + full-text search over 700+ library
  Reconcile `params_missing.md`, `SHADER_PARAMETER_AUDIT.md`, and `shader_params_extracted.json` into a single canonical metadata schema so the scanner/rating/AI-VJ paths share one source of truth. Full-day.
  → Completed 2026-05-02 (shaderCatalog.ts built, wired into ShaderBrowserWithRatings + AutoDJ.buildShaderManifest)
- [x] AI VJ stack persistence + history panel
  Add localStorage save/load of generated stacks, a "VJ history" panel showing last N stacks with their vibe prompts, and a one-click "regenerate variation" button. Surface live shader IDs in the UI. Half-day.
  → Completed 2026-05-23 (vjHistory.ts service, saveVJStack in AutoDJ.ts, collapsible VJ History panel with Restore + Regen buttons in Controls.tsx)
- [x] Per-shader param presets + AI VJ randomizer
  Build a preset system for param combos (keyed off shaderCatalog), add a "randomize params" operator that samples within min/max/step ranges for each shader in the active AI VJ stack. Enables live-performance use. Full-day.
  → Completed 2026-05-23 (vjPresets.ts: VJPreset + save/load/delete + randomizeParams with step-snap & [min,max] clamp; randomizeActiveParams() on Alucinate in AutoDJ.ts; Randomize Params / Randomize Slot / Randomize All Slots buttons + collapsible Presets panel save/restore/delete in Controls.tsx; tsc PASS). Verified in repo audit 2026-05-30.
- [x] AI VJ "auto-transition" / beat-aware sequencer
  Extend the AI VJ stack to auto-transition between generated stacks on a timer or audio-beat trigger, cross-fading params via the existing randomizeParams/preset infra. Turns one-shot generation into a continuous live set. Full-day. (Generated 2026-05-30 — was the Copilot track.)
  → Completed 2026-05-30 (commits 812b5ab feat + 60853dd fix). Full stack shipped: `beatDetector.ts`, `transitionOrchestrator.ts`, `audioGraph.ts`, `transitionMath.ts` + tests; `AutoTransitionConfig`, `startAutoTransition`/`stopAutoTransition`/`triggerNextTransition`/`buildTransitionTarget` on Alucinate (AutoDJ.ts); Auto-Transition panel (timer/beat source, interval, duration, randomize/cyclePresets mode) in Controls.tsx. Verified in repo audit 2026-06-06.
- [x] Multi-slot layer-chain preset packs + shareable URLs
  Serialize the full multi-slot chain (up to 6 slots: shaders + params + blend) into a compact shareable URL/JSON, with a curated "preset pack" gallery. Makes presets shareable beyond localStorage, unlocking the public audience. Half-to-full-day. (Generated 2026-05-30 — runner-up new idea.)
  → Completed 2026-06-13 (kimi-cli swarm). `src/services/layerChainShare.ts` (versioned SHARED_CHAIN_VERSION=1; `buildSharedChain`/`expandSharedChain`/`encodeChain`/`decodeChain`, default-param diffing + re-expansion, MAX_SHARED_SLOTS=6); `PresetPackGallery.tsx` + `public/preset_packs.json` (curated named chains); App.tsx hydrates from `?chain=` on load, `applySharedChain` + copy-share-link + share modal (Twitter/TikTok); Controls.tsx "Copy Chain Share Link" button + gallery. 21/21 tests PASS via `react-scripts test`. Committed f5edd00.

<!-- New ideas generated 2026-06-13 (New Idea mode; #1 WGSL authoring gate was that day's kimi-cli focus). -->
- [x] AI VJ → shareable chain bridge [Copilot track — 2026-06-13]
  Wire `Alucinate.getActiveShaderIds()` / `getCurrentParams()` through `buildSharedChain`+`encodeChain` so a generated VJ stack yields a one-click share link and an optional "save as preset pack" entry. AI-VJ generation and shareable URLs both just shipped — the bridge is the public-audience unlock. Half-day. Frontend-only (src/), decoupled from the scripts/ authoring track.
  → Completed (Copilot track, by 2026-06-20). `src/services/vjToSharedChain.ts` (`mapVJStackToSharedChain`, `buildCatalogDefaultsLookup`) wired into `src/App.tsx` + `src/AutoDJ.ts`. Verified present on main during 2026-06-20 audit.
- [x] Multi-slot chain "remix" / variation explorer
  From any shared chain or preset pack, generate N param/shader variations (reuse `randomizeParams`) into a live thumbnail grid for A/B selection and one-click adopt. Share + randomizer + preset packs are all in place; this is the creative-exploration layer on top. Full-day. (kimi-cli focus 2026-06-20.)
  → Completed 2026-06-20 (kimi-cli swarm, green) and **merged to `main`** (commits `4c8a4d2`, `8f4a06a` — verified ancestors of `origin/main` this audit). `src/services/variationExplorer.ts` (`generateChainVariations`, deterministic mulberry32 PRNG, reuses `randomizeParams`/`buildSharedChain`/`buildCatalogDefaultsLookup`/`mapOrderedParamsToSlotParams`/`shaderCatalog`) + `variationExplorer.test.ts`; `src/components/VariationGrid.tsx` (6-cell grid, param summary, A/B toggle, Adopt via `onApplySharedChain`→`applySharedChain`, shuffle) + `VariationGrid.test.tsx`; "Remix Chain" button wired into `Controls.tsx`. `npx tsc --noEmit` clean, `react-scripts test` 17 suites / 140 tests PASS, prod build compiled.

<!-- New ideas generated 2026-06-27 (New Idea mode; #1 Variation Explorer evolve/breed is this week's kimi-cli focus — picked & set in Today's focus). Below are the two runners-up. -->
- [x] Live-performance control surface for the AI-VJ stack (Web MIDI + keyboard)
  Map MIDI CC / keyboard keys to slot params, randomize triggers, and transition triggers — reuse `startAutoTransition`/`triggerNextTransition`/`randomizeActiveParams` (AutoDJ.ts) and the preset/randomizer infra.
  → COMPLETE 2026-07-11 (in **open PR #936** `feat/midi-control-and-wasm-parity`, not yet merged). Delivered `src/services/controlBindings.ts` + `src/services/midiControl.ts` (Web MIDI, no new npm deps) + unit tests + Controls learn/bind UI; **217 tests pass** per the PR. Landed as a clean single commit on latest `main` after being trapped on the `radiant-seraph-plan-…` branch. Action for Noah: review + merge #936.
- [~] Community preset-pack gallery backed by the Storage Manager backend (whole-stack) [in progress — 2026-07-18; backend + frontend code MERGED to main, UI wiring is the remaining slice → today's focus]
  Let users publish their shared chains to the FastAPI storage backend and browse a live community gallery, upgrading today's static `public/preset_packs.json` into a real public-audience community feature. Reuses `layerChainShare` encode/decode + `vjToSharedChain`. Why now: share links + preset packs + AI-VJ→chain bridge + breed/favorites are all shipped; this is the social/public unlock. Scope: multi-day, touches `src/` (NEW service + component only) + `storage_manager/`. **Picked as today's focus** — kimi-cli drives it; UI wiring deferred behind the App/Controls strangler refactor (#936/#938).

## Backlog
<!--
Unfinished items, known bugs, deferred ideas.
Routine maintains this automatically — you can add items too.
-->
- [x] ~~Working branch `…thm8fq` 92 ahead of `origin/main`~~ → **RESOLVED 2026-07-11.** breed/favorites landed on `main` (`94b0619`, verified ancestor of `origin/main`); `main` has advanced substantially since 2026-06-27 (generative-shader PRs #907–937, shader-scan fix #935). This week the routine cut a fresh `claude/charming-lamport-1e4x50` from current `main` (0/0). The long-lived-divergence risk is cleared.
- [x] ~~**Foundation epic #912: App/Controls strangler refactor IN FLIGHT (open PR #938, Wave 3).**~~ → **MERGED to `main` 2026-07-18.** `App.tsx`/`Controls.tsx` carved into `src/hooks/*` (`useShareChain`, `useDepthEstimation`, `useAudioReactiveParams`, …) + `src/components/controls/` (`ControlsContainer`, `panels/ParamSlidersPanel`, `panels/SlotStackPanel`, …); commit `f150e13` "split four 1000+ line source files under 700 lines" is on `main`. The "must NOT touch App/Controls" boundary that deferred gallery wiring is **retired** — today's focus wires the gallery. Original in-flight note retained below for history:
- [ ] _(history)_ **Foundation epic #912: App/Controls strangler refactor is IN FLIGHT (open PR #938, Wave 3).** `App.tsx` (~2397→~2076) and `Controls.tsx` (~1834→~1663) are being carved into hooks/panels (`useDepthEstimation`/`useAudioReactiveParams`/`useShareChain`, `ParamSlidersPanel`/`RendererBackendPanel`/`SlotStackPanel`, canonical `RendererSwitcher`), `tsconfig` target raised to ES2020, `docs/APP_STRUCTURE.md` added. **Direct blast-radius warning for the frontend routine:** any new frontend work must avoid editing `App.tsx`/`Controls.tsx` until #938 (and the MIDI PR #936, which also edits `Controls.tsx`) land, or it will conflict. Today's kimi-cli focus is scoped to NEW files + backend for exactly this reason.
- [x] ~~**Two large frontend PRs open & unmerged (2026-07-11): #936 (MIDI + WASM parity) and #938 (foundation Wave 3).**~~ → **BOTH MERGED to `main` 2026-07-18.** #936 landed via merge PR **#957** (`0069851` "Merge pull request #957 from …feat/midi-control-and-wasm-parity"); #938 strangler refactor landed (`f150e13`). Sequencing worked out. Both blast-radius blockers on `App.tsx`/`Controls.tsx` are cleared → gallery UI wiring is unblocked (today's focus).
- [ ] **NEW — `CommunityGallery` is MERGED but UNMOUNTED (dead code).** `src/components/CommunityGallery.tsx` + `src/services/communityGallery.ts` are on `main` with full test coverage, but the component is imported only by `CommunityGallery.test.tsx` — no `App.tsx`/`ControlsContainer.tsx` mount, and the `TODO(wire)` marker the swarm left in `ControlsContainer.tsx` appears to have been dropped during the #938 refactor (grep finds no `TODO(wire)`). Users cannot reach the feature. **This is today's kimi-cli focus** — wire it + prove the round trip in the running app.
- [ ] **#858 (WGSL gate: workgroup arg-count + orphan auditor) CLOSED-completed 2026-06-27.** Outputs verified present: `scripts/wgsl_precommit_gate.py` (has the `@workgroup_size` arg-count check), `scripts/audit_orphan_shader_defs.py`, `reports/orphan_shader_defs.{md,json}`. Both new checks landed as **non-blocking `[WARN]`** per the issue's decision. **Today's decoupled Copilot track (section B) = the promote-to-blocking follow-on** (fix `gen-showcase-nebula-core`'s 2-arg `@workgroup_size`, flip the workgroup check to `sys.exit(1)` after grace, make `likely-broken` orphan classifications fail CI). `scripts/`+`.github/`+`reports/`-only — zero overlap with the frontend track. **CORRECTED 2026-07-18 (verified in code + full-tree scan; supersedes earlier notes on this line):** the promote-to-blocking is essentially already DONE. 2-arg `@workgroup_size` already hard-fails (`split_workgroup_issues`: `arg_count==2`→blocking); the orphan `likely-broken`/`parse-error` classification already fails CI forward-only (`audit_orphan_shader_defs.py --base origin/main` at `ci.yml:219`); `gen-showcase-nebula-core` is already `@workgroup_size(16,16,1)` and pinned by `test_gen_showcase_nebula_core_if_present`. Full-tree scan = **0 one-arg, 0 two-arg** across 1304 shaders; orphan report = 0 likely-broken/0 orphan. The only remaining soft class is **1-arg override forms, which are VALID WGSL** (naga accepts; Z defaults to 1) — banning them outright is wrong. **Real residual hygiene** (the corrected Copilot track): (1) fix the `split_workgroup_issues` docstring↔code contradiction + add a regression test locking 2-arg→blocking/1-arg→warning; (2) run the cheap regex convention scan full-tree per-PR (sub-second) to close the forward-only coverage gap; (3) remove the committed stale `reports/orphan_shader_defs.json` (already a CI artifact) or add a drift check; (4) fix the misleading `[ERROR]` label + no-op `--fix` hint on 1-arg warning output; (5) NEW higher-value check (per Grok): after naga validate, assert workgroup dims against portable minimum limits (invocations≤256, X/Y≤256, Z≤64) — a real correctness gate, unlike the style rule. Grace-window/allowlist machinery is unnecessary (zero backlog).
- [ ] **WASM/C++ renderer thread is now ACTIONABLE (Tier-B decision resolved).** Per `MEMORY.md`, the #850-equivalent product call was made 2026-06-27: **WASM = experimental opt-in, TS WebGPU = production default** (policy in `WASM_BACKEND_POLICY.md`). #845–849 closed; a fresh epic **#885** (umbrella) + sub-issues **#886** (recording bypasses internal capture → blank WebM under WASM), **#887** (RendererManager forwarding / `instanceof`+`as any` audit), **#888** (FFT bins / GPU timings / slot-state parity), **#889** (Playwright `?renderer=wasm` smoke + bench), **#890** (docs refresh + Tier-A promotion tracking) are open (all 2026-07-02). These touch `src/App.tsx` + `src/renderer/` + `wasm_renderer/` + C++ — **still NOT owned by this weekly routine** (that's a Copilot/Jules/Noah C++ thread), but it is no longer decision-blocked. Flagged so the kimi/Copilot src-vs-scripts split stays aware of the App.tsx/renderer blast radius.
- [ ] ~~Storage Manager: verify `/api/images` and `/api/videos` streaming endpoints under load (thread-pool saturation, GCS token refresh edge cases)~~ → **CLOSED 2026-06-13** (see Done): hardened via PR #809 — signed-URL redirect + proxy fallback (`fd7bf09`), review fixes `12b2407`. Follow-up if pain persists: dedicated load test under concurrent token-refresh.
- [ ] ~~Confirm CORS allowlist (`https://test1.1ink.us`) shipped to the VPS~~ → **RESOLVED 2026-06-15** (hygiene pass): the real frontend origin is `https://test.1ink.us` (with a dot), confirmed by `storage_manager/app.py:896` `--base-url=https://test.1ink.us/image_video_effects` — and it **is** in `ALLOWED_ORIGINS` (app.py:412-417, alongside `go.1ink.us`, `noahcohn.com`, `localhost:3000`). `git log -S "test1.1ink.us" -- storage_manager/app.py` returns empty → `test1.1ink.us` was a **typo** in prior notes; it never existed in the backend. CORS is correct. (Live VPS confirmation still only verifiable from the deployed host.)
- [x] ~~`tsc --noEmit` RED — `WASMRenderer.input.test.ts` GPUCanvasContext error~~ → **FIXED 2026-07-11**: line 59 now casts `as unknown as GPUCanvasContext`. Typecheck gate clean on that line. (Likely folded into PR #936's WASM-parity work.)
- [ ] **Orphan/missing-WGSL count drifted 37 → 39** (hygiene pass 2026-06-27 `sync:shaders:dry`): `gen-luminescent-quantum-void-anglerfish` pending upload + 39 defs referencing absent local WGSL. The +2 are this week's merged generative shaders. Still folded into #858 Phase 2 (orphan auditor).
- [ ] **`npm ci` cannot complete clean-room** (hygiene pass 2026-06-27): `sharp` postinstall downloads its libvips binary from a GitHub release, which is blocked behind the agent proxy ("no credentials") → whole install aborts, `typescript` missing. `npm ci --ignore-scripts` works around it. Risk: any fresh/proxied CI env without a cached `sharp` binary fails `npm ci`. Triage: pin a sharp version whose binary is registry-reachable, vendor it, or demote to optional/devDependency if it's build-time-only.
- [ ] **2.7 MB gzipped `main.js`** (CRA "significantly larger than recommended" warning, hygiene pass 2026-06-27). Code-splitting candidate (lazy-load `@xenova/transformers` / `@mlc-ai/web-llm`). Intersects the onnxruntime-web bundle-size concern in the dependency-remediation issue.
- [ ] **npm prod-dependency vulns (5): 1 critical / 3 high / 1 moderate** (surfaced by hygiene audit 2026-06-15) — all chain through `@xenova/transformers` (depth-estimation core dep) → `protobufjs` (critical), `onnxruntime-web`/`onnx-proto` (high), `@protobufjs/utf8` (moderate). `npm audit` fix = downgrade `@xenova/transformers` to 2.0.1 (a major step back — needs validation that depth estimation still works). Not auto-fixed. Triage: upgrade path vs. risk.
- [ ] **1 shader workgroup-size warning: `gen-showcase-nebula-core`** uses `@workgroup_size(8, 8)` (expected `8x8x1` / `16x16x1`) — surfaced by `generate_shader_lists.js` during build 2026-06-15. Non-blocking (build compiles). **Now tracked by open issue #858** (WGSL gate arg-count `@workgroup_size` check, scripts/-only) — this week's decoupled Copilot/expansion track candidate. Still present on main 2026-06-27.
- [ ] **WASM/C++ renderer backend — open roadmap thread (issues #845–#850, opened 2026-06-18).** New emergent focus area: a working-but-incomplete C++ renderer compiled to WASM via Emscripten + emdawnwebgpu. #850 is a *decision* issue (invest in full production support vs. experimental toggle vs. native-app split). #845–847 = RendererManager/input/interface-method parity (touches `src/` + `wasm_renderer/renderer.cpp`); #848 = Emscripten CI/build reliability; #849 = WASM testing/visual-parity/benchmark. **Not owned by this routine** — needs Noah's roadmap call on #850 first. Flagged so future runs and the kimi/Copilot src-vs-scripts split stay aware of the `wasm_renderer/` blast radius.
- [ ] ~~`sync-images` / `sync-videos` admin endpoints need a dry-run mode~~ → **CLOSED 2026-06-06**: superseded by the two-phase **plan/apply intent flow** (`POST /api/admin/sync-images/plan` creates a no-write intent = dry-run; `/apply` executes). Old single-shot `POST /api/admin/sync-images` now returns a "gone" pointer to plan/apply.
- [ ] ~~Bind-group compatibility report triage pass~~ → **CLOSED 2026-06-06** (see Done): 966/971 compatible, 15 auto-fixed (naga-OK), 4 deferred to human redesign, 1 false positive.
- [ ] **4 bind-group layout-conflict shaders need human redesign** (deferred by the triage swarm — not auto-fixable): `plasma` (binding 12 uses `array<PlasmaBall,50>` instead of `array<vec4<f32>>`), `tone-histogram` + `tone-histogram-apply` (binding 10 `array<atomic<u32>>` histogram atomics need architectural fix), `deep-workgroup-multi-effect-blend` (`workgroup_size(16,16,4)` w/ `shared_tile[16][16][4]` — can't shrink wg without redesign).
- [x] ~~Add `_hash_library` to `TEMPLATE_FILES` in `scripts/bindgroup_checker.py`~~ → **CLOSED 2026-06-20** (verified): `_hash_library.wgsl` is now line 47 of `TEMPLATE_FILES` — recurring false positive eliminated. Landed alongside last week's WGSL authoring-gate work.
- [ ] **37 shader definitions reference missing local WGSL files** (surfaced by sync dry-run 2026-05-30) — triage whether these are intentional storage-only entries or broken refs. **Now folded into open issue #858** (Phase 2: offline orphan-definition auditor `scripts/audit_orphan_shader_defs.py`, classifies `storage-only` vs `likely-broken`).
- [ ] ~~Test runner broken: `typescript` missing from `node_modules`~~ → **CLOSED 2026-05-30**: `npm ci` restores it; CI already enforces `npm ci` before test via `ci.yml`.
- [ ] ~~`layerChain.spec.ts` — add a CI step to enforce `npm ci` before test~~ → **CLOSED 2026-05-30**: `ci.yml` already has `npm ci` + explicit TypeScript resolution gate before `npm test`.

## Done
<!--
Completed items, routine archives here with date.
Prune occasionally when this gets long.
-->
- 2026-07-18: **Community preset-pack gallery vertical slice SHIPPED + MERGED to `main`** (last week's whole-stack focus, kimi-cli swarm). BACKEND `storage_manager/app.py`: `preset_pack` STORAGE_MAP entry + Pydantic models (`PresetPackPublishPayload`/`PresetPackMeta`) + `_decode_shared_chain` validator + `POST /api/preset-packs` (publish, rejects undecodable chains), `GET /api/preset-packs` (`?limit`/`?offset`), `GET /api/preset-packs/{id}`, `POST /api/preset-packs/{id}/play`; NEW `storage_manager/tests/test_preset_packs.py` (22 tests incl. round-trip `test_published_chain_decodes_to_original`). FRONTEND `src/services/communityGallery.ts` (`publishPack`/`listCommunityPacks`/`getCommunityPack`/`recordPresetPackPlay`, reuses `STORAGE_API_URL` + `encodeChain`/`decodeChain`) + `communityGallery.test.ts` (8, incl. round-trip); `src/components/CommunityGallery.tsx` (live-fetch list + inline publish form, mirrors `PresetPackGallery`) + `CommunityGallery.test.tsx` (8). Verified per `.swarm-state.md`: backend `pytest` **94 passed**, `tsc --noEmit` clean, `react-scripts test` **27 suites / 241 tests passed**. **Remaining slice (today):** component is unmounted — UI wiring was deferred behind #936/#938, both now merged.
- 2026-07-18: **PR #936 (Web-MIDI live-control + WASM API parity) MERGED to `main`** via merge PR #957 (`0069851`). Closes last week's "review + merge #936" action. `controlBindings.ts` + `midiControl.ts` + Controls learn/bind UI now on `main`.
- 2026-07-18: **PR #938 (foundation epic #912 — App/Controls strangler refactor, Wave 3) MERGED to `main`** (`f150e13` "split four 1000+ line source files under 700 lines"). `App.tsx`/`Controls.tsx` decomposed into `src/hooks/*` + `src/components/controls/panels/*` (`ControlsContainer`, `ParamSlidersPanel`, `SlotStackPanel`, `useShareChain`, …). Retires the "must NOT touch App/Controls" boundary that had gated frontend work for three weeks.
- 2026-07-11: Variation Explorer breed/favorites **MERGED to `main`** (`94b0619` "push fix" is now an ancestor of `origin/main`). `src/services/variationFavorites.ts` + `breedVariations` in `variationExplorer.ts` confirmed present on `origin/main` this audit. Closes last week's "92-ahead unmerged branch" backlog risk — the working history landed. (The routine's designated branch this week is a fresh `claude/charming-lamport-1e4x50` cut from current `main`, 0 ahead / 0 behind.)
- 2026-07-11: Web-MIDI + keyboard live-control surface COMPLETE (last week's focus) — in **open PR #936** `feat/midi-control-and-wasm-parity`. `src/services/controlBindings.ts` + `src/services/midiControl.ts` (Web MIDI API, no new deps) + unit tests + Controls learn/bind UI; PR also folds in RendererManager/WASM API parity (`GPUTimings`/`timingSource`, `getAudioData`, `isRecording`) + a WASM smoke harness. **217 tests pass** per the PR. Pending Noah's review/merge (the work exists + is green; it just needs landing).
- 2026-07-11: `tsc --noEmit` WASM-test error RESOLVED — `src/__tests__/WASMRenderer.input.test.ts:59` now casts `as unknown as GPUCanvasContext` (was the `CanvasRenderingContext2D`/GPUCanvasContext overload mismatch). The typecheck gate is no longer red on that line. Removes last week's designated Claude Code hygiene fix.
- 2026-07-04: Variation Explorer "evolve / breed" + favorites persistence COMPLETE (last week's kimi-cli focus — verified green on the working branch this audit; merged to `main` 2026-07-11 per entry above). Per `.swarm-state.md` 4-iteration log + present files: `src/services/variationExplorer.ts` gained `breedVariations(parentA, parentB, count, catalog, options)` — pure seedable mulberry32 crossover (slot count = min(A,B,MAX_SHARED_SLOTS); 50/50 per-slot shader pick; per-param linear blend when both parents share the shader; optional 25% same-category mutation; children built through `buildSharedChain` so they round-trip `expandSharedChain`/`applySharedChain`). NEW `src/services/variationFavorites.ts` (localStorage CRUD modeled on `vjHistory.ts`/`vjPresets.ts`: `VariationFavorite`, save/load/delete/clear, `MAX_FAVORITES=50`, `generateId` with `crypto.randomUUID`+jsdom fallback). `VariationGrid.tsx` extended: editable Seed input, Shuffle (fresh seed), Reseed-from-chain, per-card star→favorite, "Breed selected" (enabled on exactly two A/B-selected cells), favorites strip with Load/delete. New tests: 7 breed + 6 favorites + 4 grid. Verified: `react-scripts test` **21 suites / 173 tests PASS** (baseline 17/140); `npx tsc --noEmit` clean except the pre-existing `WASMRenderer.input.test.ts` GPUCanvasContext error (WASM area, unrelated); `react-scripts build` compiled. Committed on branch `claude/charming-lamport-thm8fq` (`94b0619`). **Action for Noah: run the Jules wrap-up (section F) to land it on `main`.**
- 2026-06-20: Multi-slot chain "remix" / variation explorer SHIPPED (kimi-cli swarm, last week's focus — verified green + merged to `main` this audit). `src/services/variationExplorer.ts` (`generateChainVariations` + deterministic mulberry32 seedable PRNG, reuses `randomizeParams`/`buildSharedChain`/`buildCatalogDefaultsLookup`/`mapOrderedParamsToSlotParams`/`shaderCatalog`) + `variationExplorer.test.ts`; `src/components/VariationGrid.tsx` (6-cell A/B grid, param summary, Adopt routed through `onApplySharedChain`→`applySharedChain`, shuffle) + `VariationGrid.test.tsx`; "Remix Chain" button in `Controls.tsx`. Verified: `npx tsc --noEmit` clean, `react-scripts test` 17 suites / 140 tests PASS, prod build compiled. Commits `4c8a4d2` + `8f4a06a` confirmed ancestors of `origin/main`. Known unrelated failure: `wasm_renderer/build.sh` `--use-port=emdawnwebgpu` (pre-existing toolchain, issue #848).
- 2026-06-20: WGSL authoring scaffold + pre-commit canonical-binding gate SHIPPED (kimi-cli swarm — verified on main this audit). `scripts/new_shader.py` (canonical-binding scaffolder), `scripts/wgsl_precommit_gate.py` (changed-file naga + bindgroup check), `public/shaders/_template_canonical_compute.wgsl`, docs `scripts/AUTHORING.md` + `scripts/NAGA_README.md`. Wired into CI at `.github/workflows/ci.yml:191` (`python3 scripts/wgsl_precommit_gate.py --base origin/main`, with naga-cli install step). Mid-week Jules hardening (commit 44e311a, 2026-06-15): gate now locates naga via `shutil.which` (fixes CI Permission-Denied), Playwright e2e timeouts bumped to 120s, stale `harborglow` submodule uncached. Shift-left authoring path is live — reactive fix swarms should taper.
- 2026-06-20: AI VJ → shareable chain bridge SHIPPED (Copilot track — verified on main this audit). `src/services/vjToSharedChain.ts` (`mapVJStackToSharedChain`, `buildCatalogDefaultsLookup`) bridges a generated VJ stack to the share-link encode path; wired into `src/App.tsx` + `src/AutoDJ.ts`. Closes the 2026-06-13 Copilot-track idea.
- 2026-06-13: Multi-slot layer-chain preset packs + shareable URLs SHIPPED (kimi-cli swarm, commit f5edd00). `src/services/layerChainShare.ts` — versioned (`SHARED_CHAIN_VERSION=1`, `MAX_SHARED_SLOTS=6`) URL-safe encode/decode with default-param diffing on encode + re-expansion on decode (`buildSharedChain`/`expandSharedChain`/`encodeChain`/`decodeChain`, per-shader defaults lookup). `PresetPackGallery.tsx` + curated `public/preset_packs.json`. App.tsx hydrates the chain from a `?chain=` link on first load, `applySharedChain` restores modes+params (skips unknown shader ids), copy-share-link, and a share modal (Twitter/TikTok). Controls.tsx "🔗 Copy Chain Share Link" + gallery wiring. Verified 21/21 tests PASS via `react-scripts test` (`layerChainShare.test.ts` 16 + `PresetPackGallery.test.tsx` 5).
- 2026-06-13: Storage Manager image/video endpoint hardening SHIPPED (Copilot track, PR #809 — `fd7bf09` feat: signed-URL redirect + proxy fallback for `/api/images` & `/api/videos`; `12b2407` review fixes: public-constant rename, logging, type annotation, test assertions). Closes last week's backend streaming-under-load backlog item.
- 2026-06-06: Bind-group compatibility triage + auto-repair pass COMPLETE (kimi-cli swarm). Fresh checker run found 20 incompatible (the May-9 report was stale); 15 auto-fixed and all naga-OK via `scripts/fix_bindgroups.py` (+binding12 stubs, workgroup `(16,16)`→`(16,16,1)`, `custom_params`→`ripples`, +missing bindings 4–12). 4 deferred to human redesign (plasma, tone-histogram, tone-histogram-apply, deep-workgroup-multi-effect-blend — now backlog). 1 false positive (`_hash_library`). Final: 966 compatible / 5 incompatible / 3 templates / 971 total. Artifacts: `reports/bindgroup_{triage,fix_summary,fix_queue}.{md,json}`, `binding_fix_review.json`; scripts `triage_bindgroup_report.js`, `bindgroup_checker.py`, `fix_bindgroups.py`. Full log in `.swarm-state.md`.
- 2026-05-30: AI VJ "auto-transition" / beat-aware sequencer SHIPPED (commits 812b5ab feat + 60853dd fix). Services `beatDetector.ts` (adaptive bass-energy threshold, refractory debounce), `transitionOrchestrator.ts` (IDLE/WAITING/TRANSITIONING state machine, timer + beat sources, eased param lerp), `audioGraph.ts` (shared AudioContext, lowpass-isolated kick band), `utils/transitionMath.ts` — all with unit tests. `AutoTransitionConfig` + `startAutoTransition`/`stopAutoTransition`/`triggerNextTransition`/`buildTransitionTarget` on Alucinate; Auto-Transition panel in Controls.tsx (timer/beat, interval, duration, randomize/cyclePresets). Confirmed via repo audit 2026-06-06.
- 2026-05-30: Hardcoded SFTP password removed from `scripts/deploy.py` (line 251) and `scripts/deploy_app_only.py` (line 38). Both now read `DEPLOY_PASS` env var; `deploy.py` falls back to `getpass.getpass()` interactive prompt; `deploy_app_only.py` already had that fallback. **Rotate the DreamHost credential** — it was committed in plaintext to a public repo.
- 2026-05-30: Pipeline hygiene pass (Claude Code E task) — all 6 stages green: `npm ci` restored typescript@4.9.5; `npm run build` clean (963 shaders, Compiled successfully); `layerChain.spec.ts` 25/25 PASS; sync dry-run (34 to upload, no writes); deploy scripts verified correct; storage manager AST-clean. Closed two stale backlog items (test runner / CI enforcement — both already solved in ci.yml).
- 2026-05-23: Per-shader param presets + AI VJ randomizer — `vjPresets.ts` (VJPreset interface; savePreset/loadPresets/deletePreset, localStorage, max 50; `randomizeParams(shaderIds, catalog)` samples uniform [min,max], snaps to step, clamps overshoot), `randomizeActiveParams()` public method on `Alucinate` (`src/AutoDJ.ts`), and Controls.tsx UI: Randomize Params + Randomize Slot + Randomize All Slots buttons and collapsible Presets panel (save named / restore / delete). tsc PASS. Confirmed via repo audit 2026-05-30 (commits 1969be8, be1ccfa).
- 2026-05-23: AI VJ stack persistence + history panel — vjHistory.ts service (save/load/clear/removeEntry, max 20 entries, localStorage), saveVJStack wired into AutoDJ.generateFromVibe, collapsible "VJ History" panel in Controls.tsx with per-entry Restore (exact stack+params) and Regen (re-fires generateFromVibe with same vibe string) buttons, Clear History button.
- 2026-05-16: WGSL runtime-error fix pass — all 49 critical shaders in `reports/runtime_errors_report.json` fixed and validated via naga. Patterns: bulk canonical binding renames (videoSampler→u_sampler, outTex→writeTexture, etc.) resolved the majority; false-positive invalid_binding flags cleared for 8 multi-pass shaders; array_bounds ripple clamps added where needed. 49/49 naga OK per `.swarm-state.md`.
- 2026-05-09: Shader metadata normalization — `shaderCatalog.ts` service built (merges 15 category JSONs + shader_params_extracted.json, in-memory full-text index, module-level cache), wired into `ShaderBrowserWithRatings` (search) and `AutoDJ.buildShaderManifest()` (AI VJ path). Single source of truth confirmed in code audit.
- 2026-05-02: AI VJ Mode — Alucinate full-library manifest (all 15 categories, 918 shaders), LLM param suggestion (`selectShadersFromLLM` returns `{id, params}[]`), `onUpdateParams` callback, `generateFromVibe()` one-shot method, vibe-prompt text input + Generate button in Controls.tsx (kimi-cli swarm, confirmed via `.swarm-state.md` + code audit)
- 2026-04-18: Storage Manager CORS fix for `test1.1ink.us`; image/video streaming endpoints; sync-images/sync-videos admin endpoints (archived from prior notes)
- 2026-04-18: Obsidian Echo-Chamber generative shader merged (#536)
- 2026-04-18: Hyper-Refractive Rain-Matrix generative shader merged (#534)
- 2026-04-18: Bioluminescent Aether Pulsar generative shader merged
- 2026-04-18: WebGPU renderer polish — 405 play-count POST fix, bitonic-sort built-ins, sine-wave UnfilterableFloat (#532)
- 2026-04-18: Startup race, event leaks, rating cache stability fixes (#530)

## Last run
<!-- Routine writes summary here each run. Overwrites previous. -->
Date: 2026-07-18
Mode: User Idea (last week's whole-stack focus shipped + merged; the one remaining in-progress user Idea is delivered in code but not user-reachable → finish it by wiring; foundation healthy → not Fix First)
Focus: Land the community gallery for real — mount `CommunityGallery` into `ControlsContainer`/`App.tsx` now that #936 and #938 have merged, prove the publish→list→apply round trip end-to-end in the running app, then take the deferred stretch (play-count sort, thumbnail/param cards, empty/error states). kimi-cli drives it; owns frontend + backend-stretch this week.
Outcome: pending. (Reconciliation this run: **last week's focus SHIPPED + MERGED** — community preset-pack gallery vertical slice is on `main` (backend `POST/GET /api/preset-packs` + `_decode_shared_chain` validator + 22 pytest; frontend `communityGallery.ts` + `CommunityGallery.tsx` + 16 tests; `.swarm-state.md` records 94 backend / 241 frontend green). Marked in Done. **BUT the component is UNMOUNTED** — imported only by its own test, no App/Controls mount, and the `TODO(wire)` marker was dropped during the #938 refactor → users can't reach it. That's today's focus. **Two big blockers cleared this week:** #936 (MIDI) merged via PR #957 (`0069851`) and #938 (foundation strangler refactor) merged (`f150e13`) — `App.tsx`/`Controls.tsx` are now decomposed into `src/hooks/*` + `src/components/controls/panels/*`, so the three-week "must NOT touch App/Controls" boundary is retired and wiring is safe. Designated branch this run: `claude/charming-lamport-dqdfdq`, cut from current `main` `8aab9fe` (0 ahead / 0 behind). Copilot track (B) = the still-partial #858 WGSL-gate promote-to-blocking (verified: `workgroup_errors` now fail, but `workgroup_warnings` + orphan `likely-broken` classification still non-blocking in `scripts/wgsl_precommit_gate.py`) — scripts/+.github/ only, decoupled from A's src/+storage_manager work. WASM/C++ epic #885–890 remains open + not owned here. **Context gap:** recent_chats/conversation_search UNAVAILABLE (headless run) — context drawn from repo state, `weekly_plan.md`, `.swarm-state.md`, and GitHub PRs/issues only.)
