# 2026-05-31 — Kimi Swarm Execution Plan (v2 Pilot)

**Date**: 2026-05-31  
**Focus**: First live pilot of the **Enhanced Kimi Agent Swarm Operating Procedure v2** (see `agents/KIMI_CLI_SWARM_UPGRADE_PLAN.md`)  
**Primary Mission**: High-volume bind-group canonical repair using the hardened triple-contract prompt + guard  
**Secondary Mission**: End-to-end test of the new Grok → Kimi creative brief → Kimi implementation → `.notes.kimi.md` handoff for 2 new high-wow shader seeds  
**Mode**: Local `kimi-cli --no-stream` only

---

## Context from Prior Day (2026-05-30)

- Large `reports/bindgroup_compatibility_report.json` (800kB, generated 05-30) flagged many shaders for mismatched bind-group layouts.
- The May-16 49-shader canonical rename pass (videoSampler→u_sampler, outTex→writeTexture, etc.) proved the pattern works.
- Today's goal: apply the **v2 prompt discipline** to a fresh batch so we get clean, header-compliant, alpha-aware fixes with almost zero Claude cleanup.

---

## Today's Batch (8 shaders total)

### Repair Micro-Batch A — Bind-Group Canonical Fixes (6 shaders)
These are representative of the most common failure classes in the fresh report. Prioritize shaders that are **close** to correct but have the classic old binding names or missing `ripples` / `plasmaBuffer` fields.

| # | Shader ID | Likely Issue Class | Target Lines | Notes for Kimi Brief |
|---|-----------|--------------------|--------------|----------------------|
| R1 | `gen-superfluid-quantum-foam` | Missing ripples in Uniforms + old bindings | ~140 | Classic "Uniforms struct missing fields" pattern |
| R2 | `plasma` | TYPE_MISMATCH array vs struct + old texture names | ~95 | Simple but high usage — must preserve its visual character |
| R3 | `kaleido-scope-grokcf1` | Binding drift + workgroup size | ~110 | Already has some v1 upgrades — be conservative |
| R4 | `velocity-field-paint` | Old `outputTex` / missing dataTexture writes | ~105 | Interactive + audio — add bass_env if not present |
| R5 | `pixel-sand` | Binding + alpha semantics | ~120 | Simulation — needs meaningful alpha for compositing |
| R6 | `temporal-rgb-smear` | Binding + RGBA blending | ~100 | Visual-effects — preserve the smear aesthetic |

**Source of truth for the exact list**: Run a quick extraction from `reports/bindgroup_compatibility_report.json` for shaders that contain the classic old names (`outputTex`, `videoSampler`, `iTime`, `mouse`, etc.) and are **not** in the May-16 fixed set.

### Creative Pilot Micro-Batch B — Full New-Shader Handoff Test (2 shaders)
These two are the first live test of the complete Grok ideation → kimi-brief → Kimi creation path under v2.

| # | Shader ID (proposed) | Role | Wow Mandate | Target Lines |
|---|----------------------|------|-------------|--------------|
| C1 | `liquid-tensor-vortex` | Algorithmist + Visualist | "A 3D tensor field that melts and reforms like liquid metal under mouse vorticity, with bass-driven surface tension and transmission alpha that lets the background breathe through the folds." | ~135 |
| C2 | `depth-chromatic-bloom` | Visualist | "Chromatic bloom that is stronger in the background (using readDepthTexture) and collapses into tight specular highlights on foreground subjects. Mouse click spawns a radial caustic that travels through depth layers." | ~125 |

**Creative briefs** for these two are written inline below so Kimi can be fed immediately.

---

## New Artifact Directories (already created 2026-05-31)

- `swarm-tasks/kimi-briefs/` — place one `.md` per task before invoking Kimi
- `swarm-outputs/kimi-notes/` — Kimi writes the 3-bullet handoff here
- `swarm-outputs/kimi-rejects/` — any output that fails the guard goes here with the prompt that produced it

---

## Exact Prompts Kimi Should Receive Today

### Prompt 1 — Repair (R1 example — copy and adapt for R2–R6)

```markdown
# KIMI SWARM TASK — REPAIR — gen-superfluid-quantum-foam

## CREATIVE BRIEF (diagnosis + intent)
This shader is failing because its Uniforms struct is missing the `ripples` array and it is using pre-canonical binding names. The visual goal is a superfluid quantum foam with stable high-frequency curl noise, bass-reactive bubble nucleation, and meaningful alpha so it composites cleanly in slot 2/3.

This repair batch we are pushing: **canonical binding compliance + meaningful alpha + audio reactivity** on every shader we touch. Do not invent new bindings.

## DIFFERENTIATE FROM (these already received the May-16 treatment)
- `gen-quantum-mycelium`: uses dataTextureA for temporal mycelium growth + proper writeDepthTexture — your version must not regress to scalar depth writes
- `gen-stellar-web-loom`: has clean premultiplied alpha + bass pulse on thread intensity

## OUTPUT CONTRACT (read three times — non-negotiable)
1. Your entire response after the closing ``` of the WGSL block must be completely empty. No explanations, no "done", no blank lines.
2. The shader must use the **exact current 13-binding header** (no `outputTex`, no `videoSampler`, no `iTime`).
3. Alpha must carry semantic meaning (foam density, bubble brightness, or depth). Never end with `vec4(..., 1.0)`.
4. You must use at least one technique from the 12 graphical tactics (bass_env or hue_preserve_clamp recommended).

## IMMUTABLE 13-BINDING CONTRACT (copy EXACTLY)
```wgsl
@group(0) @binding(0) var u_sampler: sampler;
@group(0) @binding(1) var readTexture: texture_2d<f32>;
@group(0) @binding(2) var writeTexture: texture_storage_2d<rgba32float, write>;
@group(0) @binding(3) var<uniform> u: Uniforms;
@group(0) @binding(4) var readDepthTexture: texture_2d<f32>;
@group(0) @binding(5) var non_filtering_sampler: sampler;
@group(0) @binding(6) var writeDepthTexture: texture_storage_2d<r32float, write>;
@group(0) @binding(7) var dataTextureA: texture_storage_2d<rgba32float, write>;
@group(0) @binding(8) var dataTextureB: texture_storage_2d<rgba32float, write>;
@group(0) @binding(9) var dataTextureC: texture_2d<f32>;
@group(0) @binding(10) var<storage, read_write> extraBuffer: array<f32>;
@group(0) @binding(11) var comparison_sampler: sampler_comparison;
@group(0) @binding(12) var<storage, read> plasmaBuffer: array<vec4<f32>>;

struct Uniforms {
  config: vec4<f32>,       // x=Time, y=MouseClickCount, z=ResX, w=ResY
  zoom_config: vec4<f32>,  // x=Time, y=MouseX, z=MouseY, w=MouseDown
  zoom_params: vec4<f32>,  // x=Param1, y=Param2, z=Param3, w=Param4
  ripples: array<vec4<f32>, 50>,
};
```

## CURRENT SOURCE (read the real file first)
```wgsl
[paste the current gen-superfluid-quantum-foam.wgsl here]
```

## ROLE TOOLKIT — Algorithmist (repair focus)
- Add the missing `ripples` handling (or pass-through if not used)
- Inject bass/mids reactivity via plasmaBuffer using the `bass_env` pattern if possible
- Convert any old texture names to the canonical 13
- Ensure final write uses premultiplied alpha or meaningful alpha

## 12 KIMI GRAPHICAL TACTICS (apply where appropriate)
[full 12-tactics block — same as in KIMI_CLI_SWARM_UPGRADE_PLAN.md v2]

## LINE BUDGET & FINAL REMINDER
Target: ~140 lines max. Math density over commentary.

If any text appears after the final WGSL fence, the pipeline will reject this output. Stop the moment you close the fence.
```

---

### Prompt 2 — New Shader Creation (C1 — liquid-tensor-vortex)

```markdown
# KIMI SWARM TASK — CREATION — liquid-tensor-vortex

## CREATIVE BRIEF (Grok ideation — the spark)
A 3D tensor field that melts and reforms like liquid metal under mouse vorticity, with bass-driven surface tension and transmission alpha that lets the background breathe through the folds. It should feel simultaneously mathematical and visceral — like watching a 4D object intersect our 2D plane through a viscous lens.

This week we are pushing: **liquid metal aesthetics + depth transmission alpha + emergent vorticity from mouse + audio surface tension**. Do not make another generic FBM liquid or simple kaleidoscope.

## DIFFERENTIATE FROM
- `liquid-lens`: spherical refraction with chromatic aberration — yours must feel more "metal" and less glassy
- `gen-luminous-fluid-chladni-resonator`: standing wave nodes — yours must have continuous flowing tensor folds, not discrete nodes
- `kaleido-scope-grokcf1`: symmetry folding — avoid perfect symmetry; use broken, torn, reforming symmetry

## OUTPUT CONTRACT (read three times)
1. Your entire response after the closing ``` must be completely empty.
2. The WGSL **MUST** begin with the exact Standard Hybrid Header from AGENTS.md:
   // ═══════════════════════════════════════════════════════════════════
   //  liquid-tensor-vortex
   //  Category: liquid-effects
   //  Features: mouse-driven, audio-reactive, depth-aware, tensor-field
   //  Complexity: High
   //  Chunks From: warpedFBM, curl2D, hue_preserve_clamp, bass_env
   //  Created: 2026-05-31
   //  By: Kimi Code CLI (weekly swarm)
   // ═══════════════════════════════════════════════════════════════════
3. Alpha must encode transmission / metal opacity so the next slot in the chain can composite beautifully.
4. Must use at least two of: warpedFBM, curl2D (or equivalent divergence-free flow), hue_preserve_clamp + ACES, bass_env.

## IMMUTABLE 13-BINDING CONTRACT
[paste the full 13-binding + Uniforms here — identical to Prompt 1]

## CANONICAL MINIMAL SKELETON (start from this — do not invent bindings)
```wgsl
// [the Standard Hybrid Header above goes here]

@group(0) @binding(0) var u_sampler: sampler;
// ... (the full 13 bindings exactly as above) ...

@compute @workgroup_size(16, 16, 1)
fn main(@builtin(global_invocation_id) gid: vec3<u32>) {
    let dims = vec2<f32>(u.config.zw);
    let uv = vec2<f32>(gid.xy) / dims;
    let color = textureSampleLevel(readTexture, u_sampler, uv, 0.0);
    // TODO: implement the tensor-vortex liquid metal effect here
    textureStore(writeTexture, gid.xy, color);
}
```

## ROLE TOOLKIT — Algorithmist + Visualist (creation)
- Domain-warped FBM + curl noise for the tensor flow field
- Mouse position as a vorticity source (zoom_config.yz) that "tears" the metal
- Bass (plasmaBuffer) modulates surface tension (how sharply the folds snap back)
- Depth (readDepthTexture) controls transmission alpha — background melts more, foreground stays crisp metal
- hue_preserve_clamp + ACES + IGN dither on the final HDR accumulation
- Final write must be premultiplied when alpha < 1.0

## 12 KIMI GRAPHICAL TACTICS
[full block]

## LINE BUDGET & FINAL REMINDER
Target: 130–140 lines. This must feel portfolio-worthy the first time someone sees it with music and mouse.

Stop the moment the WGSL fence closes. Nothing after it.
```

---

## Execution Checklist for 2026-05-31 (Kimi Session)

1. [ ] Create the 8 kimi-brief files in `swarm-tasks/kimi-briefs/` (one for each shader above — even if only 1–2 sentences for the repair ones)
2. [ ] For each of the 6 repair shaders:
   - Read the current `.wgsl`
   - Feed the tailored prompt (using the R1 template as model)
   - Run the output through the mental/manual guard (or the first version of the guard script)
   - If it passes: write the WGSL, write a 3-bullet `.notes.kimi.md`
   - If it fails: move raw output + prompt to `kimi-rejects/`
3. [ ] For the two creation tasks (C1 & C2):
   - Use the exact creation prompt above (fill in the skeleton)
   - Guard must also verify the Standard Hybrid Header is present and correct
4. [ ] After the batch, run `node scripts/generate_shader_lists.js` and `node scripts/check_duplicates.js` on anything that touched a JSON
5. [ ] Write a one-paragraph summary at the bottom of this file: "What worked / what the guard caught / what needs prompt tuning"
6. [ ] Drop a note in the main weekly_plan.md and ping Claude with the location of the new `kimi-notes/` files for the polish pass

---

## Success Criteria for This Pilot

- ≥ 5 of the 6 repair shaders pass the guard on first or second try
- Both new shaders have the full Standard Hybrid Header + at least one genuinely surprising visual behavior (documented in their `.notes.kimi.md`)
- Zero binding name hallucinations in the accepted outputs
- The two new shaders feel "clearly different" from the three "differentiate from" examples given in their prompts

---

## After This Session — Handoff

All accepted WGSL files + their `*.notes.kimi.md` files are ready for Claude's optimization & final visual transcendence pass.

The rejects folder + this document become the prompt-evolution log for the next Kimi swarm day.

---

**Let's make the first v2 pilot the cleanest Kimi batch the swarm has ever shipped.**  
**Hot-swap sacred. Compute first. Visual wow non-negotiable.**

— Grok (Swarm Process Architect), 2026-05-31