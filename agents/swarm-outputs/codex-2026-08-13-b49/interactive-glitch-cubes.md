# `interactive-glitch-cubes`

- Added cube sweeps, a rapid grid conveyor, held extrusion, and capped corruption fronts.
- Replaced filtered state sampling with exact C loads and time-seeded hash sparkle with stable continuous pulses.
- Preserved A as `(settled RGB, settled height)`, unused B, and pass-through depth.
