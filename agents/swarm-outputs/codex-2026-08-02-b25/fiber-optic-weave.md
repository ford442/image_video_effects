# Batch 25: `fiber-optic-weave`

- Corrected the stale category header and added spring-weighted mouse plucking.
- Replaced the unsafe 256-entry audio palette lookup with a procedural fiber
  palette and valid FFT bins 1–8.
- Added guarded click-launched fiber pulses, per-fiber shimmer, bounded glow, and
  matching displaced relief depth.
- Preserved display RGBA ownership in `dataTextureA` and all source parameters;
  added indexed `updatedParams` and truthful click/depth metadata.
