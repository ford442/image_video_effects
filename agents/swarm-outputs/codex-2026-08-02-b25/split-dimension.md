# Batch 25: `split-dimension`

- Corrected the stale image-category header and added spring-weighted seam motion.
- Added guarded click fractures that locally bend the split, plus valid per-band
  FFT breakup and clamped glitch sample coordinates.
- Bounded highlight emission and made depth follow the chosen normal/glitch
  dimension with click-line relief.
- Preserved display RGBA ownership and all source parameters; added indexed
  `updatedParams` plus truthful click/depth metadata.
