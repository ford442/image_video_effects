- **Surprising behavior**: The Laplacian heat-diffusion melt spreads naturally around the cursor, but Voronoi cell boundaries act as thermal resistance—creating persistent frost veins that survive brief mouse passes and only fully clear under sustained heat.
- **Audio reactivity**: Bass accelerates freeze speed and directly nucleates new crystal seeds via thresholded hash; treble stochastically sparks crystal tips using `step(0.92, h2(...))` gated by crystal density and frost thickness.
- **Alpha semantics**: Alpha approaches 0.0 on untouched image regions (early-exit path emits `base.a * 0.1`), rises with luma in thin frost, and saturates toward `0.75 + crystal * 0.25 + caustic * 0.35` where thick crystal caustics dominate; depth modulates the blend so foreground frost is more opaque than background frost.
