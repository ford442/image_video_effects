# `motion-heatmap`

- Heat state now rises through exact-load advection with smooth thermal runners and capped click fronts.
- The saved color-shift control now rotates the heat palette; pointer hold and real audio remain live.
- Preserved A packing as `(heat, 0, 0, source luminance)` and left B unused.
