# `data-moshing-diffusion`

- Packed displacement state now uses exact-load advection with held-pointer swirl.
- Added smooth stream packets and capped click displacement fronts while sampling display color from the source.
- Preserved A packing as `(offset.x, offset.y, 0, alpha)` and left B unused.
