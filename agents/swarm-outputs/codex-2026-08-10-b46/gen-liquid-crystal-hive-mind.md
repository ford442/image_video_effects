# `gen-liquid-crystal-hive-mind`

- Replaced filtered state sampling with exact per-pixel state loads.
- Clicks inject bounded chemotactic fronts into the current hive pulse and membrane response.
- Preserved A exactly as `(bass, mids, treble, nextHivePulse)`; B remains unused.
