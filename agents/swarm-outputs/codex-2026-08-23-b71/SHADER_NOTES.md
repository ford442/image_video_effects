# Batch 71 ownership notes

- Mycelium retains display feedback and adds exact neighbor ridge detection so
  connected hyphae persist without converting its RGBA history into raw state.
- Cyclic Automaton retains its Greenberg-Hastings payload. Clicks now launch
  finite ignition fronts alongside spontaneous, audio, and drag-based sources.
- Cycloid Bloom and Plasma Mandalas retain their display-history ownership;
  Cycloid removes its last filtered C read, while Plasma guards every spring
  read before touching the six reserved floats.
- Quantum Silk Loom wires `weave_speed` into all scene time, uses exact display
  persistence, and derives alpha/depth from ray hits, glow, input depth, and
  click plucks instead of forcing opaque output.
- De Jong keeps raw temporal density and the 3D hero-tube channels untouched by
  ACES; click energy bends the splat plane and contributes to semantic alpha.
- Stained Glass keeps source sampling filterable for refraction but its A/C
  history exact. Held input increases optical power and clicks flex facets.
- DLA is rebuilt as persistent growth rather than a frame-local fBm image. Its
  A/C payload is raw deposit, depletion, oxidation age, and tip activity; ACES
  is applied exactly once and only to the display path.
- DMT fixes the pre-existing double-normalization of mouse coordinates, replaces
  filtered history with exact C loads, and writes meaningful escape depth.
- Dragon Curve keeps exact display feedback and adds finite fold shockwaves
  without changing its iterative curve construction or saved slider mappings.
