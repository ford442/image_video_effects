# Optical / Glass / Holographic Ten — Notes

Per shader: what was kept verbatim, feedback packing, and the native ideas visible in the diff.

| ID | Kept Verbatim | Ideas in WGSL Diff | Packing |
|---|---|---|---|
| `gen-holographic-lens-flare-matrix` | Grid cellular partition; blue noise offset; streak exp formula; angular star dispersion; 4 params (`intensity`, `speed`, `scale`, `mouse_influence`) | `ghost1`/`ghost2` conjugate optical reflection ghosts; `irisDiffraction` 6-blade hexagonal iris rays; `newtonColor` thin-film interference rings | Raw sim state in A/C: `persistentDensity`, `persistentStreak`, `persistentDistance`, `stateAlpha` |
| `gen-holographic-plasma-geode` | Rocky box/sphere SDF + cavity; 4-iteration KIFS crystal loop; volumetric plasma raymarch loop; cosine palette; 4 params (`plasma_intensity`, `crystal_density`, `holographic_hue`, `core_rotation_speed`) | `dispersedHolo` Cauchy facet dispersion & TIR glints; `arcEmission` dielectric breakdown plasma arcs; `agateBands` / `fineLine` chalcedony mineral growth banding | ACES display RGBA |
| `gen-holographic-rainbow-surface` | FBM surface heightfield; thin-film formula `2*n*d*cos(theta)`; spectral diffraction highlights; mouse wave tilt; 4 params (`intensity`, `speed`, `scale`, `color_shift`) | `marangoniColor` tangential swirl whorl advection; `drainage` vertical multilayer interference thickness gradient; `grooveSheen` anisotropic micro-groove diffraction grating | ACES display RGBA |
| `gen-holographic-data-core` | Domain repetition; node SDF (boxes + cylinders); volume diffraction (`volumetricInterference`); 60Hz flicker; 4 params (`nodeDensity`, `travelSpeed`, `dataPulseRate`, `glitchIntensity`) | `photonPulse` logic bus energy packets along cylinders; `cageWire` hexagonal quantum containment lattice cage; `moirePattern` depth parallax moiré fringe interference | ACES display RGBA |
| `gen-holographic-bismuth-core-reactor` | 4-iteration 90-degree KIFS bismuth fractal folding; orbiting micro-crystals; mouse rot3D orientation; 4 params (`intensity`, `speed`, `scale`, `mouseInfluence`) | `edgeGlint` hopper-step terrace edge diffraction glints; `multiOrderOxide` physical oxide thin-film interference; `containmentFlux` magnetic confinement field flux lines | ACES display RGBA |
| `chromatic-folds-bilateral` | Bilateral filter loop; `rgb2hsv` / `hsv2rgb` math; `foldHue`; 4 params (`fold_strength`, `pivot_hue`, `sat_scale`, `depth_influence`) | `foldR`/`foldG`/`foldB` multi-spectral split hue folding; `depthWeight` joint depth-bilateral edge preservation; `resonanceDisp` contour standing resonance waves | ACES display RGBA (exact integer `textureLoad` C, replacing filtered sample) |
| `aero-chromatics-prismatic` | Wind vector from pointer displacement; `cauchyIOR` and `wavelengthToRGB`; 4-band spectral sampling loop; 4 params (`wind`, `decay`, `chroma`, `source`) | `vortexShedding` aerodynamic curl-noise vortex plumes; `schlierenSample` velocity gradient refraction; `decayR`/`decayB` wavelength-differential feedback trails | ACES display RGBA (exact integer `textureLoad` C, replacing filtered sample) |
| `glass-shatter-morph` | Voronoi shard structure (`voronoi`); morphological min/max kernel; spring-damper cursor in `extraBuffer[133..138]`; 4 params (`shard_scale`, `displacement`, `morph_blend`, `edge_width`) | `microCracks` sub-cellular spiderweb fracture lines; `tirGlint` grazing-angle prismatic facet glints; `isochromaticFringe` photoelastic stress birefringence fringes | ACES display RGBA |
| `frosted-glass-lens-iridescence` | Multi-tap microfacet scattering; Beer-Lambert absorption; `thinFilmColor`; spring-damper cursor in `extraBuffer[133..138]`; 4 params (`frost_amount`, `lens_radius`, `edge_softness`, `film_ior`) | `dropRefract` microscopic condensation droplet beads; `wipeFactor` pointer-drag moisture wipe path; `bevelDispersion` lens bevel Cauchy prism dispersion | ACES display RGBA |
| `spec-prismatic-dispersion` | 4-band spectral sampling loop (`cauchyIOR`, `wavelengthToRGB`, `refractThroughSurface`); glass thickness absorption; spring cursor in `extraBuffer[133..138]`; 4 params (`glass_curvature`, `cauchy_b`, `glass_thickness`, `spectral_saturation`) | `causticColor` internal double-refraction caustic cusp rings; `arSheen` anti-reflective purplish-amber lens coating; `streakHighlight` astigmatic chromatic radial streaks | ACES display RGBA |

Springs and ripples: Kept only on shaders that already own the pointer as an interactive lens/shard mass (`glass-shatter-morph`, `frosted-glass-lens-iridescence`, `spec-prismatic-dispersion`). Spring-free on non-mass photographic/surface/geode shaders (`chromatic-folds-bilateral`, `gen-holographic-rainbow-surface`, `gen-holographic-plasma-geode`, `gen-holographic-data-core`, `gen-holographic-bismuth-core-reactor`, `aero-chromatics-prismatic`).
Saved preset `params` unchanged byte-exact; `updatedParams` aligned by index.
