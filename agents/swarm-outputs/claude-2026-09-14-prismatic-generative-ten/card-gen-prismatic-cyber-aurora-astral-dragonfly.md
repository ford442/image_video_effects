SHADER: gen-prismatic-cyber-aurora-astral-dragonfly
IDENTITY: 2D SDF biomechanical dragonfly (ellipse body, four mirrored flapping wings, segment tail) over a domain-warped curl-advected aurora with Worley sparkles, Voronoi wing veins, orbit-trap iridescence, plasma core, strange-attractor crystal particles, radial chromatic shift, video underlay and mouse halo.
KEEP VERBATIM: rot, hash21/22, valueNoise, fbm, domainWarp, voronoi, curlNoise, worleyLayers, sdEllipse, sdSegment, sdDragonfly, attractorParticles, chromaticShift; mouse.x body rotation + mouse halo; composite order; density/shell alpha core.
ADD (2 native ideas):
  1. Chitin wing-membrane thin-film interference — wingFrame() recovers span/chord coords of the nearest wing using sdDragonfly's own transforms; membrane thickness (~420 nm, thicker at root/leading edge, per cross-vein cell jitter, bass +40 nm) through OPD 2*n*d*cos(theta_t) (n=1.56) at 650/532/450 nm, incidence from the live flap angle, blended over the wing (mids raise mix).
  2. Longitudinal vein spars + pterostigma — costa/radius/media spars fanning from the wing root, thick costal rim, nodus cross-bar at ~46% span, and the dark pigmented pterostigma cell near the leading-edge tip (treble ember glint); darkness scales with Vein Density.
FLOOR FIXES: `audio = u.config.y` (click count) removed -> plasmaBuffer[0].xyz bass/mids/treble clamped with controlled gains (aurora, stroke depth, core, sparkles, particles, halo, feedback, chroma); Reinhard -> ACES; dataTextureA now receives the same ACES RGBA as writeTexture (was sim data d/v/bgNoise); dataTextureC textureLoad clamped; mouse-held (zoom_config.w) added: faster hover flap, brighter halo, extra chroma; click ripples added as wing-downwash gust rings (loop min(u32(config.y),50u)) that warp/brighten aurora; header replaced. No extraBuffer, no dataTextureB. JSON: 2 existing updatedParams + uniforms kept byte-exact; appended index 2 "Vein Density" (z, default 0.5 -> original 14.0 vein scale) and index 3 "Flap Rate" (w, default 0.5 -> original 18.0 flap speed) to updatedParams and uniforms; added params array (wingspan, plasmaIntensity, veinDensity, flapRate) and features.
FORBID: generic bloom/noise overlays, dataTextureB writes, extraBuffer use, changing existing slider defaults/ranges, fake audio from config/time.
A PACKING: ACES display RGBA in A
VERIFY: naga ok / gate ok / extraBuffer ok (AUDIT PASS) / sliders x (wingspan), y (plasma core), z (vein scale + spar darkness), w (flap rate) live
