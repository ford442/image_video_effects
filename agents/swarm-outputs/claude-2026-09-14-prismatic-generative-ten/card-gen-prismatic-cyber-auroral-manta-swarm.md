SHADER: gen-prismatic-cyber-auroral-manta-swarm
IDENTITY: Raymarched swarm of five SDF manta rays (ellipsoid body, swept tapered wings, whipping tail, smooth-unioned) orbiting under mouse gravity and cohesion, inside a 40-step volumetric simplex-FBM aurora band (indigo->cyan with magenta spikes), fresnel neon edges and SSS bloom.
KEEP VERBATIM: hash1/hash3, snoise, fbm, rotX/Y/Z, smin, sdf_manta body/wing/tail construction + spanwise flap, map swarm orbit/mouse pull/cohesion/velocity alignment, calcNormal, aurora volume march, manta sphere-trace, fresnel/SSS lighting, transmittance blend, gamma.
ADD (2 native ideas):
  1. Mobuliform chordwise travelling wave — in sdf_manta the pectoral disc also ripples leading->trailing edge (phase time*flap_speed*1.3 - z*3.2 - |x|*1.1), amplitude growing toward wingtips and with bass-driven audio_react.
  2. Manta countershading — nearestMantaLocal() recovers the hit manta's local frame (mirrors map pose); mantaSkin() gives near-black dorsal disc with pale shoulder chevrons (faint mids glow) and pale ventral surface with a per-individual hashed spot constellation (spot grid density follows Detail), lit by diffuse.
FLOOR FIXES: `audio = u.config.y` fake removed -> plasmaBuffer[0].xyz clamped (bass+mids -> flap/aurora turbulence/bloom, treble -> magenta spikes, bass exposure); Reinhard -> ACES (gamma kept); hardcoded alpha 1.0 -> manta coverage vs aurora optical depth (1-transmittance) + breach glow; dataTextureA write added (same RGBA); depth now written from hit distance (was 0); dead Detail (w) wired to aurora FBM octaves (default 0.5 -> original 4) and spot density; Speed (z) now time scale (default 0.3 -> original 0.5) and audio gain; mouse-held boosts swarm gravity pull x2.5; click ripples added as bioluminescent breach rings that thicken the aurora (loop min(u32(config.y),50u)); header + Uniforms comment replaced with slider names. No extraBuffer/dataTextureB/dataTextureC use. JSON: updatedParams untouched; params array (scale, intensity, speed, detail) + features added.
FORBID: generic bloom/noise overlays, dataTextureB writes, extraBuffer use, changing slider defaults/ranges, fake audio from config/time.
A PACKING: ACES display RGBA in A
VERIFY: naga ok / gate ok / extraBuffer ok (AUDIT PASS) / sliders x (cohesion), y (aurora intensity), z (speed/audio gain), w (aurora octaves + spot density) live
