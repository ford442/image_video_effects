# Codex (e) shader notes

- `liquid-jelly-fluid`: retained the already-premium Batch 59 velocity,
  pressure, and elastic-density solver after re-auditing its exact C loads,
  all-band audio, ACES, semantic alpha, and full interaction contract.
- `liquid-metal-prismatic`: retained its four persistent spectral bands,
  moving silver facets, held swirl, and chromatic click refraction; contract
  comments now identify the Codex (e) audit.
- `liquid-oil-iridescence`: retained its bounded film-thickness transport,
  capillary islands, pearl-black interference, and pointer thinning.
- `liquid-smear-structure`: replaced filtered C history with exact display
  history, bounded seven-tap LIC, source-gradient tensor tangents, held brush
  vortices, and traveling spectral fronts.
- `melting-oil-blackbody`: replaced B writeback with an A/C velocity,
  temperature, and molten-coverage state; added thermal diffusion, safe
  blackbody math, pointer heat, and radial click impulses.
- `honey-melt-blackbody`: upgraded static hex distortion into persistent
  high-viscosity thickness/temperature transport with Beer-Lambert absorption,
  gravity, held pull, and click heat.
- `viscous-drag-bilateral`: replaced filtered state reads and weight-as-alpha
  with exact displacement feedback, a bounded 5×5 bilateral kernel, coating
  state, glossy spectral response, and semantic source alpha.
- `hyper-tensor-fluid`: removed the generic clock-ring appendix and implemented
  an image-gradient eigentangent field, neighbor viscosity, curl-like noise,
  transported dye, held stirring, and bounded click waves.
- `sim-fluid-feedback-coupled`: now carries velocity, pressure, and density
  through a bounded projection/confinement step with all-band forcing and
  source-aware compositing.
- `ripple-tank`: preserved seven graph dispatches while converting every state
  handoff to A→C. The capillary pass now evolves local curvature and foam;
  render derives refraction, interference, specular, and caustics directly from
  the per-pixel wave field.
