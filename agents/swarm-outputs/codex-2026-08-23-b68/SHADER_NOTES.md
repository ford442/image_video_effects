# Batch 68 shader notes

- `cellular-automata-3d`: replaces fake config audio with plasma bands; a
  sprung camera target attracts the projected volume, click fronts loosen CA
  birth bounds, and exact-C projected display persistence is written to A.
- `elastic-chromatic-explosion`: retains per-channel EMA history while adding
  a guarded prism-focus spring, stronger finite shock fronts, semantic alpha,
  canonical ACES naming, and strict source-depth pass-through.
- `fire_smoke_volumetric`: A now explicitly carries raw smoke RGB and density;
  the state is semi-Lagrangian advected by exact C loads before sprung heat and
  bounded ignition fronts are injected.
- `glitch-ripple-drag`: all displaced history and chromatic taps are clamped
  integer loads; plasma motion, the sprung origin, and bounded ripple trains
  drive semantic display history in A.
- `ink-marbling`: A packs persistent pigment RGB and thickness. Exact-C state
  follows a marbling flow field, with live audio turbulence, sprung stirring,
  and timestamped ink drops; tone mapping never feeds back into raw state.
- `magnetic-luma-sort`: keeps the original pull spring but adds a guarded init
  flag, canonical plasma bands, read-only FFT `[5..132]` voices, exact history,
  ACES, and trail-aware alpha.
- `neural-synapse-web`: ripple count is no longer treated as delta time;
  spring state is compressed to `[133..138]`, clicks come from `u.ripples`, and
  A/C consistently carry display RGBA rather than mismatched field channels.
- `phase-memory-weave`: center and four neighbors use exact loads, racing
  per-pixel click persistence is removed, A alone owns `(psiR, psiI,
  slowMemory, activity)`, and output depth comes from the source.
- `prismatic-feedback-loop`: C is loaded exactly as raw accumulation; held
  pointer deformation follows a spring, click bursts remain bounded, A stays
  un-tonemapped, and ACES glow is output-only.
- `temporal-phosphor-burn`: binding 13 and its seven older frames remain; exact
  immediate C history, a sprung beam, and finite click burns feed a semantic
  ACES display-history A.
