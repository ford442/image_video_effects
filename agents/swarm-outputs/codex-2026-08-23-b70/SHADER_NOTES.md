# Batch 70 ownership notes

- Alpha Paint keeps signed fluid state raw while manually reconstructing exact-C
  backtraces; display color follows flow angle, dye, curl, and all audio bands.
- Chromatographic Fluid replaces its lone B interaction write with A, so force,
  advection, two diffusion iterations, interaction, phase, and render all hand
  off the same dye/temperature packing.
- Heat Haze replaces unsigned edge arithmetic with bounded integer loads and
  preserves temperature gradients and refractive activity in A.
- Both standalone slime simulations use exact trail sensors. EM Slime removes
  the `(0,0)` mouse-state corruption and uses the reserved six-float pointer
  window instead.
- Video Slime retains its video-food/drift packing and gains output ACES,
  aspect-correct hover/held input, and semantic source-aware alpha.
- Gray-Scott retains four solver iterations plus inject/render; audio changes
  feed/kill/diffusion and seeding without entering the raw U/V state as color.
- RK Advection manually reconstructs raw dye history after its RK4 backtrace and
  keeps ACES strictly output-only.
- Painterly Oil uses exact tone-mapped paint history as wet-memory while source
  convolution remains filterable; A stores display RGB plus thickness.
- Cyber Ripples retains its stronger prior upgrade. The cohort fixes its
  pre-existing abstract literal that current Naga rejected.
