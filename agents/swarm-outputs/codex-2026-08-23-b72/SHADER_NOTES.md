# Batch 72 ownership notes

- Phoenix retains a compact raw-state payload so its SDF and nebula memory can
  evolve before ACES. Its halo spring is guarded and confined to slots 133–137.
- Void Whale replaces its accidental ripple-count audio input, reconstructs
  finite click fronts, and now writes a depth-bearing ACES display history.
- Void Dragon preserves the raymarched body while replacing its display clamp
  with ACES and exact C persistence.
- Glass Flora derives alpha from ray coverage rather than forcing opaque output.
- Bonsai maps its four raw-range UI values correctly, moves bass smoothing from
  illegal slot 0 to guarded slot 133, and writes semantic display feedback.
- Fractal Coral now owns display history, assigns separate spectral roles, emits
  depth and semantic coverage, and responds to finite click propagation.
- Medusa wires the previously dead swimming-speed control into scene time and
  keeps its feedback alpha semantic rather than amplifying it above one.
- Silk Veil keeps source compositing separate from effect-layer feedback and
  replaces filtered history with exact C loads.
- Cellular Gardens removes legacy reads from engine FFT slots. Plasma bands now
  drive evolution and shimmer directly; raw trail/age feedback remains intact.
- Echo Chamber maps count, decay, spacing, and color shift to their named roles.
  Its multi-tap echo is manually reconstructed from exact texel loads, while the
  bass envelope uses guarded slot 133.
