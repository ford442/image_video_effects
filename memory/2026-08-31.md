# 2026-08-31

## Remote chrome + fullscreen hide + public-host gate

- Remote header has **Random Image** (`CMD_LOAD_RANDOM_IMAGE` already existed).
- Hide Controls also hides the titlebar (logo, random image, tabs). Overlay **Show Controls** on the canvas; `.main-container.fullscreen` is 100vh.
- `go.1ink.us` / `www.go.1ink.us`: no Open Remote, `?mode=remote` mounts MainApp. Test/localhost keep remote.
- No deploy to go. Mouse XY not touched. gpu-chores dirty files not mixed in.

## TS2322 useRemoteSync.test mock

- `handleNewRandomImage` / `loadDepthModel` are `() => Promise<void>` on `UseRemoteSyncOptions`.
- Fixed mocks: `jest.fn(async () => {})` + handler override type `() => Promise<void>`.

## sync_shaders_to_storage path fix

- Failure: run from `scripts/` → `Shader definitions directory not found: shader_definitions`
- Cause: paths were CWD-relative (`Path("shader_definitions")`)
- Fix: resolve `REPO_ROOT` from `__file__`, then `shader_definitions`, `public/shaders`, `.shader_sync_manifest.json`
- Works from repo root or `scripts/`
