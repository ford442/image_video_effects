# 2026-06-27 — WASM Phase 2: Build & CI hardening

## Done
- `build.sh`: fail without emcc unless `SKIP_WASM_BUILD=1`
- `npm run wasm:validate` script; validator checks public/wasm bridge sync too
- CI: wasm job uploads artifacts; test + test-wasm-e2e download + SKIP_WASM_BUILD
- CI: Jest WASM smoke tests in wasm job
- Canonical bridge: `wasm_renderer/wasm_bridge.js` only; copies to src/wasm + public/wasm
- Docs: `wasm_renderer/ARTIFACTS.md`, WASM_BUILD_CI_GUIDE, GAP_ANALYSIS, AGENTS.md

## Artifact strategy
CI-built, committed baseline in `public/wasm/` (~200KB). No Git LFS.
