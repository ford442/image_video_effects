# 2026-09-20

## Merge to local `main` (no push)

- On `feat/naga-wasm-validation` (`a4e4de19`), user asked to merge changes to main.
- Local `main` was `593e8ccf` (conflict-marker cleanup). Origin `main` was `b815fd7d` (#1291 Neon Plasma Chrono-Bloom) still carrying the dirty `62f2d677` markers.
- Merged origin into local main: kept local sentient/radiant cleanup; took unique Chrono-Bloom WGSL + JSON + completed queue entry. Only conflict was `reports/wgsl_precommit_report.json` (kept local 8/8 snapshot). Regenerated lists/search-index/README → catalog **1,370** (generative 475). Search-index 1,370/1,369 reused.
- Then merged `feat/naga-wasm-validation` (clean). GPU-less naga wasm + real `#include` expansion now on local main.
- Tips: `887f2aad` (origin union), `ee64841e` (feat merge). Local `main` ahead of origin by 5. Did not push.
- Gates: `verify:catalog-counts` passed; `naga-wasm:test` 15/15; `verify:wgsl-include` green. No conflict markers in WGSL/JSON. Real-GPU QA external.

## Batch pick — retro-glitch ten (second pass)

- User asked to pick a batch. Chose the **retro-glitch** family: 32/35 files have no `Upgraded:` header, and only 3 (`bayer-dither-interactive`, `halftone`, `neon-edges`) carry an `Ideas:` line. 17 of them are stamped "Composer batch cyber/digital/glitch" = the §4.3 generic overlay (spring cursor + held drag + capped ripples + ACES + three-band audio) with **no native ideas**. Per §9 these are "second pass — treat as not upgraded."
- Floor is already in place on all ten, so the whole batch is creative work, not hygiene. Do NOT re-stamp springs/ripples; several already have them and they are the problem, not the fix.
- **Claimed IDs (10):** crt-phosphor-decay, crt-magnet, vhs-tracking, signal-noise, byte-mosh, xerox-degrade, ascii-flow, pixelation-drift, spectrum-bleed, vinyl-scratch
- Each is a distinct failure mechanism (phosphor persistence / magnetic deflection / tape transport / composite noise / bitstream corruption / electrophotography / glyph raster / block raster / chroma diffusion / groove-stylus) so ideas can't collapse into one overlay. Watch the two CRT files — that's the only pairing at cloning risk.
- None collide with the 2026-05-23 Linear In-Progress list (`vhs-tracking-mouse` and `crt-clear-zone` are different shaders). Hits in `agents/swarm-outputs/phase-*` are historical scan registries, not live claims.
- `spectrum-bleed` JSON is missing the `upgraded-rgba` feature tag — only add it if the Idea Card actually lands.
- Batch folder when work starts: `agents/swarm-outputs/spark-2026-09-20-retro-glitch-ten/` (BRIEFS.md before any WGSL).
