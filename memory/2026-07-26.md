# 2026-07-26

## Batch 15 — Generative Upgrade Swarm (Edition 6) ✅

Ran the next 8-agent generative swarm following the Batches 10–14 pattern (briefs → parallel agents → gate → lists → Jest → tracker).

- **Pool scan:** 60 generative shaders remain with empty/missing `updatedParams` (52 after this batch).
- **Brief generator:** `temp/make_briefs_2026_07_26_b15.py` (fixed hard-coded `/root/...` ROOT → `/home/ftpbridge/image_video_effects`; added `controls[]`-schema support for void-spider). Briefs in `swarm-tasks/kimi-generative-briefs-2026-07-26-b15/`.
- **8 shaders upgraded (#151–158):** void-spider 131→221, psychedelic_spiral 142→199, liquid_crystal_birefringence 181→269, holo-fracture 183→267, quantum_field 184→244, fractal_dreams 186→276, supernova-core 187→275, lava-lamp-blobs 190→250. Notes in `swarm-outputs/kimi-generative-2026-07-26-b15/`.
- **Real bugs fixed:** void-spider dead audio (ripples[0].x as fake plasmaBuffer proxy); holo-fracture `zoom_config.xy` mouse bug (x=Time, 3rd occurrence of this bug class — B13/B14 had the others); liquid_crystal `audioPulse = zoom_config.w` (mouse-down mislabeled as audio); quantum_field double tonemap (Reinhard stacked under ACES) + dead `psi()`; fractal_dreams cdiv NaN guard.
- **Gate:** 8/8 green, 0 warnings (bindgroup + workgroup). ⚠️ **naga binary not installed in this VM** — naga validation deferred to CI (previous full-fleet naga scans ran on the other machine).
- **Lists/dupes:** `generate_shader_lists.js` clean; 1312 unique definitions (was 1310 at b14).
- **Jest:** 50 suites / 344 pass / 1 skip — matches baseline, no regressions.
- **Tracker:** `agents/weekly_upgrade_swarm.md` Batch 15 section added.

### Environment lessons (this VM)
- No `node`/`npm` on PATH: use emsdk's bundled node — `export PATH=/root/emsdk/node/22.16.0_64bit/bin:$PATH`. `npm install` took 27s (1430 packages, no sharp/ECONNRESET issue this time).
- No `naga` binary — gate skips naga step; do not claim "naga green" here.
- No GPU — shaders not visually exercised (per AGENTS.md).
- Not committed — working tree per user preference.

## Batch 16 — Generative Upgrade Swarm (Edition 7) ✅

Second swarm of the day, same pipeline. Pool: 52 → 44 remaining after this batch.

- **Uniform-semantics audit (important):** recon agent claimed `config.y` = delta_time per `docs/BINDING_CONTRACT.md` — that doc is **STALE**. Verified against `src/renderer/UniformBuffer.ts` + `webgpu/frame.ts:678`: `setConfig(time, rippleCount, resW, resH)` → **config.y = ripple count**. The `min(u32(u.config.y), 50u)` ripples guard used since Batch 12 is correct. Engine source > docs.
- **8 shaders upgraded (#159–166):** phoenix 144→218 (dead audio = ripple count fixed; only 2 controls — updatedParams mirrors 2), liquid_magnetic_ferro 190→240 (dead Viscosity wired to A→C temporal smoothing; NaN normalize guards), bio_lenia_continuous 192→266 (stuck spawn gate fixed; 2nd species in A.g), bioluminescent-bloom 194→259 (double ACES removed), chrono-voronoi-mycelium 194→253 (all-4-slider boilerplate evicted), cosmic-jellyfish 195→267 (dead temporal trails now displayed; depth clobber fixed), spec-quaternion-julia 195→246 (bg alpha ~100 clamped; Detail = real DE iters), tornado-vortex 200→256 (dead vRadial/vVertical physics activated).
- **Brief generator:** `temp/make_briefs_2026_07_26_b16.py` — now handles 3 JSON schemas (params[], controls[]+uniform, controls[]+uniformMapping) and 2-param shaders. Briefs: `swarm-tasks/kimi-generative-briefs-2026-07-26-b16/`; notes: `swarm-outputs/kimi-generative-2026-07-26-b16/`.
- **Gate:** 8/8 green, 0 warnings (naga still unavailable in VM → CI). extraBuffer audit: only [133..137] used. Lists/dupes clean (1312 defs). **Jest: 50 suites / 344 pass** — baseline held.
- Tracker updated (`agents/weekly_upgrade_swarm.md` Batch 16 section).

## Generative swarm tooling + pool closeout ✅

Implemented the full plan:

### Tooling
- `npm run audit:extrabuffer` / `audit:dead-sliders` (+ `:generative`)
- CI: blocking extraBuffer + dead-slider audits in `wgsl-precommit-gate`; audit unit tests; report uploads
- `wgsl_precommit_gate.py` runs baseline-aware extraBuffer scan per changed file
- Docs: `scripts/AUTHORING.md` swarm guardrail section; WGSL_BUILTINS callout
- `temp/make_briefs_common.py`, `scripts/batch_finalize_generative_pool.py`

### Content
- Batch 18: upgraded `interactive_neural_swarm`; finalized JSON for all 8 targets
- Batch pool closeout: **40 defs** got `updatedParams` via batch finalize (schemas: params, parameters, uniforms, controls)
- Phoenix: wired `param3`/`param4` (chronoMix, spinRate)
- **Generative empty-pool: 0** (excl. gen_capabilities/gen_grid)

### Catalog
- `audioReactiveBadge` util + ShaderBrowser `audio-ready` badge
- Lists: 1313 unique; Jest **59 suites / 397 pass**
- Dead-slider CI flipped to blocking; 7 new simulation multipass sliders baselined

- Diverged main: local +6, remote +3
- `git pull --rebase origin main` — conflict in `reports/wgsl_precommit_report.json` (resolved: kept local batch report)
- Rebase continued cleanly; `git push origin main` → up to date at e41ddfa2

## GPU timestamp readback (issue 2/2 of #1007) ✅

Implemented TS WebGPU GPU-timestamp path (depends on honesty gate; #1030 not merged so gate + `timestamp-query` feature request landed here too):

- `WebGPUTiming.ts`: kTs* 0–7 (WASM parity), period gate, 2-deep staging ring, resolve/copy, fire-and-forget `mapAsync`, `hasRealGpuTimings` honesty in `buildGPUTimings`
- `frame.ts`: phase tracker, `timestampWrites` on compute + blit, wall-clock fallback until real readback, resolve before submit
- `GraphRunner.ts`: optional boundary `timestampWrites`
- `device.ts`: request `timestamp-query` when adapter has it
- Jest: `WebGPUTiming.test.ts` — 51 suites / 360 pass / 1 skip; `verify:device-policy` green
- Not committed unless user asks

## Thumbnail Coverage Completion (plan implementation) ✅

Implemented tooling + UX for #921 follow-on; **347/1300 (26.7%)** coverage unchanged (GPU batch must run on discrete-GPU machine).

- **Quality:** `isErrorFrame` (black + magenta), skip allowlist `reports/thumbnail_skip_allowlist.json`, extended failure report + `thumbs:status` eligible %
- **Sim warmup:** auto 120 frames for simulation/multipass/graph registry shaders
- **UX:** `useThumbnailManifest`, category-colored `ShaderThumbPlaceholder`, ShaderGallery "Has preview" filter, ShaderBrowser placeholders + local manifest fallback, weighted attract/roulette pick (`thumbnailWeightedPick`)
- **Ops:** `scripts/run-thumbnail-waves.sh`, `scripts/audit_thumbnail_integrity.py` (flagged 89 near-black existing PNGs), weekly CI `.github/workflows/thumbnail-coverage-status.yml`
- **Bugfix:** `--limit` now applies after `--missing` filter (was taking first N catalog entries before filtering)
- **README + THUMBNAIL_PIPELINE.md** updated. Run waves: `SKIP_WASM_BUILD=1 npm run build && bash scripts/run-thumbnail-waves.sh`

## Thumbnail Coverage Completion (plan implementation) ✅

Implemented tooling + UX for #921 follow-on; **347/1300 (26.7%)** coverage unchanged (GPU batch must run on discrete-GPU machine).

- **Quality:** `isErrorFrame` (black + magenta), skip allowlist `reports/thumbnail_skip_allowlist.json`, extended failure report + `thumbs:status` eligible %
- **Sim warmup:** auto 120 frames for simulation/multipass/graph registry shaders
- **UX:** `useThumbnailManifest`, category-colored `ShaderThumbPlaceholder`, ShaderGallery "Has preview" filter, ShaderBrowser placeholders + local manifest fallback, weighted attract/roulette pick (`thumbnailWeightedPick`)
- **Ops:** `scripts/run-thumbnail-waves.sh`, `scripts/audit_thumbnail_integrity.py` (flagged 89 near-black existing PNGs), weekly CI `.github/workflows/thumbnail-coverage-status.yml`
- **Bugfix:** `--limit` now applies after `--missing` filter (was taking first N catalog entries before filtering)
- **README + THUMBNAIL_PIPELINE.md** updated. Run waves: `SKIP_WASM_BUILD=1 npm run build && bash scripts/run-thumbnail-waves.sh`

## WASM Tier B hygiene (#1013) ✅

Phase 1 (no GPU):
- `wasm_renderer/build.sh`: dedupe `-sGROWABLE_ARRAYBUFFERS=0`; docs updated for single `npm run build`
- Removed dormant `-sg.wgsl` probe (`pipeline.ts`); subgroup feature request kept for inline WGSL
- Bridge: `uploadImageData` zero-dimension guard; concat synced
- Tests: `WASMBridge.uniforms.test.ts`, `WASMRenderer.input.test.ts` loadImage + setInputSource mapping
- Jest WASM subset: 5 suites / 38 pass
- Evidence: `reports/wasm-promotion-evidence-2026-07-26.md` — **STAY TIER B** (gates 1–4 open; human GPU required)
- Build fixes (pre-existing): `CommunityGallery` ThumbnailManifest import, `ShaderMetadata` optional params/features, `audioReactiveBadge` types

- Part 1: `scripts/audit_extrabuffer.py` + `scripts/audit_dead_sliders.py` (+12 tests) — guardrails vs FFT-zone state writes & dead sliders; full-tree triage baselines committed (46 files/137 extrabuffer; 86 defs/205 dead sliders); fail forward-only.
- Part 2: batches 17 (8), 18 (8), 19 (gen_grid) — pool 43 → 0 (excl. gen_capabilities).
- Parallel track (another agent) mass-finalized ~27 defs mid-run + owns src WIP; their CommunityGallery Jest failure (2 tests) is NOT ours — logged in .swarm-state.md.
- Commits: 7f8e1d1d, a51eed5f, 818a21b3, b919bda6 + save-state.
- Jest final: 61 suites / 403 pass / 2 failed (their src WIP). Baseline was 50/344.
