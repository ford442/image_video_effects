# 2026-07-22 — Generative upgrade swarm (Batch 10, 8 agents)

## Setup
- User: "lets run a generative shader upgrade swarm"
- Pool scan: 99 generative shaders with empty/missing `updatedParams`. Picked 8 smallest
  never-in-any-batch (verified 0 tracker hits):
  fire_smoke_volumetric (117), multi-scale-evolutionary-cellular-gardens (124),
  recursive-ancestral-terrains (124), neural-synapse-web (126), magnetic-flux-garden (128),
  generative-psy-swirls (129), 4d-projection-dream-weavers (132),
  symbiotic-light-propagation-networks (133)
- KEY INSIGHT (vs Batch 9): all 8 already READ u.zoom_params.x–w in WGSL and declare 4
  JSON params — only `updatedParams` is missing. Theme = formalize updatedParams using
  EXISTING param ids/names/defaults (saved-preset contract, no renames), rewire any
  generic boilerplate mappings (e.g. applyGenerativePrimaryControls) to shader-specific
  constants, + 2–3 techniques each, expansion +50–90 lines.
- Roles: Visualist ×2 (fire_smoke, psy-swirls), Algorithmist ×3 (cellular-gardens,
  ancestral-terrains, 4d-dream-weavers), Interactivist ×2 (neural-synapse-web,
  light-propagation), Optimizer ×1 (magnetic-flux-garden)
- Briefs: swarm-tasks/kimi-generative-briefs-2026-07-22/ (generator: temp/make_briefs_2026_07_22.py)
- Notes: swarm-outputs/kimi-generative-2026-07-22/
- 8 coder subagents dispatched in background ~08:15 CEST; each self-validates with
  wgsl_precommit_gate.py until exit 0.

## Completion pass checklist
- [ ] Full gate on all 8 files
- [ ] JSON parse ×8 (updatedParams = 4, ids/defaults unchanged)
- [ ] node scripts/generate_shader_lists.js; node scripts/check_duplicates.js
- [ ] Jest: npx react-scripts test --watchAll=false --ci
- [ ] Batch 10 table in agents/weekly_upgrade_swarm.md
- [ ] No-GPU caveat: visual QA deferred to real hardware

## Batch 12 — COMPLETE ✅ (closed ~09:41 CEST)
- Gate 8/8 green. Line deltas: calligraphic-weave 160→215, bubble-chamber 163→229,
  turing-veins 165→244, molten-gold 165→234, phase-memory-weave 165→233,
  atmos_fog 166→233, mycelium 166→239, julia_set 169→254
- JSONs 8/8 contract-preserved; lists/dupes clean (1309); Jest 48 suites/332 pass
- Tracker: Batch 12 table (#127–134), Recently Completed now 134
- 🧟 turing-veins dead sliders wired; bubble-chamber z-param split + backwards Speed fixed
- 🔴 **FFT stomp zone discovery**: engine writes 128 FFT bins to extraBuffer[5..132] EVERY
  frame audio is active (src/hooks/useAudioAnalyzer.ts). 8 shaders from Batches 10–12 kept
  persistent state there + holographic-crystal wrote reserved [0]. ALL remapped to safe
  zone [133..255], re-gated 9/9 green. Index map now documented in
  agents/WGSL_BUILTINS_GENERATIVE.md — future briefs must say "[133..255]", not "[5]+".
  (neural-synapse-web's [133..141] choice in Batch 10 was already correct.)
- Generative pool: ~67 remain with empty updatedParams
- NOT committed — working tree per user preference
- Next 8: emergent-calligraphic-weave (160), bubble-chamber (163),
  generative-turing-veins (165), molten-gold (165), phase-memory-weave (165),
  atmos_volumetric_fog (166), mycelium-network (166), gen_julia_set (169)
- 🧟 turing-veins: 4 JSON params DEAD — WGSL never reads zoom_params; agent must wire
- bubble-chamber: zoom_params.z double-duty (turbulence AND spawn_rate) — agent cleans up
- calligraphic-weave: applyGenerativePrimaryControls boilerplate mapping — rewire to brush constants
- Roles: Visualist ×3 (calligraphic, molten-gold, atmos_fog), Algorithmist ×2
  (bubble-chamber, turing-veins), Optimizer ×2 (phase-memory-weave, julia_set),
  Interactivist ×1 (mycelium-network)
- Briefs: swarm-tasks/kimi-generative-briefs-2026-07-22-b12/ (generator temp/make_briefs_2026_07_22_b12.py)
- Notes: swarm-outputs/kimi-generative-2026-07-22-b12/

## Batch 11 — COMPLETE ✅ (closed ~09:20 CEST)
- Gate 8/8 green (naga + bindgroup, 0 warnings). Line deltas:
  cyclic_automaton 138→200, holo_interference 144→194, holo-crystal 148→219,
  wave_equation 149→215, plasma-orb 149→223, jet-stream 152→214,
  sacred-torus 155→219, acoustic-strings 157→239
- JSONs 8/8 contract-preserved; lists/dupes clean (1309 defs); Jest 48 suites/332 pass
- Tracker: Batch 11 table (#119–126), Recently Completed now 126
- 🐞 FIXED: gen_wave_equation feedback — sim state now in dataTextureA (+NaN-kill,
  9-pt Laplacian, click droplets, bass rain). State-layout change flagged for on-GPU check.
- Revived dead sliders: plasma-orb arcChaos, holo_interference depthWeight;
  deduped ACES in plasma-orb + holo_interference
- Generative pool: ~76 remain with empty updatedParams (84 - 8)
- NOT committed — working tree per user preference
- Next 8 smallest from generative pool (84 remain after Batch 10 + tracker exclusions):
  gen_cyclic_automaton (138), holographic_interference (144), holographic-crystal (148),
  gen_wave_equation (149), plasma-orb (149), plasma-jet-stream (152),
  sacred-geometry-torus (155), acoustic-string-theory (157)
- Skipped gen_capabilities (system-monitor demo, already "v3 Optimized" — poor fit)
- All 8 already have REAL slider mappings (not boilerplate) — theme stays:
  formalize updatedParams from existing params + 2–3 tailored techniques +50–90 lines
- 🐞 FOUND: gen_wave_equation feedback bug — solver reads (height,velocity) from
  dataTextureC.rg but writes finalColor to dataTextureA → feedback carries COLOR not
  state. Its agent must write sim state to dataTextureA (A→C copy wins).
- Roles: Algorithmist ×3 (cyclic_automaton, wave_equation, jet-stream),
  Visualist ×2 (holo_interference, sacred-torus), Optimizer ×2 (holo-crystal, plasma-orb),
  Interactivist ×1 (acoustic-string-theory)
- Briefs: swarm-tasks/kimi-generative-briefs-2026-07-22-b11/ (generator temp/make_briefs_2026_07_22_b11.py)
- Notes: swarm-outputs/kimi-generative-2026-07-22-b11/

## Status — COMPLETE ✅ (Batch 10 closed ~08:35 CEST)
- Gate 8/8 green (naga + bindgroup, 0 warnings). Line deltas:
  fire_smoke 117→181, cellular-gardens 124→174, ancestral-terrains 124→182,
  neural-synapse-web 126→205, magnetic-flux-garden 128→197, psy-swirls 129→189,
  4d-dream-weavers 132→221 (+89, biggest), light-propagation 133→185
- JSONs: 8/8 parse, exactly 4 updatedParams each (32 sliders wired), param ids/defaults
  verified unchanged (saved-preset contract), updated:true set
- generate_shader_lists.js clean (1309 defs), check_duplicates.js clean, Jest 48 suites/332 pass
- Tracker: Batch 10 table (#111–118), "Recently Completed" now 118
- NOTE: root `swarm-outputs`/`swarm-tasks` are SYMLINKS into agents/ — no duplication
- Generative pool: 99 candidates with empty updatedParams → 91 remain for future batches
- Notable agent wins: 4d-dream-weavers fixed a latent unused-`detail` bug; 8/8 gate-green
  on first pass (preamble + brief format working); fire_smoke's dataTextureA now carries
  clamped smoke state (feedback-semantics fix)
- QA flags per agent in swarm-outputs/kimi-generative-2026-07-22/*.notes.kimi.md
  (eyeballed constants, no-GPU caveat, one Q: does engine clear extraBuffer between frames?
  — light-propagation rising-edge tracking depends on it)
- NOT committed — working tree per user preference

## Progress audit + GitHub issues (Grok, afternoon)

User asked for project progress review, org/TS/C++/compile audit, next features vs foundation, vision, ~7 GH issues.

### Snapshot
- Shaders: ~1324 WGSL / ~1310 defs / thumbs **347/1310 (26.5%)**
- App foundation Wave 2 largely landed: App.tsx ~579, WebGPU modularized (device/resources/pipeline/frame), binding 13 + webgpu_limits.json SOT
- Multipass: GraphRunner wired; **only 2 graphs** in catalog (quantum-foam demo, wave-tank); ~28 linear chains
- Generative: ~79 empty updatedParams remain; Batches 10–12 done (FFT extraBuffer lesson)
- WASM: Tier B stay; bridge md5 synced; modular C++ present
- Open issues before: only #976, #983, #984 (many epics closed)
- CommunityGallery: **mounted** in AiVjStudioPanel (weekly_plan "unmounted" note stale)

### Findings worth tracking
- TS device.ts never requests `timestamp-query` (C++ does) → GPU timings always wall-clock on TS
- npm build runs wasm:build twice; package dup deps; root a.out.*/patches/upg.zip junk
- tsc bare fails on transformers d.ts (TS 4.9 vs modern types); skipLibCheck hides in CRA
- rgba32float fixed format = bandwidth tax; performancePolicy only scales resolution
- 0 `-sg.wgsl` subgroup variants (loader dormant)
- context.configure minimal on TS (works; can harden)

### Issues created
- #1007 timestamp-query + canvas configure
- #1008 format/resolution tiering
- #1009 multipass physics content on GraphRunner
- #1010 bundle/AI deps hygiene
- #1011 generative updatedParams + extraBuffer CI
- #1012 thumbs 27%→80%
- #1013 WASM Tier A go/no-go + build de-dupe

## Shader load blank / console 404+422 (afternoon)

User report (Edge): rate 422, `*-sg.wgsl` 404, `/play` 404 — shaders “fail to load or load blank”.

Root cause (live `storage.noahcohn.com` OpenAPI):
1. **Rate 422:** client sent FormData `{stars}`; live expects JSON `{rating:0–5}` (`ShaderRatingUpdate`). Verified: FormData→422, JSON→200.
2. **-sg 404:** pipeline probed optional subgroup variants; **0** `*-sg.wgsl` in repo → DevTools 404 spam (base `.wgsl` still 200).
3. **Play 404:** `/api/shaders/{id}/play` **not deployed** (only songs/preset-packs have `/play`). Generic FastAPI `Not Found`.

Fix branch `cursor/fix-shader-load-404-rate-422-167e`: `postShaderRating` helper, disable `-sg` probe, drop play POST, normalize `rating`→`stars`. Jest 339 pass.

Note: true black canvas on headless Cloud VM is still no-GPU / blocked media hosts — separate from these console errors.

## Image effects blank black after load (afternoon) — FIXED ✅

User: “image effect shaders load, but then the image goes to blank black. generative shaders are working.”

Root cause: default quality `auto` → adaptive resolution scale → `setResolutionScale` → `recreateScaleTextures` destroys `sourceTex`/`readTex` with **no re-upload**. Frame loop then copies empty black source every frame. Generative ignores source pixels → still works.

Fix PR #1018 (`cursor/fix-image-blank-after-scale-973f`):
- `restoreSourceFromOffscreen` after recreate (skip generative)
- restore on generative → image switch
- `uploadRGBA8` skips scaled `readTex` write (scalePass fills from source)
- Tests: `WebGPUMediaInput.test.ts`; full Jest 50 suites / 344 pass

