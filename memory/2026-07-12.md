# 2026-07-12

## StorageService + storage UI consolidation (#912)

Refactored monolithic `StorageService.ts` (~971 lines) into typed modules under `src/services/storage/`:

- `types.ts`, `http.ts`, `operations.ts`, `webhook.ts`, `shaders.ts`, `ratings.ts`, `assets.ts`, `client.ts`
- `StorageClient` is canonical; `StorageService.ts` is thin re-export facade
- Hand-written types (no Zod dep added)
- Aligned with `storage_manager/app.py`: `/api/health`, `/api/songs?type=image` for images, `/api/shaders/{id}/rate`
- Unit tests: `src/services/storage/__tests__/http.test.ts` (path encoding + error mapping)

UI consolidation:
- Canonical entry: `src/components/storage/StoragePanel.tsx`
- Compact controls: `StorageControlsPanel.tsx`
- `StorageBrowser.tsx`, `StorageControls.tsx`, `StorageUIComponents.tsx` → thin re-exports
- `useStorage` hook is single React surface; no public `any`

Docs:
- `docs/STORAGE_API.md` — OpenAPI-ish contract
- README section: local vs VPS storage

## Adaptive performance (mobile / iGPU)

- `src/config/performancePolicy.ts` + `wasm_renderer/performance_policy.h` — shared Battery/Balanced/Ultra/Auto presets
- `RendererManager.setRenderQuality()` + adaptive FPS scaler; WASM `setResolutionScale` via `resizeCanvas`
- UI: `RenderQualityPanel` in controls; `PerformanceStatusHUD` in status bar; localStorage via `renderQuality.ts`
- Slot cap enforced in `setMode` + `SlotStackPanel` hides slots above cap

Still open: `docs/SHADER_VARIANTS.md`, Jest tests for policy/persistence

## #858 authoring gates — blocking flip (2026-07-12)

- `wgsl_precommit_gate.py`: 2-arg `@workgroup_size` → `[ERROR]`, fails exit 1; `--fix` message in output
- `bindgroup_checker.check_workgroup_size_convention`: restored `< 3` arg rule
- `audit_orphan_shader_defs.py`: `--base` forward-only fail on changed `likely-broken` / `parse-error`
- `ci.yml`: naga-cli 30.0.0 pinned; orphan auditor `--base origin/main`; `test_authoring_gates.py` in CI
- `scripts/AUTHORING.md` updated (blocking rules, allowlist escape hatch)


## README documentation drift fix (#912)

- Rewrote README: manifest counts (1291 via `npm run build:manifest`), dual-renderer Tier A/B map, 5-min shader quick start, full scripts table, updated project structure, doc map, Cloud VM contributor notes
- `wasm_renderer/_HISTORICAL_ANALYSIS_README.md` + enhanced historical banners on `*_ANALYSIS.md`

## Depth + LLM stack hardening (#912)

- Dynamic `import()` for `@xenova/transformers` and `@mlc-ai/web-llm` (no static imports in `src/`)
- `src/services/depthEstimation/` — typed pipeline I/O, loader with WebGPU→WASM fallback, cancel, cache, offline UX
- `useDepthEstimation` — loadState, cancelLoadModel, inference never throws to renderer
- AutoDJ lazy-loads AI libs on `initialize()` only
- Tests: `src/services/depthEstimation/__tests__/types.test.ts` (5 tests); Jest 255/255 pass

## Multi-Agent Shader Upgrade Swarm — Batch 5 (16 shaders)

Dispatched 4 agents (Algorithmist, Visualist, Interactivist, Optimizer) to upgrade unclaimed small WGSL shaders using the Kimi swarm procedure.

**Selected shaders:**
- Algorithmist: `hyb-kaleidoscope-pulse`, `ambient-liquid`, `rain-ripples`, `complex-exponent-warp`
- Visualist: `neon-edge-radar`, `holographic-glitch`, `hyb-iridescent-fbm-glow`, `thermal-vision`
- Interactivist: `mouse-ink-bleed`, `interactive-voronoi-lens`, `cyber-rain`, `optical-feedback`
- Optimizer: `focal-pixelate`, `neon-edge-diffusion`, `radial-hex-lens`, `temporal-halation-freeze`

**Artifacts:**
- Briefs: `agents/swarm-tasks/kimi-briefs-2026-07-12/*.md`
- Notes: `agents/swarm-outputs/kimi-notes-2026-07-12/*.notes.kimi.md`
- Upgraded WGSL: `public/shaders/*.wgsl`
- Updated JSON: `shader_definitions/*/*.json`
- Updated tracker: `agents/weekly_upgrade_swarm.md` Batch 5

**Validation:**
- `naga` compiles all 16 shaders cleanly.
- `python3 scripts/wgsl_precommit_gate.py --files <16>`: 16/16 passed.
- `node scripts/generate_shader_lists.js`: passed.
- `node scripts/check_duplicates.js`: 1291 unique IDs, no duplicates.

**Fix applied post-dispatch:** `temporal-halation-freeze.wgsl` used reserved keyword `target`; renamed to `bassTarget`.

## Multi-Agent Shader Upgrade Swarm — Batch 5 RETRY

User asked for a retry because the first pass compacted code too aggressively. Re-dispatched the same 4 agents with stricter rules:
- Preserve original functionality from `git show HEAD`
- Add ≥3 distinct techniques per shader
- Final line count must be original HEAD +30 to +80 lines
- Keep canonical 13-binding header, `(16,16,1)`, semantic alpha

**Final line-count deltas (HEAD → retry):**
- Algorithmist: +55, +54, +60, +80
- Visualist: +39, +33, +62, +41
- Interactivist: +73, +77, +75, +80
- Optimizer: +75, +54, +69, +49

**Validation:**
- `python3 scripts/wgsl_precommit_gate.py --files <16>`: 16/16 passed
- `naga` SPIR-V generation: all 16 clean
- `node scripts/generate_shader_lists.js`: passed
- `node scripts/check_duplicates.js`: 1291 unique IDs

**Tracker updated:** `agents/weekly_upgrade_swarm.md` Batch 5 now includes line counts and retry-specific upgrade notes.

## Multi-Agent Generative Upgrade Swarm — Batch 6 (16 shaders)

Kimi generative swarm (`agents/swarm-tasks/kimi-generative-briefs-2026-07-12/`). Kimi stopped mid-batch with 15 WGSL edits, 8 notes, and 3 naga failures. Completion pass:

- **Finished:** `gen-sierpinski-tetrahedron` upgrade (+65 lines) — only shader Kimi had not edited.
- **Fixed naga:** `target` → `goal` in `gen-cybernetic-mycelium-neural-web`, `gen-worley-cellular-noise`; `p.yzx` → `p3.yzx` in `gen-showcase-nebula-core`.
- **Notes:** wrote 8 missing `agents/swarm-outputs/kimi-generative-2026-07-12/*.notes.kimi.md` files.
- **Validation:** 16/16 `wgsl_precommit_gate.py`; `generate_shader_lists.js` + `check_duplicates.js` clean (1291 IDs).

**Artifacts:**
- Briefs: `agents/swarm-tasks/kimi-generative-briefs-2026-07-12/*.md`
- Notes: `agents/swarm-outputs/kimi-generative-2026-07-12/*.notes.kimi.md`
- Tracker: `agents/weekly_upgrade_swarm.md` Batch 6

## Shader Upgrade Pass — Batch 7 (5 shaders)

Upgraded remaining small fireworks to Batch 6 optimizer standard, created new `gen-fireworks-dahlia-burst`, upgraded `phantom-lag`.

- **Fireworks:** `gen-fireworks-strobe-shell`, `gen-fireworks-crossette`, `gen-fireworks-comet-trail` — hex-bokeh, star field, GRAVITY, semantic alpha.
- **New:** `gen-fireworks-dahlia-burst` — flat dahlia-disk petal shells (distinct from spherical chrysanthemum).
- **Effect:** `phantom-lag` — spring-damper bass, chromatic multi-tap echoes, ACES, alpha preservation.
- **Validation:** 5/5 `wgsl_precommit_gate.py`; manifest 1292 IDs, no duplicates.

## New Generative Geometry Shaders — Batch 8 (4 shaders)

User-requested colorful geometry pass:

- `gen-rainbow-icosahedron-cascade` — nested icosahedral shells, spectral edges
- `gen-chromatic-zonohedron` — rhombic zonohedron facet rainbow tiling
- `gen-prismatic-mobius-helix` — iridescent Möbius helix ribbons
- `gen-neon-stellated-octahedron` — neon star tetrahedron, kaleidoscopic

All 4 pass naga + precommit gate; manifest 1296 IDs.
