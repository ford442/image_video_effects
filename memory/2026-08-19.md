## Session: Chrono-Kinetic + ESLint decision

User asked whether to repo-wide-fix ESLint (testing-library naming, no-node-access) after a sed pass corrupted ShaderCoordinateMenu.example.tsx and StorageIntegration.test.tsx, vs commit the generative shader and ignore unrelated src/ lint.

**Decision / recommendation:** Do not bulk-fix pre-existing ESLint. CI `lint` job is non-gating (`npx eslint ... || true`). AGENTS.md: lint noise is not a setup/build failure. Restore any sed-corrupted files from git; ship shader work only. Line-by-line disables on unrelated tests are a different campaign.

**This clone:** working tree clean on main. Chrono-Kinetic Fractal Engine exists as plan `shader_plans/2026-08-17_chrono-kinetic-fractal-engine.md` and pending entry; `public/shaders/gen-chrono-kinetic-fractal-engine.wgsl` is not in tree here.
