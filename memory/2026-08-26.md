# 2026-08-26 — Progress audit + next-issue board

## Balanced premium eight-shader upgrade requested

- Implement the literal single-pass cohort `magnetic-edge`, `cyber-lattice`,
  `contour-flow`, `dynamic-halftone`, `circular-pixelate`,
  `holographic-glitch`, `luminance-wind`, and `temporal_echo`.
- Preserve the 13-binding ABI, 16x16x1 workgroups, IDs/URLs, byte-exact saved
  `params`, depth semantics, A-authoritative/B-unused ownership, and read-only
  exact C history. Add aligned indexed `updatedParams` and truthful metadata.
- Distinct identity requirements: magnetic edge fields, cyber lattice traffic,
  tangent contour ribbons, print rosettes, hex lenslets, continuous holographic
  corruption, curl-wind advection, and depth-weighted orbital temporal echoes.
- All require real bass/mids/treble, four live sliders, pointer/held/click
  response, ACES presentation, bounded age-guarded click loops. Only magnetic
  and lattice may write persistent single-writer state in `[133..138]`.
- Snapshot the four dirty audit reports plus generated catalogs before running
  generators, restore unrelated drift, run focused Naga/schema gates, catalog/
  URL/uniform/type checks, Jest, and `SKIP_WASM_BUILD=1` production build.
- Cloud VM proof is structural; default composition/control/interaction/
  stability/performance acceptance remains a real-GPU handoff.

## Balanced premium eight-shader upgrade complete

- Implemented all eight literal targets with distinct magnetic flux, lattice
  traffic, contour ribbons, CMYK rosettes, hex lenslets, continuous holographic
  corruption, curl-wind, and orbital echo identities.
- Saved `params` compare exact 8/8. All definitions now have four aligned
  indexed `updatedParams` plus truthful feedback/state metadata.
- A is authoritative and B unwritten throughout. Magnetic Edge and Cyber
  Lattice alone use single-writer springs at `[133..138]`; Wind and Temporal
  store raw HDR A/C while the other six store display history.
- Proof: Naga/bind-group/workgroup 8/8; strict extraBuffer, dead-slider, and
  custom schema audits pass; duplicate, catalog gate, uniforms, TypeScript,
  Jest 84/84 (559 pass, 1 skip), and WASM-skipped production build pass.
- Fresh relative catalog regeneration passes URL policy. The pre-existing
  tracked catalog snapshot contains deploy-time absolute URLs, so final tracked
  output restores unrelated entries and retains only the eight target catalog
  changes; this baseline distinction is intentional.
- Real-GPU composition, controls, held/click feel, temporal stability, and
  performance remain the workstation handoff.

User asked for a repo progress check (errors, JS/TS/C++ usage, compile/context, foundation vs content) and 7 GitHub issues for the next code changes.

## Strategic call

**Build foundation residual before another content swarm.** Catalog is ~1,347 unified IDs (1,360 JSON defs, 1,382 WGSL). Volume is not the bottleneck. Closed #1123–#1129 / #1111 over-claimed: several "completed" tickets still have the original holes in tree. Only previously-open issue was #1080 (WASM real-GPU promotion evidence).

Leave alone: adapter ladder, `compatibleSurface=nullptr`, TimedWaitAny, canvas `alphaMode: 'opaque'` + preferred format, JS+C++ double-configure, feedback B→C then A→C, WASM Tier B until #1080, CRA+CRACO (Vite deferred).

## Snapshot

- Manifest 1,347; thumbs 353 (26.2%); 994 missing.
- Workgroups: 1,353×16×16, **26 leftover 8×8**.
- TS `parseWorkgroupSize` fallback **8×8**; C++ `ParseWorkgroupSize` fallback **16×16**.
- C++ `emptyTexture_` inherits `r32float` from depth `texDesc` then uploads 16 bytes of rgba — validation bug. TS writes one float correctly.
- `src/**/*.js` = 10 files, all WASM bridge. Dead CRA deps still in package.json (`react-app-rewired`, `customize-cra`, `webpack-cli`). No `verify:wasm-bridge-sync`.
- gpu-chores still preview-only. Audio mapper still slot-0 generative-only.
- extraBuffer: 0 new [0..132] writes, 93 known, 32 dynamic-index. Dead sliders: 73 known + new `hyper-space-jump-blackbody` / `chromatic_spread`.
- 22 underscore ids. README still says 1,291.
- Physics Lab attract pool still set 1 only. Set 2 ids exist as single-pass, not graphs.
- Compile: `build.sh` + `wasm_exports.json` SoT; CMake reads the JSON. Features array[3] with subgroups — #1123 really landed.
- Format probe no longer hardcodes true — #1124 probe + main packing landed; empty-tex leftover remains.

## Filed

1. #1179 WASM JS→TS + drop dead CRA deps
2. #1180 workgroup fallback + empty-placeholder packing
3. #1181 gpu-chores catalog auto-exposure
4. #1184 extraBuffer / dead sliders / catalog SoT
5. #1185 thumbs 26% → 50%+ deferrals
6. #1182 all-slot audio + OSC/WebMIDI VJ 2.0
7. #1183 naga-WASM + Physics Lab set 2 + GraphRunner C++ (gated #1080)

#1080 left open (needs a real GPU session).

## Git sync (pull/push)

- Diverged main: local `d62cdd34 push fix` (balanced premium eight) vs origin `f2a09c6f` (#1175 Synthetic Bismuth Geode Matrix plan + dead-slider auditor fixes).
- Rebased local onto origin; conflict only in `reports/dead_sliders_audit.{json,md}`.
- Regenerated dead-slider audit after rebase: PASS (1189 defs, 0 new, 30 known).
- Pushed `9ac855cc` to `origin/main`. Branch now even with remote.
