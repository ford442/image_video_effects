# 2026-09-23

## Foundation / progress audit (this session)

User asked: check project progress, next code changes, JS/TS/C++ organization, compile commands, context-creation options, whether to build foundation before more content, visualize later-on, **file 6 GitHub issues**.

### Where we actually are

- Catalog **1,370** (generative 475). Shader upgrade motion is high quality and **not** the bottleneck.
- Board before this audit: **#1080** (WASM GPU evidence), **#1182** (all-slot audio + OSC, `future`), **#1299** (JS never writes `plasmaBuffer`), **#1300** (`pow` NaN). Previous 6-packs (#1234–#1239, #1262–#1271) were **closed**; several closed empty (GraphRunner C++ never shipped; JSPI dual-binary not on `main`; `compile_commands.json` still missing; thumbs still 20.7%).
- Thumbs: **283 / 1369 healthy (20.7%)**, **1,069** deferrals all expire **2026-09-29**. CLIP search already exists.
- Leftover `Ideas:` headers: **861** WGSL files still have none (interactive-mouse 198, generative 216, advanced-hybrid 123). Second-pass backlog, not the next move.
- Compile: emsdk **6.0.9** pinned; ASYNCIFY-only; CMake is IDE-only; **no compile_commands.json**.
- Context: TS + C++ dual configure is healthy (`opaque` + `RENDER_ATTACHMENT` + preferred format; C++ second pass Fifo + size). COPY_SRC / Display P3 are TS opt-in; WASM still render-only.
- JS writes `extraBuffer`, never `plasmaBuffer`. C++ already uploads `vec4(bass,mid,treble,0)`.
- JS/TS/C++ split is mostly right: bridge SoT is `src/wasm/bridge/*.ts` (zero leftover `.js` under `src/`); C++ is modular (`device`/`resources`/`pipeline`/`frame`/`audio_depth`). GraphRunner + gpu-chores remain TS-only (correct under feature freeze).
- Worktrees already holding the next engine slices: `feat/sim-ring-group1` (`/root/iv-simring`), `feat/wasm-jspi-dual-build` (`/root/iv-jspi`). Encode-parity worktree is 28 behind — do not merge as-is.

### Verdict

**Build foundation before more 10-wide content.** First real code change is already-open **#1299**. Then the new six. GraphRunner C++ stays gated on **#1080**. CRA→Vite stays deferred.

### Filed (this session)

- **#1307** history-ring wrap vs probed 8/4/1 layers (7 leftovers)
- **#1308** bind group 1 sim ring — land the sim-ring worktree
- **#1309** dual JSPI artifact + compile_commands.json / clangd
- **#1310** WASM COPY_SRC + WebCodecs + rebuild 6-slot artifacts
- **#1312** 50% healthy thumbs + rescue physarum / navier-stokes-dye / multi-turing / lenia
- **#1313** later-on: catalog `#include` + GraphRunner node compositor (`@xyflow/react`) + native Dawn/wgpu fork

### Sibling issues filed the same morning (other session)

- **#1311** foundation sweep (lifecycle leaks, WASM bridge/C++ correctness, real lint gate, repo-root cleanup)
- **#1314** Engine 2.0 (off-main-thread worker, single-submit frame graph, zero-copy video, per-pass profiler)

Those complement, they do not replace, this set. Play order still starts at #1299.
