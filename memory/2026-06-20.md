# 2026-06-20 — WASM Phase 1 glue (RendererManager forwarding)

## Done
- Normalized `ShaderSlotRenderer` + `SlotZoomParamsUpdate` in `Renderer.ts`
- `RendererManager`: duck-typed `getShaderBackend()`, unified forwarding for `loadShader`/`loadShaders`/`setSlotShader`/`setSlotParams`/`updateSlotParams`/`setSlotMode`/`addRipple`
- Added `syncAllSlotParams`, `resyncShaderStack`, `supportsShaderEffects`, `addRipplePoint` alias
- `WebGPURenderer`: `setSlotParams` + `updateSlotParams(..., slotIndex)` API parity (slot 0 only for uniforms)
- `WebGPUCanvas`: sync all 3 slot params (WASM multi-slot); ripple via `addRipplePoint`
- `App.tsx`: resync shader stack on renderer switch; refs for modes/slotParams/inputSource
- Tests: `src/__tests__/RendererManager.test.ts`

## Done (continued — input source Phase 1)
- C++ `SetInputSource`: clears readTexture on generative/none; `UpdateVideoFrame` gated to video/webcam
- `ClearReadTexture()` helper in renderer.cpp
- WASMRenderer: tracks inputSource, conditional updateVideoFrame, getInputSource()
- RendererManager.render() uploads WASM video frames each animation tick
- WebGPUCanvas: managerReady gate + init-time setInputSource/setVideo/syncAllSlotParams
- wasm_bridge: pending input source applied after init
- Tests: WASMRenderer.input.test.ts, RendererManager setInputSource/render tests

## Done (Phase 3 — WASM feature parity)
- C++: `SetAudioFrequencyBins`, `GetSlotShaderId`/`GetSlotEnabled`/`GetSlotMode`, `GetGPUTimings` (CPU wall-clock), `GetSupportsDeepWorkgroup`, `SetRecording`/`IsRecording`
- Emscripten exports in `main.cpp` + `build.sh` EXPORTED list
- `wasm_bridge.js`: `updateAudioFrequencyBins`, aggregate `updateSlotParams` (partial merge), `getSlotState`, `getGPUTimings`, `getSupportsDeepWorkgroup`, `setRecording`, `captureFrameDataUrl`; default export updated
- `WASMRenderer.ts` + `RendererManager.ts`: parity methods including `getFrameImage`/`refreshFrameImage`
- `App.tsx`: `setRecording` wired in recording handlers
- Tests: `WASMRenderer.parity.test.ts` (plain-fn mocks — `jest.fn().mockReturnValue` broken in factory), `WASMBridge.test.ts` REQUIRED_EXPORTS updated
- WASM binary rebuild blocked locally: emcc 3.1.53 rejects `--use-port=emdawnwebgpu` (needs newer emsdk)

## Done (emsdk upgrade 2026-06-21)
- `./build.sh` succeeds with emcc 6.0.1-git + emdawnwebgpu v20260423
- Artifacts: `public/wasm/pixelocity_wasm.{js,wasm}` (~77K / ~121K), bridge copied to `src/wasm/`
