# 2026-08-09

- Completed shader upgrade Batch 41, tracker #364–371, continuing FAST MOTION ENCORE.
- Eight WGSL files gained closed-form/history-advected motion, fireworks mouseUV
  repairs, missing A/depth/audio wiring on Nebula and Neural Lace, bounded
  textureLoad HDR trails, and removal of spurious dataTextureB writes on three
  fireworks shaders while preserving every saved control entry.
- Added truthful JSON metadata, regenerated catalogs, and wrote Batch 41 briefs,
  per-shader notes, and coordinator review.
- Structural verification: gate 8/8, extraBuffer audit PASS, updatedParams 8/8
  exact, duplicates 1,326/1,326, Jest 76/76 suites (506 pass / 1 skip),
  supplemental production build green. Real-GPU visual QA remains external.
- ~49 generative shaders remain in the clean pending pool (57 − 8).

- **#1077 — shader-list base-url hygiene:** `generate_shader_lists.js` accepts
  `SHADER_LIST_BASE_URL` / `REACT_APP_SHADER_BASE_URL`; local prestart/prebuild stay
  relative; `verify:shader-list-urls` + manifest gate block accidental absolute URLs;
  `deploy:full` sets CDN base for deploy builds.

- Completed shader upgrade Batch 40, tracker #356–363, continuing FAST MOTION ENCORE.
- Eight WGSL files gained closed-form/history-advected motion, real plasma audio,
  bounded click loops, visible A/C trails, and generated depth while preserving
  every saved control entry and leaving B/`extraBuffer` ownership untouched.
- Added truthful JSON metadata, regenerated the 424-entry generative catalog,
  and wrote Batch 40 briefs, per-shader notes, coordinator review, and tracker entry.
- Structural verification is green. Full Jest/build expose only the pre-existing
  WASM bridge refactor blockers; an ESLint-disabled supplemental production build passes.
- Discrete-GPU visual acceptance remains outstanding by environment design.
