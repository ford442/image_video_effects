# 2026-09-05

## #1200 gpu-chores workgroup clamp (option 1)

- Live error: `gpu-chores-reduce` `DispatchWorkgroups(65536, 1, 1)` from flattened `2048² / 64`.
- Fix: `src/gpuChores/dispatch.ts` `workgroups2d` clamps X and Y. Host caches `maxWorkgroups` on attach. Hist/reduce/gain/lut/downsample all use 2D 8×8. `workgroups1d` exists as a flatten safety net only — reduce does not call it.
- Jest `src/gpuChores/gpuChores.test.ts` 31/31. HOLD go.1ink.us /pixelocity/.

## WASM runtime invariants (un-regressable gates, not re-fixes)

- Contract: `src/contracts/wasm_runtime_invariants.json`
- Verifier: `scripts/verify-device-policy-sync.js` + `scripts/wasm_import_table.js`; `npm run verify:wasm-invariants`
- All five chores dispatches: `assertDispatchWithinLimits` after `workgroups2d`
- Playwright: uncapturederror + JS→WASM rebind; skip when `webgpuProbe.ok` is false
- Evidence: `reports/wasm_promotion_evidence.md` — all six **GPU-PENDING**
- Did not touch committed `wasm_renderer/**` C++ or `public/wasm/*` (temporary bite-proofs reverted)
- Did not close issues. HOLD go.1ink.us

## wgpuSurfacePresent skip (verify, no rebuild)

- Pascal/Chrome abort from 30 Aug: emdawn stub `wgpuSurfacePresent is unsupported`.
- C++ already correct: `PresentToSurface()` in `wasm_renderer/device.cpp` blit-submits and returns; only comment mentions the symbol (comments-stripped scan: no live call). JS rAF in `WASMRenderer.startRenderLoop` owns the next `updateUniforms` ccall. Did not add html5.h / `emscripten_set_main_loop`.
- `public/wasm/pixelocity_wasm.wasm` (31 Aug 05:07) imports `wgpuSurfaceConfigure` + `wgpuSurfaceGetCurrentTexture` only — no `wgpuSurfacePresent` / `_wgpuSurfacePresent`. Glue JS has no present string.
- Rebuild skipped (plan: only if missing or still importing present). emcc 6.0.3 is on this VM if a later C++ change needs it.
- Gates: `verify:wasm-invariants` + `verify:wasm-bridge-sync` green. GPU confirm still Pascal/Chrome after `test.1ink.us` deploy + hard-reload.

## 30 Aug remote chrome QA (verify only)

- Observed on `test.1ink.us /pixelocity/` (30 Aug 2026): remote **does work**. Gaps then: no Random Image on remote; Hide Controls left random-image + titlebar. Mouse XY reverse not re-confirmed — omitted.
- Current `main` already has both chrome fixes (logged 2026-08-31): `RemoteControlHeader` + `CMD_LOAD_RANDOM_IMAGE`; `AppShell` `chromeHidden` unmounts header (logo, Random Image, tabs) and overlays Show Controls; `.main-container.fullscreen` is 100vh.
- HOLD: no `go.1ink.us` / `www.go.1ink.us` remote (`publicHost` gate). No product edits this pass.
- Focused Jest 4/4 suites, 14/14 tests: `AppShell.test`, `RemoteControlHeader.test`, `publicHost.test`, `useRemoteSync.test`.
