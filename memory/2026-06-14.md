## GH Issue Implementation Instructions (2026-06-14)
- User asked to turn proposal-only issues into actionable tasks (Claude had left implementation sketches in comments, not bodies).
- Updated #817–#823 bodies with structured sections: Prerequisites, Implementation Instructions (numbered steps + code), Task Checklist, AC, Testing.
- Recommended PR order embedded: #821 → #818+#820 → #817+#819 → #822 → #823.
- #799 is closed (2026-06-14); child issues #817-823 carry the concrete C++ work forward.

## Shader Upgrade Swarm — Batches 3, 4, 5 Complete (2026-06-14)
- **Batch 3** (generative): 12 shaders upgraded and validated. Summary at `agents/swarm-outputs/batch_2026_06_14_generative_upgrade_summary.md`.
- **Batch 4** (effect/interactive): 12 shaders upgraded and validated. Summary at `agents/swarm-outputs/batch_2026_06_14_effect_upgrade_summary.md`.
- **Batch 5** (effect/interactive): 12 shaders upgraded and validated. Summary at `agents/swarm-outputs/batch_2026_06_14_batch5_upgrade_summary.md`.
- Queue `agents/swarm-tasks/upgrade-queue.json` updated to v5.0; all 36 shaders marked `validated`; phase A complete (36/36).
- Progress logged in `agents/swarm-outputs/upgrade-progress.json`.
- Validation: 36/36 Naga passes, generate_shader_lists.js passes, duplicate check passes (1136 unique IDs).
- Manual fixes during validation:
  - `data-slicer-interactive` (Batch 4): reserved keyword `active` → `isActive`.
  - `mosaic-reveal` (Batch 5): missing comma in JSON after `description`.
  - `polar-warp-interactive` (Batch 5): fixed `vec4` composition with `vec2 mouse` (packed as `mouse.x, mouse.y`).
  - `chrono-slit-scan` (Batch 5): reserved keyword `active` → `isActive`.

## Shader Upgrade Swarm — Batch 6 Complete (2026-06-14)
- **Batch 6** (effect/interactive): 12 shaders upgraded and validated.
  - `scanline-wave`, `quantum-ripples`, `oscilloscope-overlay`, `spectral-brush`, `magnetic-interference`, `voxel-grid`, `polka-dot-reveal`, `scanline-sorting`, `neon-cursor-trace`, `directional-glitch`, `stereoscopic-3d`, `cyber-ripples`
- Summary written to `agents/swarm-outputs/batch-6-summary.md`.
- Queue updated to **v6.0**: 48 total items, 48 `validated`, 0 pending.
- Progress logged in `agents/swarm-outputs/upgrade-progress.json`.
- Validation: 12/12 Naga passes, `generate_shader_lists.js` passes, duplicate check passes (1136 unique IDs), `bindgroup_checker.py` shows no Batch 6 shader in the 6 pre-existing incompatible entries.
- Incident: `directional-glitch` initial dispatch failed with HTTP 429; retry succeeded after credits refreshed.
- No manual WGSL/JSON fixes were required for this batch.
