# 2026-07-04

## WASM Epic Tracking (#885)

- User provided full epic body for remaining C++ WASM work (post #845–#849).
- Confirmed umbrella **#885** + sub-issues **#886–#890** already exist (created 2026-07-03).
- Updated #885: added sub-issue table, July 4 code audit snapshot, cross-links.
- Code audit highlights:
  - Good: `getShaderBackend()` duck-typing, `resyncShaderStack`, `setInputSource` wired in App/WebGPUCanvas.
  - Open: App `captureStream` recording (#886), `loadImage` instanceof branches (#887), WASM GPU timings wall-only (#888), bench evidence (#889).

## Issue map

| # | Title |
|---|-------|
| 885 | Epic umbrella |
| 886 | Recording/screenshots bypass — **fixed in code** (readback recording + App delegation) |
| 887 | Forwarding + special-case audit — **fixed in code** |
| 888 | Feature parity (FFT, timings) — **largely complete** (GPU timestamps documented as wall-clock) |
| 889 | Playwright + benchmarks — **implemented** (smoke, parity, bench JSON, CI jobs; GPU suite skips headless) |
| 890 | Docs + promotion tracking — **implemented** (WASM_PROMOTION_TRACKING.md + doc refresh) |

## #890 implementation (2026-07-04)
- `WASM_PROMOTION_TRACKING.md` — four promotion gates with evidence requirements + append-only log
- `WASM_RENDERER_GAP_ANALYSIS.md` — July 2026 refresh; §3 historical; #886–#889 marked done
- `wasm_renderer/STATUS.md` — rewritten; no "Phase 3 complete / production ready" claims
- `README.md`, `WASM_TESTING.md`, `WASM_BACKEND_POLICY.md`, `wasm_renderer/README.md` — Tier B consistency
- `fix_renderer_cpp.py` → `wasm_renderer/archive/` with README

## #889 implementation (2026-07-04)
- `tests/helpers/rendererHarness.ts`: shared static server, strict/soft GPU modes (`WASM_GPU_TESTS=1`), benchmark report writer, `index.html` guard.
- `tests/wasm-renderer.smoke.spec.ts`: 5-shader matrix + multi-slot; soft vs strict backend assertions.
- `tests/wasm-benchmark.spec.ts`: FPS/frame-time comparison → `test-results/wasm-benchmark-report.json`.
- `tests/renderer-parity.spec.ts`: uses `isStrictGpuMode()`.
- `package.json`: `test:wasm:e2e`, `test:wasm:gpu`, `test:wasm:bench`, `test:wasm:full`.
- CI: `test-wasm-e2e` + `wasm-gpu-manual` workflow_dispatch; benchmark artifact upload.
- `WASM_TEST_SUITE.md` rewritten as contributor guide.
- Fixed `WebGPURenderer.getAudioData()` return type (build blocker for e2e).

## #886 implementation (2026-07-04)
- `wasm_bridge.js`: `startRecording` uses GPU readback → offscreen canvas → `captureStream` when WASM initialized.
- `RendererManager`: `startRecording`, `stopRendererRecording`, `usesInternalRecording()`.
- `App.tsx`: WASM path delegates to manager; WebGPU path unchanged (`canvas.captureStream`).
- Screenshots already via `manager.takeScreenshot` (internal readback on WASM).

## #887 implementation (2026-07-04)
- `RendererManager`: duck-typed `loadImage`, `reloadShader`, `getActiveRendererType` (metrics-based), `firePlasma`, `getAudioData`, `getAvailableModes`; removed `instanceof WebGPURenderer`/`WASMRenderer` dispatch branches.
- `WASMRenderer.loadImage()` added (parity with WebGPU).
- `WebGPUCanvas`: `shaderCatalog` prop from App `availableModes`; removed dead `onImageDimensions` + `getAvailableModes`/`as any` firePlasma; uses `manager.firePlasma`.
- `App.tsx`: direct manager API calls (`loadImage`, `setImageList`, `updateDepthMap`, `setInputSource`).
- Tests expanded in `RendererManager.test.ts` + `WebGPUCanvas.test.tsx`.

## #888 implementation (2026-07-04)
- Verified C++ already implements full FFT (`SetAudioFrequencyBins` → `extraBuffer[5..132]` in `UpdateUniformBuffer`).
- Added `GPUTimings.timingSource` (`gpu-timestamp` | `wall-clock` | `unavailable`); WASM returns wall-clock ms with `available:false`.
- `WASMRenderer.getAudioData()` caches bass/mid/treble + 128 FFT bins (parity with WebGPU).
- `setRecordingMode` / `isRecording()` / `getRecordingMode()` on WASMRenderer.
- `RendererManager.getAudioData()` + `isRecording()` duck-typed for both backends.
- Tests expanded in parity + RendererManager + WASMBridge.
