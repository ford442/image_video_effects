# 2026-07-11

## Shader scan fix (95 errors → code fixes)

User exported shader scan report: 1301 shaders, 95 errors.

### Root causes
1. **91 × 404 fetch** — Scanner fetched only `test.1ink.us`; 88 shaders exist locally + in storage API but not on CDN. 3 junk test API entries have no WGSL.
2. **4 × subgroup compile** — `-sg` variants (`pp-sharpen-sg`, etc.) need `subgroups` device feature; scanner used plain `requestDevice()`.
3. **3 test junk** — `test`, `test-shader-123`, `test-real-shader-456` in storage API with no code.

### Fixes shipped (branch `cursor/fix-shader-scan-errors-182d`)
- `ShaderScanner`: `fetchShaderWgsl` fallback chain; `requestPixelocityDevice` with subgroups; skip `-sg` when unsupported.
- `shaderApi`: filter junk test entries from API list.
- `scripts/shader_scan_fix_list.txt` (88 IDs) + `sync_shaders_to_1ink.py --ids-file` for CDN upload when credentials available.

### Remaining manual step
Run `SHADER_SYNC_PASSWORD=... python scripts/sync_shaders_to_1ink.py --ids-file scripts/shader_scan_fix_list.txt` to put 88 WGSL files on test.1ink.us (optional once scanner fallback is deployed).

## Session: PR #936 from radiant-seraph product work

- Extracted MIDI/controlBindings + WASM parity from radiant-seraph onto clean branch `feat/midi-control-and-wasm-parity` from latest main (3-way merge for conflict-risk files).
- PR: https://github.com/ford442/image_video_effects/pull/936
- 217 unit tests pass.
- Deleted stale Jules remotes (jules-168…, jules-794…).
- Remaining remotes: main, feat/midi-control-and-wasm-parity, radiant-seraph-plan-...

## Issue triage (open GH issues)

Reviewed all open issues on ford442/image_video_effects (24 open). Epic #912 children are generally agent-ready. Notes:
- #898 effectively DONE (owner completed checklist) — should close; #897 epic needs checkbox refresh
- Low-effort winners: #919, #918, #931
- Needs GPU hardware (not Cloud VM): #921, #925, #927, #933 partially
- Expand most: #929, #928, #923, #933 (product/architecture ambiguity)
- Agent fit notes saved in session response

## Cursor-agent assigned: #912 Wave 1

- Model: composer-2.5-fast
- Worktree: /root/.cursor/worktrees/image_video_effects/foundation-912
- Branch: chore/foundation-912 (from origin/main)
- PID: see /tmp/cursor-agent-logs/912-wave1.pid
- Issues: #919, #918, #931
- Prompt: /tmp/cursor-agent-912-wave1-prompt.txt

## Cursor-agent Wave 2: #912

- Model: composer-2.5
- Worktree: /root/.cursor/worktrees/image_video_effects/foundation-912-w2
- Branch: chore/foundation-912-w2
- Issues: #915, #917 (+ optional #920 tsconfig)
- Orphan precompute: /tmp/cursor-agent-912-wave2-orphans.json
- PID: /tmp/cursor-agent-logs/912-wave2.pid

## Shader catalog hygiene (#912)

- Extended `audit_orphan_shader_defs.py`: bidirectional audit, CI fails on only_def / only_wgsl
- Seeded 84 orphan WGSL → `shader_definitions/*.json` via `seed_orphan_shader_defs.py`
- `new_shader.py` now creates JSON + WGSL pair
- `docs/SHADER_TEMPLATES.md` documents `_` prefix / multipass / -sg policy
- Audit: only_def=0, only_wgsl=0; unified manifest 1291 shaders

- Split `wasm_renderer/renderer.cpp` monolith into:
  - `device.cpp`, `resources.cpp`, `pipeline.cpp`, `frame.cpp`, `audio_depth.cpp`, `wasm_internal.cpp/h`
  - `renderer.cpp` — lifecycle facade only
- `build.sh` + `CMakeLists.txt` updated; compile-flag notes in build.sh
- `CreateDevice()` now requests `Float32Filterable` when adapter supports it
- `wasm_renderer/README.md` file map + flag review table
- emcc build + 36 Jest tests pass (WASM bridge + webgpuDevicePolicy)

- Added `src/renderer/webgpuDevicePolicy.ts` mirroring C++ `CreateDevice()` (~307–550):
  - 4-step adapter ladder, `requiredLimits` for 13-binding compute contract, adapter limit validation
  - Cross-link comment in `wasm_renderer/renderer.cpp`
- `WebGPURenderer.init()` uses policy module; `getDiagnostics()` surfaces `lastInitError` / `adapterSummary`
- `requestPixelocityDevice.ts` updated with same `requiredLimits`
- Unit tests: `src/renderer/webgpuDevicePolicy.test.ts` (7 tests pass)

- Model: composer-2.5
- Worktree: foundation-912-w3 / branch chore/foundation-912-w3
- Issues: #913 App hooks, #914 Controls panels, RendererSwitcher canonical
- Wave 2 DONE: 80215b20 #915, 444a342a #917, 02a707c4 #920 ES2020
- Wave 1: 3 commits for #919 #918 #931 (may still be dirty/finishing)

## Foundation #912 three waves COMPLETE (local, not pushed)

### Wave 1 chore/foundation-912
- 7f41ec5a #919 single wasm:build
- 4e6d2bcf #918 legacy lists
- 4f7c52f2 #931 README
- Dirty leftover: regenerated shader-lists + wasm artifacts (build side-effects)

### Wave 2 chore/foundation-912-w2
- 80215b20 #915 requiredLimits + adapter fallback
- 444a342a #917 catalog gate + orphan deferred
- 02a707c4 #920 ES2020

### Wave 3 chore/foundation-912-w3
- b40c84e3 #913 hooks (depth, audio, share)
- f3e8cb98 #914 panels + RendererSwitcher
- c4a42d19 APP_STRUCTURE.md + ES2020
- App 2397→2076, Controls 1834→1663, Jest 180/180

Next: push + open PRs when user says; optional Wave 4 for remaining App/Controls extractions.

## Wave 3 PR opened
- Branch chore/foundation-912-w3 pushed
- PR #938 https://github.com/ford442/image_video_effects/pull/938

## Epic #913 App.tsx refactor (2026-07-11 afternoon session)

Cherry-picked + conflict-resolved hook extraction onto `feat/midi-control-and-wasm-parity`:
- `useDepthEstimation`, `useAudioReactiveParams`, `useShareChain` in `src/hooks/`
- App 2479→2159 LOC; WASM recording preserved via `finishRecordingBlob` → `openRecordingShareModal`
- `docs/APP_STRUCTURE.md` added
- Jest 217/217 green
- Staged, not committed (user rule: commit only on request)


## Legacy shader-list merge (#912 / #918 on feat/midi-control-and-wasm-parity)

- Removed stale `public/shader-lists/interactive.json` (38) and `liquid.json` (3); all IDs already in canonical `interactive-mouse` / `liquid-effects` / `distortion` (radial-rgb).
- `build-unified-manifest.ts`: explicit 14-file allowlist (no directory scan).
- `generate_shader_lists.js`: deletes stale list JSONs not matching `shader_definitions/` folders.
- `shaderCatalog.ts`, `ShaderValidator.tsx`: legacy files dropped; `hybrid.json` in catalog.
- README: canonical category table, 1,291 shaders / 14 categories.
- Validation: manifest 0 duplicate warnings; `check_duplicates.js` 1291 unique.

## Single wasm:build on production build (#912 / #919)

- `package.json`: removed trailing `wasm:build` from `build` script; `prebuild` runs it once before `craco build`.
- `WASM_BUILD_CI_GUIDE.md`: full production path, CI job split, `wasm:validate` table.
- `README.md` + `AGENTS.md`: document single compile path and `SKIP_WASM_BUILD=1`.

## TypeScript hygiene (#912 / #920)

- `tsconfig.json`: `target` raised from `es5` → `ES2020` (strict unchanged).
- `IRenderer` canonical interface in `Renderer.ts`; `WebGPURenderer`, `WASMRenderer`, `JSRenderer` implement it.
- `WASMDiagnostics` / `WebGPUDiagnostics` centralized; `RendererManager` duck casts removed.
- `ShaderEntry.requiresDeepWorkgroup` used in App (no `as any`).
- `DepthEstimatorPipeline` narrow type in `useDepthEstimation.ts`.
- `src/` clean under `tsc --noEmit`; node_modules transformers d.ts still noisy (pre-existing).

User pasted full epic body. Current workspace branch: `feat/midi-control-and-wasm-parity` (PR #936 MIDI/WASM parity) — NOT foundation branches. Foundation waves 1–2 still local-only; wave 3 is PR #938.

## Epic: Advanced Physics & Multipass Visual Simulations (2026-07-11 evening)

User pasted epic — priority: ripple tank → fabric → caustics → stretch goals.

### Agent kit created
- `swarm-tasks/advanced-physics/` (mirrors `agents/swarm-tasks/advanced-physics/`)
  - README.md — tier gate (A/B/C), acceptance checklist, launch template
  - MULTIPASS_SIM_CONTRACT.md — 13-binding ping-pong on A/B/C, linear multipass OK, graph runner blocked
  - agent-1-ripple-tank.md, agent-2-fabric-of-reality.md, agent-3-photonic-caustics.md, STRETCH_GOALS.md
- `docs/plans/PLAN-ADVANCED-EFFECTS.md` — cross-links to agent kit

### Multipass readiness assessment
- **Tier B OK now:** linear `resolveMultipassChain()` + frame feedback `dataTexA→dataTexC`
- **Tier C blocked:** Jacobi×N, cloth 5-iter solver, 2M photon batches — needs graph runner (#929)
- Existing prototypes: `wave-equation` (single-pass), `photonic-caustics` (32 photons), no `fabric-of-reality`

### wave-equation quick polish (Tier A)
- JSON: zoom_params mappings, audio-reactive feature, showcase blurb
- WGSL: bass from plasmaBuffer boosts sourceStrength

## WASM Tier B → A evidence collection (2026-07-11 evening)

User task: measurement only, not more glue. Policy: WASM_BACKEND_POLICY.md + WASM_PROMOTION_TRACKING.md.

### Decision: **STAY TIER B**
- Gate 1 Perf: no GPU bench (`wasm-benchmark-report.json` stub only)
- Gate 2 Reliability: parity not run on ≥2 GPU configs; CI skips without adapter
- Gate 3 Integration: manual Controls smoke unsigned
- Gate 4 Ops: 4 consecutive green weeks NOT MET (Jun failures; Jul 10–11 green partial week)

### Verified
- `npm run test:wasm:unit` → 29/29 pass
- CI run 29148209376: wasm + test-wasm-e2e green; 13 Playwright tests skipped (no WebGPU)

### Artifacts
- `WASM_PROMOTION_TRACKING.md` (was referenced but missing from tree)
- `reports/wasm-promotion-evidence-2026-07-11.md`
- `test-results/wasm-benchmark-report.json` (stub)

### Blockers for full evidence
- Cloud VM: no GPU adapter
- Local branch build fails TS2352 in webgpuDevicePolicy.test.ts — use main or fix before Playwright locally

**Success criteria status (on main / pre-foundation):**
| Criterion | Issue | Status |
|-----------|-------|--------|
| App/Controls split | #913, #914 | Wave 3 PR #938 (partial: ~300 LOC shaved each) |
| TS requiredLimits | #915 | Wave 2 local branch only |
| Orphan WGSL policy | #917 | Wave 2: gate + deferred list, not full reconcile |
| Thumbnail plan | #921 | Open — needs GPU |
| Double wasm:build | #919 | Wave 1 local branch only |
| README 1200+ | #931 | Wave 1 local branch only |

**Recommended merge order:** W1 → W2 → W3 (stacked PRs), then land #936 MIDI parity separately or rebase after foundation.
