# 2026-06-16 — WASM docs refresh (#823)

## Done
- Refreshed WASM docs to match code-verified state (June 2026):
  - Presentation **implemented** (`Render()` → `PresentToSurface()` renderer.cpp:1725)
  - Blocker moved from "missing present" to init/format/limits handshake (hardened #817–#822)
- Updated: `WASM_RENDERER_GAP_ANALYSIS.md`, `wasm_renderer/STATUS.md`, `wasm_renderer/README.md`, `WASM_BUILD_CI_GUIDE.md`, `WASM_SMOKE_TEST.md`, `WASM_TESTING.md`
- Added C++ Solidification Tracking tables with links to #799 roadmap comment and #817–#823
- Caveated "Phase 3 complete" — means compute+present, not full E2E usability
- Fixed WASM_BUILD_CI_GUIDE: local `build.sh` still exit 0 without emcc (CI is hardened)

## Code state verified
- `getPreferredCanvasFormat`, `requiredLimits`, fatal surface, Shutdown on partial init in renderer.cpp
- Bridge sync: `wasm_renderer/wasm_bridge.js` == `src/wasm/wasm_bridge.js`

## Still open (docs accurately reflect)
- RendererManager WASM forwarding
- setInputSource app wiring
- Live-browser edge GPU verification
