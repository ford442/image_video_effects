# 2026-09-21

## Shipped: retro-glitch ten (second-pass upgrades)

Batch picked yesterday, implemented today. Folder:
`agents/swarm-outputs/spark-2026-09-21-retro-glitch-ten/` (BRIEFS → NOTES → COORDINATOR_REVIEW).

**Why it counted as an upgrade:** all ten already had the plumbing floor, and seven were stamped
`// … Composer batch cyber/digital/glitch` whose header is §4.3's generic overlay verbatim. Per §9
that is "not upgraded". So the whole batch was creative work — **zero springs, zero ripple loops,
zero IQ palettes added.**

**IDs and the ideas actually added (3 each, 30 distinct, none repeated across files):**
- crt-phosphor-decay: two-rate phosphor knee; triad-aligned grain bleed; interlaced field parity
- crt-magnet: tangential convergence error (gun ring, not one axis); warped shadow-mask moiré;
  purity stain erased by the degauss ring
- vhs-tracking: head-switch skew/flagging; dropout-compensator line repeat; line-alternate chroma phase
- signal-noise: luma-shouldered noise; real quantisation staircase; dot crawl on vertical luma edges
- byte-mosh: motion-vector inheritance; keyframe recovery flash; row desync trail
- xerox-degrade: toner starvation bands; Mach-band edge halo; compounding generation loss through C
- ascii-flow: ink-coverage glyph ramp; coverage-weighted glyph blend; typed-cell wake
- pixelation-drift: block area average; block colour quantisation; drift-lit tile bevel
- spectrum-bleed: wavelength-ordered bleed distance; chromatographic advance front; dry-edge rim
- vinyl-scratch: eccentric spindle wow; radius-dependent groove pitch + label; stylus scratch marks

**Three files had an inert mechanism that an idea finally gave a job to:** crt-magnet's degauss ring,
vinyl-scratch's clicks, xerox-degrade's C history (was a flat 12% blend doing nothing).

**Dead code found and fixed en route (stated, not silent):** `crt-phosphor-decay`'s triad mask used
`fract(uv.x * resX)`, which is identically 0.5 for every texel — the "subpixel mask" was a flat
tint. Now phases off `coord.x / 3.0`. **This is the largest visual delta in the batch; eyeball it
first on a real GPU.**

**JSON:** only change in `shader_definitions/` is one line — `upgraded-rgba` added to
`spectrum-bleed` features. All saved `params` byte-exact; every preset still loads.

**Gates:** precommit 10/10 · naga `--all` 1380 valid / 40 invalid, **40 known, 0 new** ·
extraBuffer PASS · dead-sliders PASS (10 defs scanned, all four zoom_params live in all ten) ·
`generate_shader_lists.js` + `verify:catalog-counts` (1370) · `verify:wgsl-include` green ·
`SKIP_WASM_BUILD=1 npm run build` OK.

**Jest: 712 pass / 6 fail — pre-existing.** Verified by stashing the batch and re-running: byte-for-byte
the same 4 suites / 6 tests fail on a clean tree. All are WASM-bridge `./bridge/api.js` ESM
resolution, nothing to do with shaders. (Note: AGENTS.md still says "~125 tests, all pass" — stale,
the suite is 719 tests now.)

**Real-GPU visual QA: NOT done.** No Vulkan ICD on this VM. Five specific things to look at are
listed at the bottom of COORDINATOR_REVIEW.md.

**Not committed / not pushed** — left in the working tree for review.
