# 2026-09-21

## Shipped: retro-glitch ten (second-pass upgrades)

Batch picked yesterday, implemented today. Folder:
`agents/swarm-outputs/spark-2026-09-21-retro-glitch-ten/` (BRIEFS → NOTES → COORDINATOR_REVIEW).

**Why it counted as an upgrade:** all ten already had the plumbing floor, and seven were stamped
`// … Composer batch cyber/digital/glitch` whose header is §4.3's generic overlay verbatim. Per §9
that is "not upgraded". So the whole batch was creative work — **zero springs, zero ripple loops,
zero IQ palettes added.**

**IDs and the ideas actually added (3 each, 30 distinct, none repeated across files):**
- crt-phosphor-decay: two-rate phosphor knee; triad-aligned grain bleed; interlaced field parity
- crt-magnet: tangential convergence error (gun ring, not one axis); warped shadow-mask moiré;
  purity stain erased by the degauss ring
- vhs-tracking: head-switch skew/flagging; dropout-compensator line repeat; line-alternate chroma phase
- signal-noise: luma-shouldered noise; real quantisation staircase; dot crawl on vertical luma edges
- byte-mosh: motion-vector inheritance; keyframe recovery flash; row desync trail
- xerox-degrade: toner starvation bands; Mach-band edge halo; compounding generation loss through C
- ascii-flow: ink-coverage glyph ramp; coverage-weighted glyph blend; typed-cell wake
- pixelation-drift: block area average; block colour quantisation; drift-lit tile bevel
- spectrum-bleed: wavelength-ordered bleed distance; chromatographic advance front; dry-edge rim
- vinyl-scratch: eccentric spindle wow; radius-dependent groove pitch + label; stylus scratch marks

**Three files had an inert mechanism that an idea finally gave a job to:** crt-magnet's degauss ring,
vinyl-scratch's clicks, xerox-degrade's C history (was a flat 12% blend doing nothing).

**Dead code found and fixed en route (stated, not silent):** `crt-phosphor-decay`'s triad mask used
`fract(uv.x * resX)`, which is identically 0.5 for every texel — the "subpixel mask" was a flat
tint. Now phases off `coord.x / 3.0`. **This is the largest visual delta in the batch; eyeball it
first on a real GPU.**

**JSON:** only change in `shader_definitions/` is one line — `upgraded-rgba` added to
`spectrum-bleed` features. All saved `params` byte-exact; every preset still loads.

**Gates:** precommit 10/10 · naga `--all` 1380 valid / 40 invalid, **40 known, 0 new** ·
extraBuffer PASS · dead-sliders PASS (10 defs scanned, all four zoom_params live in all ten) ·
`generate_shader_lists.js` + `verify:catalog-counts` (1370) · `verify:wgsl-include` green ·
`SKIP_WASM_BUILD=1 npm run build` OK.

**Jest: 712 pass / 6 fail — pre-existing.** Verified by stashing the batch and re-running: byte-for-byte
the same 4 suites / 6 tests fail on a clean tree. All are WASM-bridge `./bridge/api.js` ESM
resolution, nothing to do with shaders. (Note: AGENTS.md still says "~125 tests, all pass" — stale,
the suite is 719 tests now.)

**Real-GPU visual QA: NOT done.** No Vulkan ICD on this VM. Five specific things to look at are
listed at the bottom of COORDINATOR_REVIEW.md.

**Not committed / not pushed** — left in the working tree for review.

## Shipped: liquid eight (second batch today)

Folder: `agents/swarm-outputs/spark-2026-09-21-liquid-eight/`. `liquid-effects` had 30/31 files with no
`Ideas:` line. Read 12 candidates in full; excluded 4 after reading (glass-wipes had Batch 67 named
ideas without a header, liquid-metal and rain-ripples were already idea-dense, liquid-v1 is the
intentional clean baseline). Skipped all multipass files — A packing is a contract between passes.

**8 shaders, 2 ideas each, 16 distinct:**
- liquid-rainbow-prismatic: gravity drainage + Newton's black film; dispersed caustics
- luma-velocity-melt: drip pinch-off into beaded columns; depth-ledge pooling
- liquid: capillary precursor ring (anomalous dispersion); foam drains into troughs
- kimi_liquid_glass: total internal reflection on steep flanks; rising seed bubbles
- liquid-oil: displacement wake (pointer now parts oil instead of lifting it); shear-aligned film streaks
- liquid-mirror: glitter path; energy-roughened reflection (B's first visible job)
- ink-marbling: Jaffer's area-preserving drop map (replaces HEAD's ring push); comb rake
- liquid-displacement: vorticity confinement (8 extra C loads); flow-line streaks

Four of these share one solver template (liquid / kimi / oil / mirror); each got physics only its own
claim supports. Caught one bug in my own draft before gating: luma-velocity-melt's history gate
dropped persistence exactly where the beads slow the flow — added `pileHold`.

`upgraded-rgba` added to 5 JSON files (one line each; params asserted equal in the edit script).
Gates: precommit 8/8; naga --all 0 new; extraBuffer + dead-sliders (8 scanned) PASS; catalog 1370;
include green; build OK; Jest same 4 pre-existing WASM-bridge suites.

GPU checks that matter most: liquid-displacement at turbulence=1 (confinement is positive feedback);
liquid-mirror glint density at rest.

## Shipped: post-processing seven (third batch today)

Folder: `agents/swarm-outputs/spark-2026-09-21-post-processing-seven/`. 7, not padded to 8.
- pp-sharpen: anti-halo overshoot clamp; luminance-only sharpening (modes 0/1; mode 2 untouched)
- pp-vignette: cos^4 natural falloff; grain rides the emulsion (midtone response, under the vignette)
- pp-chromatic: longitudinal CA across the focal plane; purple fringing on blown highlights
- temporal-slit-scan: sub-frame layer interpolation; slit exposure integration
- optical-flow-tracer: Shi-Tomasi confidence; pyramidal coarse-to-fine LK
- temporal-frequency-decomposition: Hann-windowed DFT; DFT phase -> glow hue
- spatio-temporal-3d-conv: motion-adaptive temporal weights (NR only — card corrected mid-build);
  variance-driven NR strength

**Found: history-ring depth bug in all 11 binding-13 shaders.** Renderer wraps the ring head at the
ALLOCATED layer count (8/4/1 after VRAM probe; frame.ts:109); every shader wrapped at hardcoded 8.
On 4-layer devices frame ages come back scrambled (WGSL clamps the array index). Fixed in the 4
temporal files in this batch; the other 7 listed in NOTES as a follow-up.

Also flagged, not changed: the temporal files carry `upgraded-rgba` without ACES (pre-existing drift),
and pp-sharpen mode 2 is still the contract's neon "reject" example (kept for presets).
Gates all green; Jest same 4 pre-existing suites. Pushed to main.

## Shipped: distortion lens eight (fourth batch today)

Folder: `agents/swarm-outputs/spark-2026-09-21-distortion-lens-eight/`. `distortion` had 43/63 files
with no `Ideas:` line. Read 10 in full. Excluded spiral-lens: it's over-stuffed, and most of its layers
are gated on silent plasmaBuffer audio, so it needs a subtractive pass.

**8 shaders, 18 ideas, none repeated:**
- gravity-well: lensed far-side disk halo over the shadow; Keplerian shear streaks (Ω ∝ r^-1.5, bounded winding)
- black-hole: frame-dragging swirl; photon-ring secondary (point-reflected) image
- heat-haze-gpt52: inferior mirage below a ground line; boundary-layer haze boost; held-pointer mirage pool
- zoom-burst: highlight comets (luma-weighted taps); zoom-ring dwell ghost
- interactive-zoom-blur: depth-occluded taps; radial trail advection (+ floor: exact C load)
- infinite-zoom-lens: log-periodic Droste nesting; endless fall on Zoom Strength; frame seams
- bubble-lens: inverted rear-wall reflection by sphere-incidence Schlick (IOR); Marangoni vortices
- refraction-tunnel: glass-pipe wall reflection; perspective (1/r) hoops/helix (+ floor: Twist slider
  never rotated the sampled image; now does, time spin excluded so default = HEAD)

Card corrections logged in BRIEFS (refraction-tunnel ×3, bubble-lens ×1). Zero JSON changes.
Flagged, unchanged: black-hole premultiplies by alpha (~0.2–0.5), which darkens the frame. black-hole,
heat-haze and interactive-zoom-blur carry upgraded-rgba without ACES.
Gates: precommit 8/8; extraBuffer + dead-sliders PASS; catalog, include green; build OK; Jest 712/6
(the known bridge/api.js suites + an untracked slotLimits test from a concurrent session).
**Not committed**: a concurrent session has uncommitted WASM/renderer edits in the same tree, so stage
only the 8 WGSL files, the 4 reports and the batch folder.

## Shipped: stateful-simulation six (fifth batch today)

Folder: `agents/swarm-outputs/spark-2026-09-21-sim-six/`. Aimed at 8 sims from artistic + simulation.
**Ported update rules to numpy to test dynamics without a GPU**; the harnesses are in the folder.
That pulled two carded files and changed two ideas before any WGSL.

**6 shaders, 12 ideas:**
- boids: blind-spot vision cone; held pointer = predator (flee + sideways dodge)
- ion-stream: charge split around the magnet; cyclotron tightening of the helix
- sim-ink-diffusion-rgba: Bousseau edge darkening; granulation (+ floor: depth was never written)
- steamy-glass: runoff rivulets (the runoff channel was never displayed); beaded wipe edge
- sim-fluid-feedback-coupled: buoyant dye; schlieren from the never-shown pressure field
- photonic-caustics: Jacobian 1/|det J| caustics; cursor glint (+ floor: A was display incl. photo
  read back as irradiance, so the photo fed itself into the light every frame)

**Rescue list (broken at runtime, NOT upgraded):** physarum (extraBuffer agents), navier-stokes-dye
(A overwritten, plasmaBuffer palette), multi-turing (unstable GS → checkerboard noise; stable version
is uniform at default params), lenia (stores thresholded state → frozen stamps).
JSON: upgraded-rgba on 3 files. Gates green; Jest same known failures. Not committed.

## GraphRunner C++ port (#1270) — gate check, NOT started
- User pasted #1270 body. Gate = #1080 Promote. #1080 still OPEN; last written decision STAY TIER B
  (2026-08-01), no GPU evidence since. → Did not start, per the issue's own first line + policy §1a.
- #1270 was closed COMPLETED 2026-09-21 04:53 UTC but nothing shipped (no graph_plan.h,
  graph_runner.cpp, setGraphPlan). Third closed tracking issue for the port (#1183, #1238, #1270).
- Stale in issue text: MAX_SHADER_SLOTS=3 is main only; feat/wasm-slot-limit-contract has 6 (uncommitted).
  "dataB→dataC then dataA→dataC" is the END-OF-FRAME feedback copy (outside the graph,
  MULTIPASS_GRAPH.md:110); intra-graph barriers in multipassGraph.ts emit one copy per read.

## Shipped: artistic surface eight (sixth batch today)

Folder: `agents/swarm-outputs/spark-2026-09-21-artistic-surface-eight/`. Theme: ink, paper and surface
materials. Rejected after reading: anisotropic-kuwahara (JSON runs the -tensor graph), ink_dispersion_alpha
(decompiled, JSON/params describe another effect — rescue list), circular-pixelate, spirograph-reveal.

**8 shaders, 18 ideas** (see MEMORY.md entry). Three runtime bugs found by reading:
frosty-window never ran (zero C → permanent early return), static-reveal's reveal threshold was mouse X,
polka-dot jitter was per pixel. melting-oil never accumulated (C used only for the gradient).
numpy check of the frost front in `frost.py`. Card corrections logged at the foot of BRIEFS.
**Not committed** — tree still has the uncommitted optical-eight / sim-six / WASM work.
- Reopened #1270 with a correction comment (user approved): gate status, nothing shipped, slot count 3→6, copy-order is end-of-frame not in-graph.

## Pushed to origin/main (explicit request)

- Fast-forwarded local `main` `77c48f27` → `398f0071` (`feat/wasm-slot-limit-contract`, message `push fix`).
- Pushed `origin/main` `77c48f27..398f0071`. No unique origin work to union. No `<<<<<<<` markers in the tree.
- In that commit: WASM slot-limit contract (6 slots), distortion-lens eight, sim six, artistic-surface eight, thumbs-farm/deferral scripts.

## Shipped: generative eight (claude)

Folder: `agents/swarm-outputs/claude-2026-09-21-generative-eight/`. Picked the 8 smallest generative files with no
`Ideas:` line (skipped gen_capabilities, a probe). 16 ideas, none shared; see MEMORY.md entry. Bugs found by reading:
hyperbolic-tree forks were per-pixel hash noise (speckle), protocell `pow(1-|d|*6, 3)` negative base → NaN off-cell,
protocell C sampled at centred coords, cyber-terminal rain's bright end trailed the motion. plasmaBuffer still
unwritten (audioDepth.ts:5) — no idea depends on audio. **Not committed.**
