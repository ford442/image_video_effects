# 2026-09-06

## #1205 residual holes

- JS `compileShader` Validation error scope: never cache Dawn's non-null invalid pipeline; fallback only if its scope is clean; else banner + skip.
- C++ LoadShader: WaitAny != Success (or no instance) → treat pipeline invalid, do not store.
- Invariant lock: BGL `RgbaStorageFormat(colorFormat_)`, rewrite + hadError + waitFailed, Depth Texture Write CopySrc, frame depth CopyTex.
- Catalog stays authored rgba32float. No GPU confirm in this VM.

## Grok simpler generative / kinetic ten

- Upgraded 10 generative IDs under the incremental-ideas contract. Underscore-backed WGSL paths preserved.
- Ideas: Jacobian lattice + C phosphor; SmoothLife hunt fronts + edge flash; terrain terraces + foam; Buddhabrot set body + dwell; SH jets + storm eye; Chladni radial/azimuth tint + Lambert; Worley nucleus + F2−F1 crack; epicycle arms + pen ink; dragon crease + gen thickness; De Jong stretch + dwell.
- Naga 10/10, extraBuffer 0, dead sliders 0, catalog 1,359, SKIP_WASM_BUILD=1 build green. Real-GPU QA external. Briefs: `agents/swarm-outputs/grok-2026-09-06-gen-kinetic-ten/`.

## Photo / print / grade eight (example batch)

- Upgraded 8 photographic/print/grade shaders as the first batch under the incremental-ideas contract, then pointed `docs/SHADER_UPGRADE_BATCH.md` §7 at them.
- IDs: pp-bloom, pp-tone-map, analog-film-degrade, color-blindness, crumpled-paper, retro-gameboy, conv-bilateral-dream, tilt-shift.
- Briefs written first: `agents/swarm-outputs/grok-2026-09-06-photo-eight/BRIEFS.md`. Naga 8/8, extraBuffer/dead sliders 0, params exact vs HEAD. Jest 91/94 (3 pre-existing WASM `bridge/api.js`). Real-GPU QA external.

## Shader upgrade process review

- User asked to review batch-upgrade docs: they should require **incremental ideas added to each existing effect**, not a reimagining and not a formatting/uniform-alignment pass. Antigravity was already running a ~10-shader batch; shader files left alone.
- Wrote `docs/SHADER_UPGRADE_BATCH.md` as the live contract (Idea Cards, native vs generic overlay, batch size vs model, library timeframe). Pointed `agents/CLOUD_UPGRADE.md` (killed “full rewrite” / “header fix only”), prompt templates, README, AUTHORING, swarm usage, and `notes/shaders_upgrade_plan.md` at it.
- Follow-up: expanded the contract with worked good/bad cards, header/JSON/packing rules, MEMORY closeout shape, coordinator fail-list, live-vs-historical map, paste block. Corrected remaining entry points (`AGENTS.md`, `grok.md` stack, builtins, BINDING_CONTRACT, CREATIVE_VISION, swarm generator) and stamped April–June docs historical so agents stop treating ACES sprints and line-count targets as the law.
- Advice: Flash 6–8, Opus/Grok 8–10, ceiling 12. Hygiene floor is close to saturated on recent cohorts; remaining work is months of distinct ideas, not weeks of ACES stamps. Real-GPU visual QA is still external.

## #1204 residual — Pascal-safe historyTex budget

- Default working size 1024. 2048 only after `allowsFullWorkingSize` (discrete, maxBufferSize ≥ 1GiB, not Pascal/GTX 10-series, no OOM this tab) plus a full-pool allocate (historyTex first). Upgrade destroys 1024 before 2048; never hold both.
- WASM `init.ts` sizeFallback/clamp 1024; bridge copies emitted. C++ / `public/wasm/pixelocity_wasm.*` not rebuilt.
- Jest: historyTexProbe + resources + performanceStatus + backendLifecycle. `verify:wasm-invariants` + `verify:wasm-bridge-sync` green. Real-GPU Pascal confirm still external.

## WASM rebuild for maxAnisotropy (#1203)

- Dawn still logged `Max anisotropy (0.000000) is less than 1` on Filtering / Non-filtering / Comparison samplers in the same Pascal/Chrome session as the present abort. C++ was already correct: `samplerDesc.maxAnisotropy = 1` once in `wasm_renderer/resources.cpp` before all three `wgpuDeviceCreateSampler` calls.
- This pass did **not** rewrite C++. Rebuilt `public/wasm/pixelocity_wasm.{js,wasm}` with **emcc 6.0.3** + emdawn (`npm run wasm:build`, no `SKIP_WASM_BUILD`).
- Gates: `wasm:validate` green; `verify:wasm-invariants` green; import table has `wgpuSurfaceConfigure` + `wgpuSurfaceGetCurrentTexture` only — no `wgpuSurfacePresent` / `_wgpuSurfacePresent`.
- Did not deploy. Did not close #1203. Real-GPU confirm still Pascal/Chrome after hard-reload of new artifacts. HOLD go.1ink.us.

## 2026-09-06 — 10-Shader Interactive / Post-Processing / Image Upgrade Cohort

- Upgraded 10 shaders across geometric, interactive-mouse, post-processing, image, and distortion categories:
  1. `kinetic_tiles` (geometric): traveling wave cascade, rotating bevel tile shear, single-writer spring cursor in `extraBuffer[133..138]`, exact `textureLoad` C temporal trails.
  2. `sliding-tile-glitch` (interactive-mouse): directional row/column sliding conveyors, restoring decay, chromatic tear, exact-C phosphor trails.
  3. `pp-sharpen` (post-processing): bilateral halo-suppressed unsharp mask, high-pass Laplacian pop, spring inspection lens, click clarity waves.
  4. `pp-vignette` (post-processing): anamorphic aperture falloff, triangular film grain, multi-style grading (sepia/warm/cool/bleach), spring center deflection, flash blooms.
  5. `pixel-scattering` (image): curl-noise velocity field advection, Fibonacci golden spiral dispersal, vortex wake, exact-C streak tails.
  6. `refraction-shards` (interactive-mouse): crystalline shard facet drift (smooth sinusoidal orbital drift replacing `floor(time*0.2)` hash strobing), Cauchy spectral dispersion, fracture wavefronts.
  7. `pixelate-blast` (distortion): domain-warped Voronoi cellular explosion, blast dispersal, Cauchy chroma along blast normals, multi-ring shockwaves.
  8. `double-exposure` (interactive-mouse): photographic double exposure, spring transform pivot, rotational ripple shockwaves, exact-C ghost trails.
  9. `brush-strokes` (interactive-mouse): impasto oil brush simulation, bristle friction, dynamic glaze cycling, click paint spatter ripples, exact-C paint accumulation.
  10. `voronoi-light` (interactive-mouse): spring cursor lantern, cybernetic neon tech mode, stained-glass facet refraction, bass ember borders, exact-C phosphorescence.
- Contract enforcement:
  - Canonical 13-binding WGSL compute header `@workgroup_size(16, 16, 1)` with out-of-bounds guards.
  - Exact `textureLoad(dataTextureC, coord, 0)` previous-frame feedback.
  - A-only writes (`dataTextureA`), ACES display RGBA, semantic alpha.
  - 3-band audio (`plasmaBuffer[0].xyz`).
  - Single-writer spring-damper cursor in `extraBuffer[133..138]` by invocation `(0, 0)` (never touches `[0..132]`).
  - Bounded click ripples (`min(u32(u.config.y), 50u)`), held pointer drag.
  - Byte-exact saved preset parameter contracts in JSON definitions with aligned `updatedParams`.
- Gates passed: Naga 10/10; `audit:extrabuffer` 0 new writes; `audit:dead-sliders` 0 new dead sliders (40/40 live); `prestart` 1,359 catalog shaders; `verify:shader-list-urls` green; `verify:uniforms` green; `verify:catalog-counts` green; `typecheck` green; `SKIP_WASM_BUILD=1 npm run build` green; `verify:bundle-size` green; `verify:toolchain-foundation` green.

## 2026-09-06 — Optical / Glass / Holographic Ten Upgrade Cohort

- Output Artifacts: `agents/swarm-outputs/gemini-2026-09-06-optical-ten/` (`BRIEFS.md`, `NOTES.md`, `COORDINATOR_REVIEW.md`).
- IDs: `gen-holographic-lens-flare-matrix`, `gen-holographic-plasma-geode`, `gen-holographic-rainbow-surface`, `gen-holographic-data-core`, `gen-holographic-bismuth-core-reactor`, `chromatic-folds-bilateral`, `aero-chromatics-prismatic`, `glass-shatter-morph`, `frosted-glass-lens-iridescence`, `spec-prismatic-dispersion`.
- Per shader, native visual ideas added to existing main path:
  - `gen-holographic-lens-flare-matrix`: conjugate optical reflection ghosts; 6-blade hexagonal iris diffraction; Newton-ring thin-film interference fringes. Raw sim state in A/C.
  - `gen-holographic-plasma-geode`: crystal Cauchy facet dispersion & TIR; dielectric breakdown plasma arc filaments; agate mineral growth banding.
  - `gen-holographic-rainbow-surface`: Marangoni stress flow advection; drainage-gradient multilayer interference; anisotropic micro-groove diffraction grating.
  - `gen-holographic-data-core`: logic bus photon energy packets; hexagonal quantum containment lattice cage; depth parallax moiré fringe interference.
  - `gen-holographic-bismuth-core-reactor`: hopper-step terrace edge diffraction glints; multi-order oxide thin-film interference; magnetic containment field flux lines.
  - `chromatic-folds-bilateral`: multi-spectral split hue folding; joint depth-bilateral edge preservation; contour standing resonance waves. Exact-C load.
  - `aero-chromatics-prismatic`: aerodynamic vortex shedding plumes; Schlieren velocity gradient refraction; wavelength-differential feedback decay trails. Exact-C load.
  - `glass-shatter-morph`: sub-cellular micro-fracture spiderweb cracks; grazing prismatic TIR facet glints; photoelastic stress birefringence fringes. Spring kept.
  - `frosted-glass-lens-iridescence`: microscopic condensation droplet beads; pointer-drag moisture wipe path; lens bevel Cauchy prism dispersion. Spring kept.
  - `spec-prismatic-dispersion`: internal double-refraction caustic cusp rings; anti-reflective purplish-amber lens sheen; astigmatic chromatic radial streaks. Spring kept.
- Springs policy: Kept only on pointer-tracking lenses/shards (`glass-shatter-morph`, `frosted-glass-lens-iridescence`, `spec-prismatic-dispersion`); omitted on surface/photo/geode shaders.
- Floor: Canonical 13 bindings, 16×16, bounds guard, exact C loads, ACES display RGBA (or documented raw sim), semantic alpha, saved params byte-exact in JSON, aligned `updatedParams`.
- Gates: Naga 10/10, extraBuffer 0 new writes, dead sliders 0 new (40/40 live), catalog lists + unified manifest 1,359, `verify:shader-list-urls` green, `verify:uniforms` green, `verify:catalog-counts` green. Real-GPU visual QA external.


