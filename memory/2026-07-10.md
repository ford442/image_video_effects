# 2026-07-10

## Session: Project progress audit + GitHub issue roadmap

### What we did
Full repo strategic review for next code changes: foundation quality, JS/TS/C++ usage, compile commands, WebGPU context settings, content vs structure priority. Created **22 new GitHub issues** (#912–#933).

### Snapshot
- **1201** cataloged shaders, **1288** WGSL, **346** thumbs (~29%)
- Phase B Advanced Enhancement: complete (48/48, 2026-07-08)
- WASM: Tier B experimental; glue epics closed (#817–#890)
- Only prior open issues: #897/#898 fireworks
- Monoliths: App ~2479, Controls ~2211, WebGPURenderer ~1646, renderer.cpp ~2293

### Foundation findings
1. **TS WebGPU weaker than C++ on device init:** no `requiredLimits`, single high-performance adapter attempt; C++ has 4-step ladder + limit validation.
2. **npm build runs wasm:build twice** (prebuild + build).
3. **93 orphan WGSL** / **6 defs without WGSL**; legacy `interactive.json`/`liquid.json` cause 41 dedupe warnings.
4. **tsconfig target ES5** outdated for WebGPU-only app.
5. Duplicate renderer toggle components; StorageService sprawl; README counts drift.

### Strategic call
**Foundation first** for 1–2 cycles before next big content swarm. Content OK in parallel: fireworks #897, small generative upgrades. Multipass sims (#924) wait on graph architecture (#923).

### Issues created
Epic #912 + children #913–#933 (see issue comment for full table). Suggested sprint: #919 → #918 → #917 → #915 → #913/#914 → #920 → product #922/#921.

### C++ compile notes reviewed
`wasm_renderer/build.sh`: c++20 -O2, emdawnwebgpu, ASYNCIFY, MODULARIZE, ALLOW_MEMORY_GROWTH; `requiredFeatureCount=0` (no float32 feature request on device); compatibleSurface null by design.
