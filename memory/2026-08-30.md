# 2026-08-30

## Fast-Motion Shader Upgrade Ten

- Upgraded: voronoi-zoom-turbulence, solarize-warp, luma-smear-interactive, neon-fluid-warp, optical-illusion-spin, vortex-drag, zoom-burst, temporal-slit-paint, neon-warp, mouse-mandelbrot-zoom-portal.
- Temporal Slit Paint: raw A canvas. Neon Warp: display A (analytic heat). Rest: ACES display A.
- Naga 10/10, focused extraBuffer/dead-slider pass, catalog 1353, SKIP_WASM_BUILD=1 build green. Jest 79/84 — WASM api.js resolve is pre-existing. Real-GPU QA external.

## Sync: pull / push shader upgrades

- Branch had diverged: local 1 commit (`174e5c26` push fix — description polish on bubble-lens / liquid-zoom / phosphor-decay / scan-distort) vs remote 5+ (Physics Lab set-2 + batch-61 union, cohort-3 cyber/glitch ↔ lighting-optics reconcile, WASM bridge TS cleanup/re-land).
- `git pull --rebase origin main` conflicted only on `shader_definitions/retro-glitch/phosphor-decay.json`.
- Resolution: kept **remote** upgraded phosphor-decay (new description, `workgroup_size`, `updated: true`, richer features/params). Local change was only appending "Temporal but bounded." to the pre-upgrade description.
- Other three defs auto-merged: kept local quality-note suffixes on top of remote upgrade bodies.
- Rebased commit `350bf525` pushed. `main` now **up to date** with `origin/main` (0/0).

## Remote tip content landed

- Merge leftover shader upgrades: Physics Lab set-2 graphs + batch-61 union
- Reconcile cohort-3 cyber/glitch with lighting-optics
- WASM bridge selective-upgrade union cleanup (remove leftover .ts, then later re-land/bridge emit work on tip)

# 2026-08-30 — catalog SoT + audit hygiene (#1128 / #1011)

- `dla-walkers.wgsl`: wired `walkerSpeed` / `attractStrength` (graph parent `dla-crystals` dead-slider fix).
- `hyper-space-jump-blackbody` removed from dead-slider baseline (`chromatic_spread` live).
- extraBuffer baseline split: `engine_owned` (1) vs `shader_bug` (43); 32 dynamic-index writes triaged.
- `verify:catalog-counts` gates README/manifest/lists (1353). Graph parents whitelisted in catalog audit.
- `public/shader-id-aliases.json` + `resolveShaderId` for 22 underscore legacy ids.

# 2026-08-30 — #1181 gpu-chores source auto-exposure (landed in tree)

- Opt-in **Auto exposure (source)** (default off, `px_source_auto_exposure`). After
  `encodeInputCopy`, hist/LUT/downsample on raw `readTex`, then `apply_gain_2d`
  ping-pong via `writeTex` → `readTex` so catalog shaders see mid-grey source.
- Skip: toggle off, `?no_gpu_compute`, probe fail, WASM, FP32-pinned physics graphs.
- EV stays on host breadcrumbs — not extraBuffer `[0..2]`, FFT, or `[139]`.
- First GPU gain frames are identity until hist `mapAsync` (period 8).
- Dev Tools LUT classify preview from 64×64 classifyTex readback (R = band 0..7).
- `thumbs:generate` app engine uses chores `downsample_2d` with canvas fallback.
- Jest 34/34 focused; SKIP_WASM_BUILD=1 build; main 270.62 KiB gzip / 320.
- No PNG capture / visual gain QA on this VM.

# 2026-08-30 — thumbnail deferral contract (#1185)

- Deferral CI: fail only on newly eligible missing healthy thumbs (no %/count gate). `until` aliases `expires`. Sticky PR comment. Seeded 1069 gpu-capture deferrals through 2026-09-29.
- Attract-first `--priority=attract` on generate + wave `attract`. No PNG capture on this VM.
- Attract/roulette use `hasHealthyThumbnail` + `public/thumbnails/unhealthy.json` (77 integrity flags). Placeholders stay.

# 2026-08-30 — #1180 residual packing gate

- TS/C++ workgroup fallback and catalog 8×8 migration were already on main.
- Residual: `workgroup_dispatch.json` `emptyPlaceholder` `{ format: r32float, bytesPerRow: 4 }` plus `verifyEmptyPlaceholder()` in `scripts/verify-device-policy-sync.js`.
- Gates: `verify:device-policy`, `verify:uniforms`, focused Jest (13), `SKIP_WASM_BUILD=1 npm run build` green.
- Adapter ladder / canvas configure / gpu-chores untouched.

# 2026-08-30 — sync: rebase local onto remote Fast-Motion tip

- Diverged again: local `c96b97e2` (catalog/gpu-chores/thumbnails) vs remote `e0abaa0a` (Fast-Motion ten).
- Rebase conflicts: memory + dead_sliders/extrabuffer audit reports.
- Resolution: kept full-catalog audit artifacts (match local baselines); merged memory notes from both sides.
