# 2026-08-15

## Shader upgrade Batch 51 — LIQUID DYNAMICS, GEOMETRY & VISCOUS FLOW

- Upgraded tracker #439–446 across the Liquid Effects cohort: Liquid Fast,
  Liquid RGB, Liquid Jelly, Liquid Rainbow, Liquid Perspective, Liquid Glitch,
  Liquid Viscous Nebula grokcf1, and Liquid Viscous (Simple).
- Added dual continuous motion (Hamiltonian divergence-free streamfunctions,
  complex potential vortex superposition, Kelvin-Voigt viscoelastic elasticity,
  trochoidal Gerstner waves, Laplace cohesion, and vorticity confinement), 2.5D
  heightfield normal derivatives, Snell's law refraction, thin-film optical
  interference, Voronoi cellular quantization, and interactive pointer drag wakes.
- Replaced all rgba32float feedback reads with exact `textureLoad` from `dataTextureC`.
- Structural proof: 8/8 Naga and bindgroup gate, 0 new extraBuffer violations,
  0 new dead sliders, catalogs in sync (1,340 unique IDs, 0 duplicates), uniforms layout
  contract in sync. Real-GPU visual QA remains external.

## Multipass Physics Lab flagships (#1081)

- Engine: smart pass-budget (keep color write, shrink repeats), GraphRunReport
  in Dev Tools, Physics Lab browser chip.
- Content: chromatographic-fluid, gray-scott-tank, optical-flow-dream.
- Attract + presets + docs updated. Placeholder thumbs checked in; GPU visual
  QA still needed on a discrete-GPU host.

## Deploy stopped prompting for FTP/SFTP credentials

- User: deploy always asked for FTP user/pass.
- Cause: May 30 commit `a4cb1712` removed the hardcoded DreamHost password from
  `scripts/deploy.py` / `deploy_app_only.py`. Scripts fell back to `getpass`.
- Fix: shared `scripts/deploy_credentials.py` loads gitignored `.env.deploy`
  (or `~/.config/pixelocity/deploy.env`) plus `DEPLOY_*` / `FTP_*` aliases.
  `deploy.py`, `deploy_app_only.py`, and `sync_shaders_to_1ink.py` use it.
- Local `.env.deploy` is 0600 and gitignored. This VM's `id_ed25519` was added
  to `ford442@1ink.us` `~/.ssh/authorized_keys`; BatchMode SSH now works.
- `.env.deploy.example` documents the file. Do not commit the real password.
  The old password is still the live one and was previously public in git —
  rotate when convenient.

## Thumbnail foundation wave #1076

- Implemented the tooling/documentation guardrails: minimal thumbnail engine is
  explicitly generative-only, PR coverage regression check compares against the
  base git ref, and status reports attract/Physics Lab priority coverage.
- Cloud VM status remains 349/1324 nominal, 272/1323 healthy, and 20/21 priority;
  real capture still requires a discrete-GPU workstation.

## Toolchain truth pass (#1076 / #1083)

- Recorded TS 5.4.5 landed on CRA 5 + CRACO 7 in `docs/TOOLCHAIN_DECISION.md`
  (decision log, dependency-boundary table, toolchain command matrix).
- README: toolchain commands table + `npm run typecheck` script.
- Deprecated cleanup: removed `BaseRenderer.ts`, root `StorageUIComponents.tsx` /
  `StorageControls.tsx` shims, `getStorageService` / `createStorageService` /
  `resetStorageService` aliases. `StorageService.ts` facade kept with canonical names.
- Vite spike explicitly deferred (triggers not met).
- Verification green: typecheck, device-policy, uniforms, 513 Jest pass, production
  build (`SKIP_WASM_BUILD=1`), `verify:toolchain-foundation` (main ~258 KiB gzip).
- No changes to WebGPU device-init policy or feedback copy order.

## gpu-chores Tier 4b (shared pre-FX)

- Facade: `src/gpuChores/` (histogram / reduce / LUT / downsample / auto-exposure).
- Host adopts renderer `GPUDevice` only. Kill switch `?no_gpu_compute`.
- Live path: histogram-driven auto-exposure gain on the 64×64 preview, not catalog FX.
- Breadcrumbs in Dev Tools (`gpuComputeAvailable`, reason, last op, backend, EV).
- CPU goldens + diagnostics tests green; full Jest 79 suites / 527 pass / 1 skip.
- Docs: `docs/GPU_CHORES.md`. Domain FX stay Tier 4a.

## #1107 WebGPU hard-fail boot probe

- Finished WIP: canvas-slot overlay mount + CSS, device.ts publish, docs/WEBGPU_BOOT_PROBE.md,
  overlay/probe tests, RendererManager 348 LOC, no auto Canvas2D fallback.
- Verify: tsc clean, 81 suites / 535 pass / 1 skip, production build, dependency-boundaries green.
- window.webgpuProbe breadcrumb on success and failure; handoff avoids double requestDevice.
