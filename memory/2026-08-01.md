
## Branch cleanup (2026-08-01)

Goal: fold useful alternate-branch work into main, then delete branches.

### Useful content landed
- Pulled origin/main: weekly plan #1052 + bismuth void-owl impl #1051 (were merged remote-only).
- Imported orphan plans that only lived on side branches (`0a74d08a`):
  - `shader_plans/2026-07-22_radiant-quantum-aether-celestial-lion.md`
  - `shader_plans/2026-08-01_quantum-acoustic-bioluminescent-void-urchin.md`
  - both appended to `shader_plans/queue.json` pending.

### Already on main (no cherry-pick needed)
- Foundation #912 waves (device policy, ES2020, list cleanup, prebuild wasm) — superseded by later main work; old catalog-pair script redundant vs `audit_orphan_shader_defs.py`.
- Relay hops 1–5, MIDI (#936/#957), CORS/readTex/blank-after-scale (#1006–#1021/#1018), leviathan, flora, coral, kraken plans, etc.

### Explicitly discarded
- `feat/midi-control-and-wasm-parity` tip: stale "push fix" + swarm notes only (PR #995 closed).
- `implement-moth-shader` tip: 397-file rewrite garbage (PR #1020 closed).
- hop-4 branch tip: would *regress* hop-5 motion already on main.
- CORS branch tip residual: corrupted test double-definition vs already-merged fix.

### Closed PRs
- #995 midi, #1020 moth, #1053 urchin draft (content landed).

### Deleted
- All local alternate branches + foundation worktrees.
- All remaining remote branches except `main`.
- Repo now: only `main` (local + origin).

### Left alone (WIP not branch-related)
- Batch 21 uncommitted shader work: moire-interference, mouse-gravity, fireworks-edge-ignite, briefs under `kimi-briefs-2026-07-31-b21/`.

## Multipass productization map (exploration)

Mapped #1041 deliverables vs tree. Graphs live (5 in GRAPH_REGISTRY). Gaps: no PHYSICS_LAB.md; thumbs only ripple-tank; no physics preset pack; attract pool generative-only; fabric zoom_params.w damping/selfHeal collision; photonic-emitter unused zoom_params (dead-slider false positives on graph secondaries); badge UI dual CSS; maxPasses HUD exists but undoc'd in VJ_STUDIO.

## Physics Lab flagship polish (cloud agent)

Productized Tier C multipass stacks (#1041 track):
- Flagships polished: ripple-tank (audio rain), fabric-of-reality (stable damping/selfHeal split, tear/mouse, preferred A→C handoff fix), photonic-caustics-graph (params in emitter, visible accumulator)
- Discoverability: multipass badge CSS unified, `public/presets/physics-lab.json` + preset_packs entries, thumbs for fabric/photonic, attract pool + 22s dwell
- Docs: `docs/PHYSICS_LAB.md`, VJ_STUDIO Physics Lab section, Render Quality pass-budget hint
- Engine: `copiesNeededBeforeRead` prefers explicit dataA/dataB when sibling buffers diverge (fabric render was sampling tear mask as positions)

## Toolchain foundation (#1042, Phase 0 + Phase 1 guardrails)

- Measured fresh CRA production output: `main` 257,469 bytes gzip (~251.43 KiB); lazy chunks remained `auto-dj` ~6.92 KiB, `transformers` ~175.76 KiB, `web-llm` ~2,090.61 KiB.
- Added a 320 KiB gzip main-chunk CI budget plus output-manifest checks that the three AI feature/library chunks are not CRA entrypoints.
- Added dependency-boundary enforcement: TypeScript 5.x, no duplicate direct/aliased packages, AI package references only in `transformersLoader` / `webLlmLoader`.
- Confirmed package + lock already declare TypeScript 5.4.5; stale local `node_modules` had 4.9.5 and was corrected with `npm ci`. Typecheck passes after narrow test-fixture typing repairs.
- CRA 5's published peer metadata still caps TypeScript at 4.x, so `npm ls typescript` reports an invalid peer even though locked TS 5.4.5 passes full typecheck and CRA/craco production build. CI now tests that real compatibility; Vite is the long-term removal of the metadata mismatch.
- Documented dev/build commands and `public/wasm/` as the only deployable WASM artifact source of truth; bridge fragments remain canonical in `wasm_renderer/bridge/`.
- Removed tracked root junk (`a.out.wasm`, `upg.zip`, `patch_wasm_final16.js`, `fix_eslint2.py`) and expanded root ignores. All removals remain recoverable from Git.
- Verification: production build, TypeScript 5.4 typecheck, 64 Jest suites / 428 pass / 1 skip, device-policy sync, WASM artifact validation, foundation gate, and diff check all green.
- CRA→Vite remains a separate optional spike; device init was deliberately unchanged. Concurrent Batch 22 shader work was preserved.

## WebGPU frame seam extraction (#1043)

- Strategic intent: finish the TypeScript WebGPU foundation before adding more multipass content or promoting WASM; no shader or user-visible behavior changes.
- `src/renderer/webgpu/frame.ts` dropped from 873 to **194 LOC** and now stays the RAF/media/uniform/history/FPS facade.
- Extracted renderer-owned getter plumbing to `frameState.ts`, scale/canvas acquire/final blit/timestamp resolve/submit to `present.ts`, and parallel/chained/GraphRunner dispatch to `slotDispatch.ts`.
- Preserved the feedback contract in live code and the older pure `slotOrchestrator` model: `dataTexB → dataTexC` first, then `dataTexA → dataTexC` so primary simulation state wins.
- Added pure tests for feedback copy order, registered-graph resolution, render-quality graph caps, blit bind-group cache invalidation, and generative present-pipeline selection.
- Updated `docs/APP_STRUCTURE.md` and `src/renderer/README.md`; C++ `frame.cpp` remains the naming/ownership facade and does not need a split yet.
- Verification: focused 2 suites / 5 tests; full Jest **68 suites, 464 pass, 1 skip**; `npx tsc --noEmit`; focused ESLint; device-policy sync; uniforms sync; production CRA build; bundle policy (main 254.49 KiB gzip / 320 KiB) all green.
- Cloud build note: direct `tsx` manifest generation hit the known `listen EPERM` IPC restriction. `node --import tsx scripts/build-unified-manifest.ts` plus `npm run build --ignore-scripts` proved the production build; unrelated generated shader-list drift was removed afterward.
- No live GPU visual proof in this VM; verification is structural/unit/build/policy evidence.

## Thumbnail coverage foundation + GPU handoff

- Current nominal coverage is **349/1,306 (26.7%)**; one justified hardware skip leaves 1,305 eligible entries.
- Fixed the integrity auditor's PNG decoder: it now reconstructs filter types 0-4 instead of measuring filtered bytes. The corrected audit reports **77 real black frames**, not the previous 89 mixed flags.
- Honest healthy coverage is **272/1,305 (20.8%)**; the 80% target is 1,044, so **772 healthy thumbnails remain** (695 missing plus 77 invalid existing PNGs).
- `run-thumbnail-waves.sh` now force-retries integrity failures before missing entries, then prioritizes generative, simulation multipass, and interactive-mouse categories.
- A production-app WebGPU probe on this Cloud VM returned a zero-energy black frame; do not accept thumbnails from this host. Use a verified discrete-GPU workstation/VPS/self-hosted runner.
- Keep the 77 renderable failures eligible; never move them to the skip list merely to improve coverage. The sole skip, `deep-workgroup-multi-effect-blend`, now records its 1,024-invocation hardware limit.
- Attract/roulette 3x weighting remains manifest-backed and covered by unit tests. Scheduled CI stays reporting-only before a 50% healthy baseline.
- Monthly snapshot: `reports/thumbnail-coverage-2026-08.md`.
