
## Branch cleanup (2026-08-01)

Goal: fold useful alternate-branch work into main, then delete branches.

### Useful content landed
- Pulled origin/main: weekly plan #1052 + bismuth void-owl impl #1051 (were merged remote-only).
- Imported orphan plans that only lived on side branches (`0a74d08a`):
  - `shader_plans/2026-07-22_radiant-quantum-aether-celestial-lion.md`
  - `shader_plans/2026-08-01_quantum-acoustic-bioluminescent-void-urchin.md`
  - both appended to `shader_plans/queue.json` pending.

### Already on main (no cherry-pick needed)
- Foundation #912 waves (device policy, ES2020, list cleanup, prebuild wasm) — superseded by later main work; old catalog-pair script redundant vs `audit_orphan_shader_defs.py`.
- Relay hops 1–5, MIDI (#936/#957), CORS/readTex/blank-after-scale (#1006–#1021/#1018), leviathan, flora, coral, kraken plans, etc.

### Explicitly discarded
- `feat/midi-control-and-wasm-parity` tip: stale "push fix" + swarm notes only (PR #995 closed).
- `implement-moth-shader` tip: 397-file rewrite garbage (PR #1020 closed).
- hop-4 branch tip: would *regress* hop-5 motion already on main.
- CORS branch tip residual: corrupted test double-definition vs already-merged fix.

### Closed PRs
- #995 midi, #1020 moth, #1053 urchin draft (content landed).

### Deleted
- All local alternate branches + foundation worktrees.
- All remaining remote branches except `main`.
- Repo now: only `main` (local + origin).

### Left alone (WIP not branch-related)
- Batch 21 uncommitted shader work: moire-interference, mouse-gravity, fireworks-edge-ignite, briefs under `kimi-briefs-2026-07-31-b21/`.

## Multipass productization map (exploration)

Mapped #1041 deliverables vs tree. Graphs live (5 in GRAPH_REGISTRY). Gaps: no PHYSICS_LAB.md; thumbs only ripple-tank; no physics preset pack; attract pool generative-only; fabric zoom_params.w damping/selfHeal collision; photonic-emitter unused zoom_params (dead-slider false positives on graph secondaries); badge UI dual CSS; maxPasses HUD exists but undoc'd in VJ_STUDIO.

## Physics Lab flagship polish (cloud agent)

Productized Tier C multipass stacks (#1041 track):
- Flagships polished: ripple-tank (audio rain), fabric-of-reality (stable damping/selfHeal split, tear/mouse, preferred A→C handoff fix), photonic-caustics-graph (params in emitter, visible accumulator)
- Discoverability: multipass badge CSS unified, `public/presets/physics-lab.json` + preset_packs entries, thumbs for fabric/photonic, attract pool + 22s dwell
- Docs: `docs/PHYSICS_LAB.md`, VJ_STUDIO Physics Lab section, Render Quality pass-budget hint
- Engine: `copiesNeededBeforeRead` prefers explicit dataA/dataB when sibling buffers diverge (fabric render was sampling tear mask as positions)
