# 2026-07-31

## Batch 19 — All-Category Wave 3 ✅

Same pipeline as b17/b18 (plan: `/root/.kimi/plans/hawkeye-fire-cable.md`). Pool 705 → 697. Briefs: `temp/make_briefs_2026_07_31_b19.py` (reuses b17 scanner), `swarm-tasks/kimi-briefs-2026-07-31-b19/`; notes: `swarm-outputs/kimi-2026-07-31-b19/`.

- **8 shaders upgraded (#183–190), 100–103 liners:** reactive-glass-grid 100→163, ascii-glyph 101→169, temporal-phosphor-burn-motion-adaptive 101→155, codebreaker-reveal 102→170, digital-reveal 102→166, magnetic-ring 102→166, signal-modulation 102→161, holographic-shatter 103→162.
- **Real bugs fixed:**
  - `holographic-shatter` **TRIPLE BUG** — OOB palette read (`plasmaBuffer[palIdx % 256u]` → bins 1–8, 2nd OOB case after b18 holographic-sticker); **dead 'Depth Weight' slider** (`depthLayeredAlpha()` defined, never called — wired into finalAlpha); **inverted impact falloff** (`smoothstep(0.0, 0.6, dM)` grew with distance — mouse-down shattered everything except the cursor; now near-focused + global baseline). New bug class: dead helper fn = dead slider.
  - `signal-modulation` **fake spectral bands** — 8-band visualizer driven by `fract(sin())` hash, not FFT (fake-FFT-proxy class). Now real bins 1–8, hash kept as ±10% jitter.
  - `ascii-glyph` tagged mouse-driven but `zoom_config` NEVER read (2nd unread-mouse case after b18 neon-topology) — spring lens wired. `temporal-phosphor-burn-motion-adaptive` same (history-ring shader, mouse lens + click burn stamps, ring contract verbatim, extraBuffer read-only).
  - Stale uniform comments fixed in 3 (config.y = rippleCount, zoom_config.w = mouseDown).
- All 8 gained guarded click-ripple interactivity; 6 gained spring-damper glide (extraBuffer [133..138] only).
- **Gate:** 8/8 green (naga + bindgroup, 0 warnings, 0 extraBuffer violations).
- **Lists/dupes:** 1318 unique definitions (+1 vs b18 = gen-vitreous-quantum-lotus-singularity from #1047, committed between batches), no dupes.
- **Jest:** 63 suites / 422 pass / 1 skip — unchanged baseline.
- **Tracker:** `agents/weekly_upgrade_swarm.md` Batch 19 section; header 182→190.
- Not committed — working tree per user preference.

### Bug-class tally (updated, worth CI audits)
- Mislabeled sliders: b17×2, b18×2
- Mouse tagged but never read: b18 neon-topology, b19 ascii-glyph + temporal-phosphor-burn-motion-adaptive
- Mask-as-color feedback: b14 spore-galaxy, b18 neural-resonance
- Mouse coord units: b18 fireworks-portrait-burst
- Flat-0.0 depth clobber: b16 cosmic-jellyfish, b18 fireworks-portrait-burst
- Dead/fake audio (fake FFT proxy): b15 void-spider, b16 phoenix, **b19 signal-modulation (fake spectral bands)**
- OOB plasmaBuffer index: b18 holographic-sticker, **b19 holographic-shatter**
- Dead helper fn → dead slider: **b19 holographic-shatter (depthLayeredAlpha)** — NEW
- Inverted distance falloff: **b19 holographic-shatter** — NEW (check `smoothstep(0, r, dist)` growing with dist where near-focus intended)

## Batch 22 — All-Category Wave 6 ✅

Same pipeline. Pool 680 → 672. Briefs: `temp/make_briefs_2026_07_31_b22.py`, `swarm-tasks/kimi-briefs-2026-07-31-b22/`; notes: `swarm-outputs/kimi-2026-07-31-b22/`.

- **8 shaders upgraded (#207–214), 107–110 liners:** quantum-cursor 107→177, spectral-glitch-sort 108→178, mirror-dimension 109→172, pixel-explode 109→161, prismatic-3d-compositor 109→182, directional-blur-wipe 110→191, ember-drift-dissolve 110→166, luma-refraction 110→186.
- **Real bugs fixed:**
  - `pixel-explode` **ALL 4 SLIDERS DEAD** — `u.zoom_params` never read; everything hardcoded behind generic labels. Wired bit-exact (force mix(0,0.16)→0.08, grid mix(16,64)→40, range mix(2,10)→6); Speed birthed new wobble motion. Worst dead-slider case yet (neon-edge-reveal at least READ them).
  - `prismatic-3d-compositor` **INVERTED mouse-units bug** — `zoom_config.yz / dims` double-divided an already-normalized mouse → parallax pinned to corner, headline feature never worked. NEW VARIANT (4th mouse-units sighting). Plus 'cameraZ' = secretly mouseDown; dead treble→glow.
  - `luma-refraction` **MASK-AS-COLOR** (4th sighting) — 'temporal wave memory' mixed wave STATE (h∈[-10,10]) into display color. Removed; wave-sim A/C contract verbatim.
  - `directional-blur-wipe` **dead 'Split Pos' slider** (read, never used; wired bit-identical at default) + dead per-loop `chroma` var → now disperses.
  - `spectral-glitch-sort` elliptical mouse influence (no aspect correction) — NEW minor class.
  - `mirror-dimension` dead treble → seam glow. `pixel-explode` dead treble → crackle.
- **State-feedback note:** ember-drift-dissolve (age/lateral/intensity/glow in A) and luma-refraction (h,v in A) keep masks in A because the engine ONLY reads history via dataTextureC (= prev A) — A=state is FORCED for sim shaders. The display→A rule applies only when C is read as COLOR or chains expect color.
- **Gate:** 8/8 green (naga + bindgroup, 0 warnings, 0 extraBuffer violations). **Lists/dupes:** 1319 unique. **Jest:** 64 suites / 428 pass / 1 skip (upstream added tests). Tracker Batch 22 section; header 206→214. Not committed — working tree per user preference.
- Next batch picks: quantum-tunnel-interactive (110), vhs-tracking-mouse (110), then 111-liners.

### Bug-class tally (updated)
- Mouse coord units: b18, b21×2 (normalized-as-pixels); **b22 prismatic (INVERTED: normalized divided by res again)** — grep pattern: `zoom_config.y / .*res` or `/ dims`
- Dead sliders: b19 (dead helper), b20 (gOff, generic×4), b21 (clippedCount, tile_blend), **b22 pixel-explode (ALL 4, never read) + directional-blur-wipe (split_pos)** — ripe for `audit_dead_sliders.py` sweep
- Mask-as-color: b14, b18, b20 (latent), **b22 luma-refraction (sim state as color!)**
- Inverted mouse units = the newest variant class.

## Batch 21 — All-Category Wave 5 ✅

Same pipeline. Pool 687 → 679. Briefs: `temp/make_briefs_2026_07_31_b21.py`, `swarm-tasks/kimi-briefs-2026-07-31-b21/`; notes: `swarm-outputs/kimi-2026-07-31-b21/`.

- **8 shaders upgraded (#199–206), 105–107 liners:** moire-interference 105→174, mouse-gravity 105→166, fireworks-edge-ignite 106→175, spec-histogram-equalize 106→163, speed-lines-focus 106→172, fireworks-patriotic-july4 107→162, interactive-emboss 107→176, kinetic-dispersion 107→186.
- **Real bugs fixed:**
  - `spec-histogram-equalize` JACKPOT — **TWO dead sliders** (clip_limit via dead `clippedCount` — no actual CLAHE clip; now real clip+redistribute CDF. tile_blend read-but-never-referenced; now seam softening) + **masks in A slot** (display→A, debug quad→B) + dead PI/TAU + stale '8x8' doc.
  - `fireworks-edge-ignite` + `fireworks-patriotic-july4` — **family diseases** (same as b18 fireworks-portrait-burst): mouse-coord-units bug (`(yz - res*0.5)` treats normalized as pixels → off-screen bursts; fix `(yz * res - res*0.5)/min(res)`) + flat-0.0 depth clobber behind 'depth-aware' tags. 2nd/3rd and 3rd/4th sightings. **grep the fireworks family for stragglers:** `sampleImg(uv*0.5+0.5)` centered-UV convention + `vec4<f32>(0.0)` depth write.
  - `fireworks-patriotic-july4` dead mids → stripe wave.
  - `moire-interference` dead `dir` var; `speed-lines-focus`/`moire` odd depth-write alpha 1.0 (normalize — r32float only stores .r, cosmetic).
  - `interactive-emboss` depth-aware tag earned (reliefContrast × depth); soft-knee on unclamped emboss.
  - `kinetic-dispersion` spring velocity → intensity ('mouse-velocity dispersion' description now literally true).
  - Dev thinking-out-loud comments preserved verbatim in mouse-gravity + interactive-emboss (file personality — brief said so, agents honored it).
- **Gate:** 8/8 green (naga + bindgroup, 0 warnings, 0 extraBuffer violations). **Lists/dupes:** 1319 unique (+1 = bismuth void-owl #1051). **Jest:** 63 suites / 422 pass / 1 skip. Tracker Batch 21 section; header 198→206. Not committed — working tree per user preference.
- Next batch picks: quantum-cursor (107), spectral-glitch-sort (108), then 108-liners.

### Parallel session note
Another session did branch cleanup 2026-08-01 (deleted all non-main branches, closed junk PRs #995/#1020/#1053, imported orphan plans) and explicitly left our uncommitted batch WIP alone. Repo is now main-only.

## Batch 20 — All-Category Wave 4 ✅

Same pipeline. Pool 695 → 687. Briefs: `temp/make_briefs_2026_07_31_b20.py`, `swarm-tasks/kimi-briefs-2026-07-31-b20/`; notes: `swarm-outputs/kimi-2026-07-31-b20/`.

- **8 shaders upgraded (#191–198), 103–105 liners:** luma-topography 103→170, scanline-drift 103→155, sonar-pulse 103→169, cyber-halftone-scanner 104→158, neon-edge-reveal 104→177, spectral-rain 104→172, lenticular-holographic-shift 105→166, long-exposure 105→156.
- **Real bugs fixed:**
  - `neon-edge-reveal` **ALL 4 labels generic/wrong** (Intensity→radius, Speed→edge boost, Scale→glow, Detail→alpha-only) — rewired honest, defaults reproduce legacy look; plus **unclamped ~19.8× HDR emission** (soft-knee). NOTE: my brief's Sobel window mix constants were wrong (0.06/0.325 at default vs claimed 0.05/0.30) — agent followed formula, I corrected to mix(0.08,0.02)/mix(0.5,0.10) in review. Lesson: double-check mix() endpoint math in briefs.
  - `long-exposure` **fake Glow Radius** (dead gOff — fixed 3×3 blur regardless of slider; now real ±gStep taps) + global click reset → positional eraser brush.
  - `cyber-halftone-scanner` **OOB palette %256** (3rd sighting!) + dead PHI const.
  - `spectral-rain` mids/treble declared but never used → per-column FFT voices.
  - `lenticular-holographic-shift` masks in dataTextureA (latent chain poison) → display→A, masks→B.
  - `sonar-pulse` ripples uniform unused — in a SONAR shader. Click pings fired.
  - `luma-topography` JSON labels honest — the lies were in the WGSL struct comment (x=Strength/y=Radius/z=Aberration/w=Darkness vs real Relief/Intensity/Shininess/Ambient). Comment-only fix. Lesson: cross-check BOTH directions before claiming mislabeled sliders.
- **Gate:** 8/8 green (naga + bindgroup, 0 warnings, 0 extraBuffer violations). **Lists/dupes:** 1318 unique. **Jest:** 63 suites / 422 pass / 1 skip. Tracker Batch 20 section; header 190→198. Not committed — working tree per user preference.
- Next batch picks: moire-interference (105), mouse-gravity (105), then 106-liners.

### Bug-class tally (updated)
- Mislabeled/generic sliders: b17×2, b18×2, **b20 neon-edge-reveal (all 4)**
- Mouse tagged but never read: b18, b19×2; partial (mouse.y only): b20 scanline-drift
- Fake/dead audio: b15, b16, b19 signal-modulation, **b20 spectral-rain (dead mids/treble)**
- OOB plasmaBuffer %256: b18, b19, **b20 cyber-halftone-scanner** — 3 sightings, ripe for a CI audit script
- Dead helper/var → dead slider/feature: b19 holographic-shatter, **b20 long-exposure (gOff)**
- Unclamped HDR: b17 kaleido-portal, cyber-magnifier; **b20 neon-edge-reveal**
- Masks in A slot: b14, b18 (active), **b20 lenticular (latent)**
- Inverted falloff: b19 holographic-shatter
- Stale WGSL comments/header category: basically every batch — harmless but confusing; keep fixing comment-only

### Env notes
- naga available; node via `export PATH=/root/emsdk/node/22.16.0_64bit/bin:$PATH`; no GPU.
- `agents/swarm-tasks` and `agents/swarm-outputs` are symlinks to root dirs (git shows them under agents/).
- Next batch: re-run b17 `scan_pool()` — top picks now luma-topography (103), scanline-drift (103), sonar-pulse (103), cyber-halftone-scanner (104).
