# 2026-07-30

## Batch 17 — Smallest-First All-Category Upgrade Swarm ✅

First non-generative swarm after the generative pool closeout. Plan: `/root/.kimi/plans/spectrum-robin-blue-marvel.md` (user picked smallest-first all-category, 8 agents).

- **Pool scanner:** `temp/make_briefs_2026_07_30_b17.py` — generalizes `make_briefs_common` to ALL categories (was generative-only), smallest WGSL first. Excludes: tracker-mentioned, non-empty `updatedParams`, gen_capabilities/gen_grid, and **multipass defs** (own `multipass` key or referenced via `nextShader`). Multipass exclusion knocked out the 3 smallest in pool (`rd-on-video-pass1/2/3`) — pass I/O contracts are off-limits to generic swarms. Pool: **743 shaders**; next batches re-run this scanner.
- **8 shaders upgraded (#167–174), all interactive-mouse, 88–97 lines:** velvet-vortex 88→141, cyber-magnifier 93→159, chronos-brush 94→144, kimi_spotlight 94→163, interactive-glitch-brush 95→161, pixel-focus 95→146, kaleido-portal-interactive 96→166, cursor-aura 97→168.
- **Real bugs fixed:** chronos-brush **3 mislabeled sliders** (Freeze Decay→hue speed, Time Edge Distort→fade, Paint/Erase→opacity — rewired honest, default decay bit-identical); cursor-aura **2 mislabeled sliders** (Edge Softness→hidden mix, Color Hue→hidden pulse speed, hue hardcoded blue — IQ hue w/ 0.5 = exact legacy blue); kaleido-portal **unclamped ~7.0 HDR border** (soft-knee + 1.5 cap); cyber-magnifier additive HUD glow unclamped (hue-preserving 1.2 clamp); pixel-focus depth buffer read-but-ignored (now depth-aware focus). All 8 gained guarded click-ripple interactivity; 6 gained spring-damper mouse glide (extraBuffer [133..139] only).
- **Gate:** 8/8 green, 0 warnings — **naga IS installed in this VM now** (was missing 07-26) — full validation ran. 0 extraBuffer violations.
- **Lists/dupes:** `generate_shader_lists.js` clean; **1317 unique definitions**, no dupes (`check_duplicates.js`).
- **Jest:** 63 suites / 422 pass / 1 skip — green (suite count up from 59/397 at 07-26 closeout due to new audit tests).
- **Tracker:** `agents/weekly_upgrade_swarm.md` Batch 17 section; header count fixed 150→174.
- Briefs: `swarm-tasks/kimi-briefs-2026-07-30-b17/`; notes: `swarm-outputs/kimi-2026-07-30-b17/`.

## Batch 18 — All-Category Wave 2 ✅

Same pipeline, pool 712 → 704. Briefs: `temp/make_briefs_2026_07_30_b18.py` (reuses b17 scanner), `swarm-tasks/kimi-briefs-2026-07-30-b18/`; notes: `swarm-outputs/kimi-2026-07-30-b18/`.

- **8 shaders upgraded (#175–182), 97–100 liners:** neural-nexus 97→167, prismatic-feedback-loop 97→176, neon-topology 98→150, temporal-slit-scan 98→160, spectral-distortion 99→163, fireworks-portrait-burst 100→166, holographic-sticker 100→170, neural-resonance 100→162.
- **Real bugs fixed:** prismatic-feedback-loop **ALL 4 labels wrong** (Feedback→accumRate, Blur→prism, Glow→rotation, ChromaticSpread 0.02→feedback mix; rewired + blowout guards); neon-topology **tagged mouse-driven but mouse unread** (+2 mislabeled sliders + dead `alpha` var); neural-resonance **mask-as-color feedback** (spore-galaxy class: display→A, masks→B); fireworks-portrait-burst **mouse coord units bug** (normalized treated as pixels → off-screen bursts) + flat-0.0 depth clobber (now luma-derived); holographic-sticker **OOB palette read** (`plasmaBuffer[palIdx % 256u]` → bins 1–8).
- temporal-slit-scan: history-ring shader (binding 13) — mouse scan pivot + click tears, engine ring contract verbatim.
- **Gate:** 8/8 green (naga + bindgroup, 0 warnings). **Lists/dupes:** 1317 unique. **Jest:** 63 suites / 422 pass. Tracker Batch 18 section; header 174→182.
- Not committed — working tree per user preference.

### Bug-class tally (recurring, worth CI audits)
- Mislabeled sliders (JSON name ≠ WGSL role): b17×2 (5 sliders), b18×2 (6 sliders)
- Mask-as-color feedback (A=masks, C read as color): b14 spore-galaxy, b18 neural-resonance
- Mouse coord units (normalized vs pixels): b18 fireworks-portrait-burst (3rd mouse-semantics bug class overall)
- Flat-0.0 depth clobber: b16 cosmic-jellyfish, b18 fireworks-portrait-burst
- Dead audio / fake FFT proxy: b15 void-spider, b16 phoenix
- OOB plasmaBuffer index: b18 holographic-sticker

### Environment notes (this VM, updated)
- naga now available — no more "defer to CI" caveat for the gate.
- node still via `export PATH=/root/emsdk/node/22.16.0_64bit/bin:$PATH`.
- No GPU — shaders not visually exercised.
- Not committed — working tree per user preference.
