# 2026-07-05

## Merge conflict resolution on radiant-seraph-plan-...

- User hit merge conflicts while (likely) pulling/merging main or another branch:
  - package.json
  - reports/wgsl_precommit_report.json
  - scripts/test_workgroup_gate.py
- "push fix" commits had baked-in conflict markers (bad state committed).
- Resolved:
  - package.json: kept craco for `test:wasm:unit` (consistent with craco start/build/test), preserved the e2e + full WASM test script aliases + swarm:upgrade.
  - report: chose recent local run data (but then refreshed).
  - test py: kept the descriptive 3-arg assert for nebula core.
- Post-resolution cleanup (the merge also surfaced policy drift):
  - 2-arg @workgroup_size is now *permitted* (from earlier "Fix workgroup size checks..." change).
  - Updated outdated tests, fixture comment, split_workgroup_issues docstring, and report.json to match current checker behavior (2-arg → no workgroup issue).
  - All `python scripts/test_workgroup_gate.py` now green.
- Commits:
  - ecb76084 fix: resolve merge conflicts...
  - 03d2a4ce fix: align tests/fixture/report with 2-arg ...
- Branch now ahead of origin/main by 4.
- Push note: upstream is set to origin/main but branch name differs → plain `git push` will fail with the "upstream branch does not match" error.
  Safe push command when ready: `git push origin radiant-seraph-plan-9630782556526266489`
  Or `git push -u origin HEAD`

## Other
- No other conflicts or markers remain.
- package.json JSON valid.
- Working tree clean after fixes.

## Git push unfamiliar message (this session)
- User hit the classic "The upstream branch of your current branch does not match the name of your current branch" when running plain `git push`.
- Root cause (as noted earlier): branch tracks `origin/main` (from merge/creation) while local name is the long `radiant-seraph-plan-9630782556526266489`.
- Confirmed via `git push --dry-run`:
  ```
  fatal: The upstream branch of your current branch does not match
  the name of your current branch.  To push to the upstream branch
  on the remote, use

      git push origin HEAD:main

  To push to the branch of the same name on the remote, use

      git push origin HEAD
  ```
- Branch still does not exist on remote (will be created on first push).
- Recommended: `git push -u origin radiant-seraph-plan-9630782556526266489` (creates + sets proper upstream so plain `git push` works going forward).
- Status: ahead by 6. Working tree clean.

## Fireworks generative shaders + image effects (2026-07-05)
- User request: create fireworks generative shaders and image effects.
- Delivered two new shaders in generative category (following full 13-binding canonical contract, ACES, audio, mouse, temporal feedback):
  1. **Fireworks Nocturne** (`gen-fireworks-nocturne`)
     - Classic multi-mortar night display.
     - Ascent comets → timed radial bursts with gravity + drag.
     - Secondary crackle, long falling embers, soft smoke.
     - Bass = bigger/more energetic shells, Treble = spark density.
     - Mouse hold = personal command shell.
     - Beautiful jewel/gold palette + trails via feedback.
  2. **Image Pyro** (`gen-image-pyro`)
     - Image-reactive: samples loaded image for launch bias + spark colors.
     - Bright regions of photo "ignite" and explode in their own hues.
     - Same gravity physics + trails, mouse barrage.
     - Perfect for turning portraits, cityscapes, or any image into fireworks.
- JSON definitions + WGSL in place.
- `node scripts/generate_shader_lists.js` + `npm run build:manifest` run → both appear in generative.json + unified manifest (371 generative total).
- Structural validation: 13 bindings, @workgroup_size(16,16,1), @compute + fn main + textureStore all present. Generator accepted cleanly.
- Vision fit: psychedelic energy + beautiful glowing trails + strange "photo coming alive as celebration".
- Next possible: a dedicated willow-cascade variant, more image-specific post effects, or thumbnail generation.

## GitHub Issues for agent reuse (2026-07-05)
Created two GitHub issues purpose-built to be handed to other AI agents (Claude, Kimi batches, Copilot, etc.) for expanding fireworks:

**#897 — Epic: Fireworks & Pyrotechnics Generative Shader Series**
- Umbrella issue.
- Documents the two new shaders (`gen-fireworks-nocturne` + `gen-image-pyro`).
- Detailed "Required Reading for AI Agents / Contributors" section.
- Contract checklist + generator instructions.
- Roadmap of future work.
- Labels: enhancement, generative, shader, fireworks.

**#898 — Fireworks Variants: Willow, Chrysanthemum, Multi-Stage & Advanced Image Pyrotechnics**
- Explicitly calls out parent #897.
- "AI Agent Instructions (copy-paste ready)" block at the top.
- Concrete specs for Willow Cascade, Chrysanthemum/Peony, multi-stage crackle, photo detonation, pixel flight, depth integration, and strange variants.
- Implementation notes, reuse guidance, and full agent checklist.
- Written in the same highly-detailed style as the WASM epic (#885 and children) that other agents have successfully executed.

These are ready to copy the issue number or link and give to any agent with "implement the tasks in #898 using the instructions in the body". The bodies contain everything needed to avoid hallucinating the contract or process.

## Fireworks library expansion (2026-07-05, session 2)

Added 3 new shaders to the fireworks family (#897 / #898 checklist):

1. **Willow Cascade** (`gen-fireworks-willow-cascade`)
   - Pure generative. Long drooping golden/silver trails, wind drift, strong temporal persistence.
   - Params: Trail Length, Droop, Wind, Color Warmth.

2. **Chrysanthemum Burst** (`gen-fireworks-chrysanthemum`)
   - Pure generative. Dense spherical bursts with concentric ring layers, inner white → outer color.
   - Params: Burst Size, Ring Layers, Spark Density, Color Spread.

3. **Pixel Detonation** (`gen-image-pixel-detonation`)
   - Image-reactive. Bright pixels + edges ignite; sparks lift source colors into gravity arcs.
   - Depth-aware launches, progressive photo dissolution, edge crackle.
   - Params: Burst Power, Ignition, Pixel Lift, Trail Fade.

- Generator: 374 generative shaders (was 371). Workgroup gate green.
- Family now: nocturne, image-pyro, willow-cascade, chrysanthemum, pixel-detonation (5 total).

## #898 completion (2026-07-05, session 2 continued)

Added **Crackle Palm** (`gen-fireworks-crackle-palm`) — multi-stage shell:
- Stage 1: primary radial burst
- Stage 2: delayed crackle sub-points (treble-driven micro-pops)
- Stage 3: palm frond arms (mids-driven)
- Params: Shell Energy, Stage Delay, Crackle, Palm Spread

Posted completion comment to GH #898: https://github.com/ford442/image_video_effects/issues/898#issuecomment-4884848863

**#898 checklist: fully complete.** Family now 6 shaders. 375 generative total.

## July 4th fireworks batch (2026-07-05)

User requested 4 generative + 4 image effects for July 4th US celebration.

### Generative (4 new)
- `gen-fireworks-ring-shell` — halo/donut ring bursts
- `gen-fireworks-crossette` — four-arm split crossette shells
- `gen-fireworks-kamuro-gold` — dense slow-falling gold/silver glitter
- `gen-fireworks-roman-candle` — vertical star barrage from tubes

### Image effects (4 new, in `shader_definitions/image/`)
- `fireworks-edge-ignite` — contours/edges launch pyrotechnics
- `fireworks-portrait-burst` — bright regions detonate as burst cores
- `fireworks-patriotic-july4` — red/white/blue July 4th palette
- `fireworks-depth-parade` — depth-layered sequential launches

Totals after batch: **379 generative**, **93 image**, **1194 unified manifest**.
Full fireworks library: **14 shaders** (10 generative + 4 image effects, plus earlier gen-image-* in generative).

## July 4th batch 2 (2026-07-05) — 4 more generative

- `gen-fireworks-horse-tail` — brocade/horse tail long golden streamers
- `gen-fireworks-comet-trail` — blazing comet head + luminous trail
- `gen-fireworks-fan-shell` — wide hemisphere fan spread
- `gen-fireworks-strobe-shell` — rhythmic multi-flash strobe bursts

**383 generative** total. Fireworks generative family: **14 shaders**.

## #898 fragment-spec vs canonical contract (2026-07-05, session 3)
- User pasted copy-paste fragment-shader snippets (`@fragment`, `import from util/helpers.wgsl`) for Willow-Cascade, Chrysanthemum, Photo-Detonation (`gen-image-pyro-advanced`).
- **Already implemented** in repo with canonical 13-binding `@compute` contract:
  - `gen-fireworks-willow-cascade` + JSON (Trail Length, Droop, Wind, Color Warmth)
  - `gen-fireworks-chrysanthemum` + JSON (Burst Size, Ring Layers, Spark Density, Color Spread)
  - `gen-image-pixel-detonation` + JSON (Burst Power, Ignition, Pixel Lift, Trail Fade) — covers Photo-Detonation / Advanced Image Pyrotechnics
- Pasted snippets are incompatible with Pixelocity engine (no `util/helpers.wgsl`, no fragment pipeline).
- Fixed missing `fn saturate` in willow-cascade.wgsl.
- Generator re-run: 379 generative. Naga valid + workgroup gate green for all three.

## Fireworks contributor kit (2026-07-05, session 4)
- User pasted draft contributor kit with incorrect contract (wrong bindings, `util/helpers.wgsl` imports, `@fragment`, wrong JSON schema).
- Created accurate `agents/FIREWORKS_CONTRIBUTOR_KIT.md` aligned with `WGSL_BUILTINS_GENERATIVE.md`, real JSON formats (generative `updatedParams` vs image `params`), validation commands, shipped roadmap + open variants.

## Fireworks physics & interaction expansion (2026-07-05, session 5)

Implemented the approved Phase 1 "Physics & Interaction Expansion" from the fireworks epic (#897):

1. **Wind & Ripple Fireworks** (`gen-fireworks-wind-ripple`)
   - Time-varying wind drifts every spark; `u.ripples` trigger directed radial barrages.
   - Params: Wind Energy, Ripple Sensitivity, Trail Length, Color Drift.

2. **Smoke Bloom Fireworks** (`gen-fireworks-smoke-bloom`)
   - FBM-based volumetric smoke puffs around bursts and along trails.
   - Cheap 5-tap feedback bloom from `dataTextureC` for luminous halos.
   - Params: Smoke Density, Bloom Strength, Burst Energy, Trail Decay.

3. **Audio Symphony Fireworks** (`gen-fireworks-audio-symphony`)
   - Smoothed bass envelope drives transient launches; mids layer secondary shells; treble scatters micro-sparks.
   - Params: Launch Density, Bass Drive, Mids Layering, Treble Sparkle.

- Added `agents/FIREWORKS_LIBRARY_GUIDE.md` as a quick-reference family guide: roster, contract checklist, canonical helper recipes, audio/mouse/depth conventions, parameter naming, and validation steps.
- Updated `agents/FIREWORKS_CONTRIBUTOR_KIT.md` roadmap to mark the three new shaders as shipped.
- Registration: `node scripts/generate_shader_lists.js` + `npm run build:manifest` → 386 generative shaders in unified manifest.
- Validation:
  - `python3 scripts/wgsl_precommit_gate.py --files public/shaders/gen-fireworks-*.wgsl` → all new files pass naga + bindgroup + 3-arg workgroup checks.
  - `node scripts/validate-naga.js public/shaders` → `gen-fireworks-wind-ripple.wgsl`, `gen-fireworks-smoke-bloom.wgsl`, `gen-fireworks-audio-symphony.wgsl` all ✅ (repo-wide report has unrelated pre-existing failures).
  - `npm test -- --watchAll=false --ci` → 23 suites, 215 tests passed.
- Family totals: 18 generative fireworks + 4 image-reactive pyrotechnic effects.
