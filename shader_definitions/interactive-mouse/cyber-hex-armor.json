{
  "id": "cyber-hex-armor",
  "name": "Cyber Hex Armor",
  "url": "shaders/cyber-hex-armor.wgsl",
  "description": "Articulated hex armor retracts around the pointer while beveled seams and audio-routed circuit traffic carry expanding click waves.",
  "params": [
    {
      "id": "hex_size",
      "name": "Hex Size",
      "default": 0.5,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "mapping": "zoom_params.x",
      "description": "Size of the hexagonal grid cells"
    },
    {
      "id": "glow_intensity",
      "name": "Glow Intensity",
      "default": 0.5,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "mapping": "zoom_params.y",
      "description": "Intensity of the hex edge glow"
    },
    {
      "id": "reveal_radius",
      "name": "Reveal Radius",
      "default": 0.3,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "mapping": "zoom_params.z",
      "description": "Radius around mouse where armor retracts"
    },
    {
      "id": "border_thickness",
      "name": "Border Thickness",
      "default": 0.3,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "mapping": "zoom_params.w",
      "description": "Thickness of hexagon borders"
    }
  ],
  "features": [
    "mouse-driven",
    "overlay",
    "audio-reactive",
    "temporal",
    "exact-history",
    "aces-tone-map",
    "three-band-audio",
    "held-pointer",
    "click-reactive",
    "articulated-hex-armor"
  ],
  "updatedParams": [
    { "index": 0, "name": "Hex Size", "default": 0.5, "min": 0, "max": 1, "step": 0.01 },
    { "index": 1, "name": "Glow Intensity", "default": 0.5, "min": 0, "max": 1, "step": 0.01 },
    { "index": 2, "name": "Reveal Radius", "default": 0.3, "min": 0, "max": 1, "step": 0.01 },
    { "index": 3, "name": "Border Thickness", "default": 0.3, "min": 0, "max": 1, "step": 0.01 }
  ],
  "feedbackPacking": "A/C = final ACES display RGBA with bounded exact circuit persistence loads; B is unused",
  "tags": [
    "filter",
    "image-processing"
  ]
}
